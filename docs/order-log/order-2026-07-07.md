# 依頼・回答ログ

このファイルは、開発依頼と回答の記録用です。

## 運用ルール
- 依頼者が「## 依頼」セクションに依頼内容を記入
- Claudeが「### 回答」として回答を追記
- 完了した依頼はそのまま履歴として残す
- 依頼者は、依頼管理のため、Claudeのコミットハッシュを記録する
- order.mdが500行を超えたら、キリの良いところで、/order-log以下に保管する。

---


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show b80ef3a
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます。
確認しました。

引き続き、修正を行います。
1. 修正
- 1. 宙の窓のプレビュー領域
  - 再生ボタン周りの修正
    - 再生オプションが①②の場合で、プレビュー中にプレビュー画面上部に金色で表示されるコマ数情報などは、上部ではなくて、下部に表示できないでしょうか。星景写真を撮る方からすると空に文字があるのは、ちょっと。
- 2. 辻検索メニュー
  - 辻検索の結果リスト項目に、オフセット中心角、天体方位角差と天体視高度差をデッサンに追加しました。
  - 検索中心オプションをデッサンに追加しました。
- 3. My辻検索メニュー
  - 辻検索の結果リスト項目に、オフセット中心角、天体方位角差と天体視高度差をデッサンに追加しました。
  - 検索中心オプションをデッサンに追加しました。

では、実装と修正をよろしくお願いいたします。

### 回答 (2026-07-07) 修正ラウンド第7弾(進捗表示を下部へ・結果リスト3項目・検索中心オプション(点/線))
いただいた項目すべてに対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. 進捗・コマ数情報をプレビュー画面の下部へ → 対応済み**
- 再生①②・書き出し中の金色の表示(再生中/再生準備中/動画生成中/書き出し中)を、プレビュー画面の**中央下部(下端から10px)**に移しました。空側に文字が被らなくなります。コントロールメニューを開いていてもメニューには重なりません(表示はプレビュー画面の内側)。デッサン05の再生ボタンとファイル出力の説明にも追記しました。

**2-1・3-1. 結果リストに3項目を追加 → 対応済み**
- 辻検索とMy辻検索一括計算の結果リストの「視半径」と「標高グラフ」の間に、以下の3列を追加しました(列見出しクリックのソートにも対応)。
  - **天体方位角差**「+-0.0000°」= 天体の方位角 − 目的点の方位角(基準方位角)。±180°に正規化し、目的点より右(時計回り)なら正・左なら負の符号つき。
  - **天体視高度差**「+-0.0000°」= 天体の視高度 − 目的点の視高度(基準視高度)。上なら正・下なら負の符号つき。
  - **オフセット中心角**「0.0000°」= その検索で使った天の川オプションのオフセット中心角(辻検索=基本オプションの値、My辻検索=その行の値)。
- **My辻検索のFile出力(結果CSV)**もデッサンの列構成どおりに整備しました: 視半径の後に天体方位角差・天体視高度差、オフセット視高距離の後に**オフセット回転角・オフセット回転仰角・オフセット中心角・標高グラフ**(OK/NG/-)を追加しています(回転角・回転仰角・標高グラフはデッサンに元々あったのに実装のCSVから漏れていた列で、今回まとめて追加しました)。なお「標高グラフメッセージ」はデッサンで削除いただきましたが、実装のCSVにはもともとこの列が存在しなかったため、削除の作業はありません(今後も出力しません)。

**2-2・3-2. 検索中心オプション(点/線) → 対応済み**
- 辻検索メニュー(許容範囲の下)とMy辻検索の各行に、**「検索中心オプション」ラベル+「:オフセット点」(初期値オン)/「:基準点からオフセット点までの線」のラジオボタン**を追加しました。
- 判定の仕様:
  - **オフセット点(点)**: 従来どおり、検索中心=基準点+オフセットの1点から許容範囲(方位角/視高度)内をヒットとします。
  - **線**: 基準点(基準方位角/基準視高度)からオフセット点までの**線分**を検索中心とし、「線分上のいずれかの点から許容範囲内」に入る瞬間をヒットとします(天体が線分のどこかをかすめれば拾える=点で外れる途中経過も検索できます)。
  - 精度角距離・精度記号(◎○△-)は、線のときは**線分への最近点からの角距離**で算出します。
- 検証では、点モードで10°外れてヒットしない固定星が、同じ許容範囲の線モード(基準点→オフセット点の線分が星の日周経路を横切る配置)で正しくヒットし、精度角距離も最近点基準(ほぼ0°)になることを確認しました。
- 辻検索メニューの選択はローカルストレージ・URLに保存されます(URL短縮辞書はv4に版上げ・発行済みURLはそのまま読めます)。My辻検索は行ごとに独立して選択でき(辻検索取得時は辻検索メニューの選択をコピー)、**設定CSVも13列目に「検索中心」(point/line)を追加して37列**にしました。これまでの36列・21列のCSVも列数で判別してそのまま取り込めます(デッサン08のCSV項目一覧も37列に更新)。

**デッサン03/08の段目・項番の振り直し → 対応済み**
- 挿入で重複していた段目・項番をファイル順に振り直しました(03: 段目0〜33・項番1〜72、08: 行1〜30・項番1〜89)。**本文中の項番参照(「34.時間フィルタ」「84.行追加ボタン」など約360箇所)も新番号に追従**させ、参照と定義の一致を機械検証済みです。あわせて、以前から仕様詳細側で古い番号のままだった「標高オプション」見出し(03/08)も現行の項番(69./78.)に直しました。

**検証**: ①進捗ピルがプレビュー下部・メニュー非重複 ②ラジオ既定=オフセット点・URL v4往復・v2辞書凍結・ワーカーの点/線判定(点0件/線1件・最近点距離) ③辻検索結果の3列と値の一致(符号つき差分・オフセット中心角) ④My辻検索の行独立ラジオ・File出力CSV59列(ヘッダーと本体の列数一致・回転角/回転仰角/中心角/標高グラフ入り・メッセージ列なし) ⑤設定CSV37列往復・36列/21列互換、の全グループPASS。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 358e2b3
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます。
確認しました。

引き続き、修正を行います。
1. 修正
- 1. 辻検索メニュー
  - 「オフセット方位角」「オフセット視高度」「オフセット方位距離」「オフセット視高距離」「オフセット回転角」「オフセット回転仰角」を「辻オフセット方位角」「辻オフセット視高度」「辻オフセット方位距離」「辻オフセット視高距離」「辻オフセット回転角」「辻オフセット回転仰角」に名称変更をデッサンに反映。
- 2. My辻検索メニュー
  - 「オフセット方位角」「オフセット視高度」「オフセット方位距離」「オフセット視高距離」「オフセット回転角」「オフセット回転仰角」を「辻オフセット方位角」「辻オフセット視高度」「辻オフセット方位距離」「辻オフセット視高距離」「辻オフセット回転角」「辻オフセット回転仰角」に名称変更をデッサンに反映。
  - 「辻」が一文字増えたので、横幅が足りないので、テキストボックスの横幅を少し狭める調整をすること。縦のテキストボックスのラインが綺麗に揃うことが望ましい。
- 3. 宙の窓メニュー
  - 「オフセット方位角」「オフセット視高度」を「カメラオフセット方位角」「カメラオフセット視高度」に名称変更をデッサンに反映。
  - 「辻オフセット方位角」「辻オフセット視高度」「辻オフセット方位距離」「辻オフセット視高距離」「辻オフセット回転角」「辻オフセット回転仰角」の追加をデッサンに反映。
- 4. 宙の窓メニューのプレビュー領域
  - 「オフセット方位角」「オフセット視高度」を「カメラオフセット方位角」「カメラオフセット視高度」に名称変更をデッサンに反映。
  - 「辻オフセット方位角」「辻オフセット視高度」「辻オフセット方位距離」「辻オフセット視高距離」「辻オフセット回転角」「辻オフセット回転仰角」の追加をデッサンに反映。

よろしくお願いいたします。

### 回答 (2026-07-07) 修正ラウンド第8弾(辻/カメラオフセットの名称変更・宙の窓への辻オフセット6項目追加)
いただいた項目すべてに対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. 辻検索メニューの名称変更 → 対応済み**
- 6項目のラベルを「辻オフセット方位角(°):」〜「辻オフセット回転仰角(°):」に変更しました(ヘルプの説明文も追従)。プレースホルダーはデッサンどおり従来のままです。内部のID・保存キー・URLパラメータは変えていないので、保存済みの設定や発行済みURLには影響ありません。

**2. My辻検索メニューの名称変更+幅調整 → 対応済み**
- 各行の6ラベルを「辻オフセット…」に変更し、あわせて**ラベル幅を73px→80pxに広げ、数値テキストボックスを75px→68pxに狭めました**(1組の合計148pxは不変)。行内の全テキストボックスを同じ幅にしたので、**縦のラインは左右2列ともきれいに揃います**(検証: 行のはみ出しなし・入力右端の縦整列が2クラスタに収まることを確認)。
- File出力(結果CSV)の列名も「辻オフセット方位角」〜「辻オフセット回転仰角」に変更しました。なお、**設定CSV(全CSV入力/追加CSV入力/CSV出力)の見出し行はデッサン08のCSV項目一覧が「オフセット方位角」等のままなので変更していません**(変更をご希望でしたらデッサンの項目一覧の更新とあわせて対応します。見出し行は取り込み時に読み飛ばすため、どちらでも過去ファイルの互換に影響はありません)。

**3・4. 宙の窓メニュー+コントロールメニュー → 対応済み**
- 「オフセット方位角/視高度」を**「カメラオフセット方位角/視高度」**に変更しました(両メニューとも。従来どおり辻検索とは非連動のカメラの向きの操作で、ドラッグパンと連動)。
- 基準視高度の下(コントロールメニューは時刻の下)に**辻オフセット6項目を追加**しました:
  - **辻オフセット方位角/視高度**(編集可・ステップ0.01): **辻検索メニューの辻オフセット方位角/視高度と双方向連動**します。宙の窓・コントロールメニュー・辻検索メニューのどこで編集しても、3箇所すべての値と読み取り専用4項目が即座に揃います(編集中の欄は上書きしません)。
  - **辻オフセット方位距離/視高距離/回転角/回転仰角**(読み取り専用): 辻検索メニューと同じ計算値を表示します。
- 検証: 宙の窓側で方位角12.5°・ctrl側で視高度-3.25°を入力→3箇所の入力欄と読み取り専用4種×3箇所がすべて一致、辻検索メニュー側の編集も宙の窓側へ反映、カメラオフセット(soraOffsetAz)は独立で不変、を確認しました。

**デッサン05の段目・項番の振り直し → 対応済み**
- 挿入で重複していた両節の番号をファイル順に振り直しました(宙の窓メニュー節: 段目0〜50・項番1〜57、プレビュー領域節: 段目1〜35・項番1〜49)。本文中の項番参照(視界範囲の相互参照44./45.など)も新番号に追従させ、単調増加を機械検証済みです。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show b7d85f1
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます。
確認しました。

- File出力(結果CSV)の列名も「辻オフセット方位角」〜「辻オフセット回転仰角」に変更しました。なお、**設定CSV(全CSV入力/追加CSV入力/CSV出力)の見出し行はデッサン08のCSV項目一覧が「オフセット方位角」等のままなので変更していません**(変更をご希望でしたらデッサンの項目一覧の更新とあわせて対応します。見出し行は取り込み時に読み飛ばすため、どちらでも過去ファイルの互換に影響はありません)。
  - 変更を希望です。よろしくお願いいたします。

引き続き、修正を行います。
1. 修正
- 1. 宙の窓メニュー
  - 目的点チェックボックスと、検索中心チェックボックスを追加したことをデッサンに反映。段数、項番は、ずれています。
  - 焦点距離テキストボックスと、合焦距離スライダーの初期値と制約を修正したのをデッサンに反映。
  - フィッシュアイスライダーとパノラマスライダーの初期値を制約を修正したことをデッサンに反映。
  - カメラオフセット方位角、カメラオフセット視高度、視界範囲テキストボックスの制約を修正したことをデッサンに反映。
- 2. 宙の窓メニューのプレビュー領域
  - 「カメラオフセット方位角」「カメラオフセット視高度」の位置の変更をデッサンに反映。段数、項番は、ずれています。

よろしくお願いいたします。

### 回答 (2026-07-07) 修正ラウンド第9弾(設定CSV列名・目的点/検索中心チェック・7項目の非保存化・ctrl行移動)
いただいた項目すべてに対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**0. 設定CSVの見出し変更 → 対応済み**
- 設定CSV(全CSV入力/追加CSV入力/CSV出力)の見出しの9/10列目を**「辻オフセット方位角」「辻オフセット視高度」**に変更し、デッサン08のCSV項目一覧も更新しました。見出し行は取り込み時に読み飛ばすため、旧見出しの過去ファイルもそのまま使えます。

**1-1. 目的点(+)・検索中心(×)チェックボックス → 追加しました**
- 画面中心(+)の下に**「:目的点(+)」「:検索中心(×)」**(各初期値オン)を追加しました。設定はローカルストレージ・URLに保存されます(URL短縮辞書はv5に版上げ・発行済みURLは互換)。
- **目的点(+)**: これまで常時表示だった目的点の赤い十字(+)マーカーの表示/非表示を切り替えます。
- **検索中心(×)**: 検索中心(=目的点方向+辻オフセット方位角/視高度)に**金色のX字(×)マーカー**を表示します。辻検索メニューの検索中心オプションが「基準点からオフセット点までの線」の場合は、**目的点から検索中心までの金色の線**も表示します(「オフセット点」の場合は×のみ)。地形に隠れず常に見え、画面サイズに依らず一定の大きさです。
- 辻オフセット(3箇所どこで編集しても)・検索中心オプションの変更で、×マーカーと線は即座に追従します。再生(アニメーション)中も表示されます。

**1-2〜1-4. 7項目のローカルストレージ非保存化 → 対応済み**
- デッサンの新しい制約どおり、**焦点距離・合焦距離・フィッシュアイの歪み・パノラマ水平画角・カメラオフセット方位角/視高度・視界範囲**の7項目をローカルストレージに保存しないようにしました。サイトへアクセスするたびに初期値(焦点距離35mm・歪み50%・パノラマ=レンズ画角・カメラオフセット0・合焦距離/視界範囲=観測点と目的点の距離由来)が適用され、**URL取得のURLからは従来どおり復元されます**。
- あわせて、これまでの「宙の窓ボタンを押すたびに焦点距離を35mmへリセット」する動作は廃止しました(非保存化により毎アクセスで初期値になるため。同じセッション内でパネルを閉じ開きしても値は保持されます)。

**2. プレビュー領域のカメラオフセット位置 → 移動しました**
- コントロールメニューのカメラオフセット方位角/視高度の2行を、デッサンどおり**時刻の直後(辻オフセット群の前)**に移動しました(宙の窓メニュー側は現状の並びのまま=デッサンどおり)。

**デッサン05の段目・項番の振り直し → 対応済み**
- 両節をファイル順に振り直しました(宙の窓メニュー節: 段目0〜52・項番1〜59(目的点=57・検索中心=58)、プレビュー領域節: 段目1〜35・項番1〜49(カメラオフセット=17/18・辻オフセット=19〜24))。単調増加を機械検証済みです。

**検証**: ①設定CSV見出し9/10列目の変更・37列・往復一致 ②7項目がlocalStorageに残らない(それ以外のキーは保存継続)・トグルで焦点距離が保持・URLパラメータに7項目あり ③チェック2行の位置と既定オン・URL v5往復・v4以前の辞書凍結 ④目的点(+)オフで赤十字消滅・検索中心×が別位置に表示・線モードで金色の線出現・オフで消滅・再生中も表示・辻オフセット編集で×が移動 ⑤ctrl行順(時刻→カメラオフセット→辻オフセット)、の全グループPASS。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show aa323dc
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます。
確認しました。

辻検索で、検索中心オプションを基準点からオフセット点までの線にしたときに、東京タワーと富士山で実施した場合、パール富士で、精度角距離が◎なのに、宙の窓でプレビューすると、随分とずれているようです。
月	◎	0.00097°	2026年07月07日	(火)	09:49:48	
プレビューがずれているのか、辻検索の線での検索結果に誤りがあるのか、どちらなのでしょうか。
ちなみにプレビューがずれているのは、辻オフセット方位角を45°、辻オフセット視高度を45°にしています。
この辻オフセットの線と随分と離れています。
どうなのでしょうか。
調査をお願いいたします。

### 回答 (2026-07-07) 調査結果: 検索は正しく、プレビューの「線」の描き方が原因でした(修正済み)
調査しました。結論からお伝えすると、**辻検索の線での検索結果は正しく、宙の窓のプレビューに描いていた金色の線の「描き方」が検索の定義とズレていた**のが原因です。線の描き方を検索と同じ定義に修正しました。

**調査の詳細(東京タワー→富士山・辻オフセット45°/45°で再現)**
- ご提示の辻時刻 2026/07/07 09:49:48 の月の位置を計算すると、方位角264.206°・視高度15.33°でした。
- 検索の「線」は、**方位角/視高度の平面上の直線**(基準点から、方位角+45°・視高度+45°のオフセット点まで、方位角と視高度を同じ割合で進む線)です。ヒット判定も精度角距離もこの直線が基準です。この直線と月の角距離は**0.0008°**で、ご提示の◎0.00097°と一致 — つまり**月は検索の線の上に正確に乗っており、検索は正しい**です。
- 一方、プレビューの金色の線は(第9弾で実装した際に)**天球上の大円**(2点を球面上で最短につなぐ弧)で描いていました。両端は同じでも中間は別の曲線で、45°/45°のような大きなオフセットでは**中間で約3.2°**(月の直径6個分以上)離れます。ご覧になった「月が線から随分離れている」のはこのズレです。
- 低い高度では方位角1°の見かけの幅が広く、高い高度では狭くなるため、「方位角/視高度を等割合で進む線」と「球面上の最短の弧」は途中で離れていきます。許容範囲も方位角/視高度で指定する仕様なので、**検索の定義(方位角/視高度平面の直線)が一貫した正**と判断しました。

**修正内容**
- プレビューの金色の線を、検索とまったく同じ**方位角/視高度の線形補間**(48分割)で描くようにしました。これで検索で◎になった天体は、プレビューでも線の上(または線のすぐ近く)に見えます。
- 検証: 上記の再現ケースで、修正後の描画線と月の角距離は**0.0019°**(=線上)になりました。点モード(×のみ)・チェックボックスのオン/オフなど既存動作の回帰も確認済み・JSエラー0件です。

パール富士の検索、ぜひお楽しみください。次のご指摘をお待ちしています。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 1aa4fd1
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます。
確認しました。
ぴったりになりました。
ありがとうございます。

それから、前から気になっていたのですが、宙の窓のプレビュー領域の日時情報のコントロールで、タイムピッカーの両脇にスペースがあって、上のカレンダーとずれています。タイムピッカーが幅を広げられないのなら、上のカレンダーの幅を狭めるとかできないでしょうか。また、「▶︎」「▶︎▶︎」のボタンサイズが異なります。日時情報メニューのボタンサイズと同じにして欲しいです。
いかがでしょうか。
よろしくお願いいたします。

### 回答 (2026-07-07) 修正ラウンド第11弾(プレビュー領域の日時コントロールの幅揃え・ボタンサイズ統一)
両方とも対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**原因**
- 「◀◀」「◀」「▶︎」「▶︎▶︎」のボタン: 日時情報メニューのボタンには**幅34pxの固定指定**があるのに対し、プレビュー領域のコントロールメニューのボタンは幅指定がなく文字数なりの幅(「▶︎」が細く「▶︎▶︎」が太い)になっていました。
- カレンダーとタイムピッカーのズレ: どちらも幅指定がなく、ブラウザ固有の幅(日付ピッカーの方が広い)で表示されていたため、時刻の行だけ合計幅が狭く、両脇にスペースが出ていました。

**修正**
- コントロールメニューの8つのボタン(◀◀/◀/▶︎/▶︎▶︎ ×日付・時刻)を、**日時情報メニューとまったく同じ幅34pxの固定サイズ**に統一しました。
- カレンダー(日付ピッカー)とタイムピッカーを、日時情報メニューの時刻欄と同じ方式(行の残り幅いっぱいに伸縮・中央揃え)にしました。両行とも「34px×4ボタン+可変ピッカー」の同じ構成になるので、**ピッカーの左端・右端が上下でぴったり一致し、両脇のスペースもなくなります**。
- 検証: 8ボタンすべて幅34px(日時情報メニューの◀ボタンと同幅)・日付/時刻ピッカーの左右端の差0px・行末尾のボタン右端も一致、を確認しました。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 210c933
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます。
確認しました。
スマホでも大丈夫でした。

では、大型機能追加に参ります。
「辻メッシュ検索」機能を追加します。
デッサンは、`00-overview.md`,`01-location.md`,`04-tsujimeshsearch.md`です。
実装をよろしくお願い申し上げます。

### 回答 (2026-07-07) 大型機能追加: 辻メッシュ検索
デッサン04の辻メッシュ検索を一式実装しました(ヘッドレスChromiumで検証・JSエラー0件)。

**実装した機能**
- **「辻メッシュ」トグルボタン**: 位置情報メニューの8段目(辻検索と宙の窓の間)に追加。
- **辻メッシュ検索メニュー**: 辻検索メニューと間違わないよう**薄い金色の背景**で、検索期間・基準方位角/視高度(観測点/目的点から自動算出・手動編集可)・辻オフセット6項目・許容範囲・検索中心オプション(点/線)・天の川オプション(オフセット中心角は基本オプションと連動)・月齢フィルタ・時間フィルタ(開始/終了グループ)・**精度フィルタ(ラジオ: ◎/◎×2/◎×4/◎×8、既定◎)**・標高オプション(OK/NG)・URL取得、を新設しました。設定はローカルストレージ・URLに保存されます(短縮辞書はv6へ版上げ・発行済みURLは互換)。
- **検索結果表示領域**: 画面下部に辻検索と同型のパネル(結果リスト21列・列見出しソート・進捗バー)+**最大化トグル(⛶)**で2/3まで拡大(下に宙の窓などがあるときは1/3)+下部に**開閉式コントロール**(No.N/日時/辻時刻の表示と、◀/行番号スライダー/▶で選択行を変更)。**辻検索とは同時表示不可**(互いに開くと相手が閉じます)、全天儀/標高グラフ/宙の窓とは同時表示できます。
- **地図マーカー**: 精度フィルタ以内になった画素を**白色マーカー**で表示し、リストで選択中の行の画素は**金色**に変わります。**金色マーカーをクリックするとその画素を観測点に設定**できます(位置情報を取得)。マーカーはCanvas描画で軽量化し、表示上限2万画素(超過時はステータスに明記)。

**探索アルゴリズム(性能の要)**
- 9枚のDEM標高タイル(dem_png・ズーム14・観測点を含むタイル+8近傍)の全画素=最大約59万画素を画素ごとに時間全探索すると計算量が爆発するため、**探索を反転**しました:
  1. 全画素について「画素→目的点」の基準方位角(WGS84測地線)・基準視高度(見かけ高度・画素標高=DEM値/高さ0m)を前計算し、方位角0.1°ビンの索引を作る。
  2. 天体の毎分の方位角/視高度は**領域中心で1回だけ**計算し、ビン索引から候補画素だけを引いて精度フィルタ(球面角距離≤ε)で判定する。
  3. 秒単位の精細化も天体位置を1回だけ計算して全ヒット画素で共有(画素側は算術のみ)。
- 地平座標系は観測位置ごとに回転する(中心から3kmの画素で最大約0.03°)ため、**画素ごとの座標系回転補正**(局所鉛直の傾き+子午線収束)を入れています。検証では、ヒット画素を観測点にして既存の辻検索ワーカーで直接検索した結果と、**時刻差0秒・精度角距離差0.00003°(約0.1秒角)**で一致しました。
- 計算量はAstronomy呼び出しが既存辻検索と同オーダーのため、1天体×365日で10秒前後+前計算数秒が目安です(ワーカープール並列・CPU数+1)。

**仕様の解釈(デッサンに明記がない点)**
- **結果リストの1行=「天体×日」のイベント**です(行の辻時刻・方位角などはその日の最良=最小距離画素の値)。行に紐づく「その日に精度フィルタ以内になった全画素」がマーカー表示され、画素ごとの最良時刻・距離はマーカーのツールチップで確認できます。
- **許容範囲方位角/視高度**は「メニューの基準方位角/視高度から±許容範囲内にある画素だけを検索対象にする」画素の絞り込みとして使います(検索の当たり判定は精度フィルタ)。
- **標高オプション**は行の最良画素→目的点で可視判定します(DEMタイルはキャッシュを使い回し。判定は先頭300行まで・超過分は「-」でステータスに明記)。
- 精度記号は精度フィルタ(≤±0.125°)以内のみを検索するため常に「◎」表示です。1日で画素2万件を超えた場合は距離の近い順に2万件へ制限し、ステータスに明記します。

**検証**: ①UI一式(金色背景・ラジオ既定◎・時間フィルタ生成・辻検索⇄辻メッシュの相互クローズ・最大化2/3⇄1/3・URL v6往復と旧辞書凍結) ②東京タワー付近→富士山・太陽・精度◎で検索し、ヒット行・白/金マーカー・21列リスト・No.表示 ③**グラウンドトゥルース比較**(ヒット画素3点で既存辻検索ワーカーと時刻/距離一致) ④精度×4でヒット画素が単調減少・線モード動作・行選択で金色切替・金色クリックで観測点設定 ⑤閉→再開のキャンセル動作、の全グループPASS。

実際の運用では、まず精度フィルタ◎・検索期間365日程度から試して、ヒットが多すぎる場合は◎×2〜×8で絞るのがおすすめです。次のご指摘をお待ちしています。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 08be90b
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます！
想像以上のものができましたね！
心より感謝御礼を申し上げます。
ここまですごいとはおどろきです！

早速いくつか質問をしたいです。
そして、機能追加もしたいです。

- 質問
「マーカーは上限2万で省略」とありますが、結果で描画したマーカーの数が2万件を超えたかどうかは分かるのでしょうか。
また、「一部の日で画素2万件超を省略」という意味がよくわからず、画素を2万件超えたのかどうかもわかりません。
どういう対象が省略されてしまうのでしょうか。
あっわかりました！各画素にマーカーを配置するから、一部の日で2万件以上の画素を計算・表示するためには、マーカーも2万件以上必要なので、省略していますよ、ということを伝えているのですね。
それなら、そういった制約は、ヘルプに記載をしましょう。
それから、どうしてこんなに処理速度が速いのでしょうか。
どんな点を工夫したのでしょうか。
用いた技術などはありますでしょうか。

- 機能追加
DEM標高タイルの9タイル分が画面内に収まるようにズームして欲しいです。多少はみだしても良いです。
どちらかというと、最大ズームして、結果の行を選択している集合の重心あたりを画面の中心に表示するように、検索結果画面を選択したら、画面移動をして欲しいです。
いかがでしょうか。

- 機能修正
「マーカーは上限2万で省略」と記載するよりは、「単位日当たりのマーカー数:20,000以上(省略中)」として、検索結果の行番号を選択するたびに、その日の実際のマーカー数も表示してはいかがでしょうか。

- 不具合の可能性
検索結果表示領域のコントロール領域の行番号スライダーを移動させても、白いマーカーが移動せずに同じ場所に描画されています。
金色のマーカーは、行番号スライダーを移動させると、それに合わせて、画面内で移動します。
不具合かどうか、調査をお願いいたします。

どうでしょうか。
ご一考のほど、よろしくお願いいたします。

### 回答 (2026-07-07) 辻メッシュ検索の続き(質問回答・行選択ズーム・マーカー数表示・白マーカー配分の改善)
嬉しいお言葉、ありがとうございます。4点すべて対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**質問1: マーカー上限の意味 → ご理解のとおりです+ヘルプに記載しました**
- おっしゃるとおり「各画素に1つのマーカーを置くので、数が多いと描画が重くなるため省略している」という意味です。上限は2種類あります:
  - **1日(1行)あたりのヒット画素**: 20,000画素を超えた日は、距離の近い順に20,000件へ省略(この行は「20,000以上(省略中)」表示)。
  - **地図の白マーカーの合計**: 計20,000画素まで(超過分は間引き)。
- ヘルプに**「辻メッシュ検索」の項を新設**し、これらの制約(行=天体×日・白/金マーカーの上限・標高判定は先頭300行など)を一覧で記載しました。

**質問2: どうして処理速度が速いのか**
- 一番の工夫は**「探索の反転」**です。59万画素それぞれで時間を全探索すると「59万×毎分の天体計算」で破綻しますが、天体の位置は近くの画素同士でほぼ同じことを利用して、①全画素の「画素→目的点」の方位角/視高度を先に一括計算して**方位角0.1°ごとの索引**を作り、②天体の毎分の位置は**領域中心で1回だけ**計算して、索引から「その方位に目的点が見える画素」だけを取り出して判定します。天体計算の回数が画素数に依存しなくなるので、計算量は通常の辻検索1回分とほぼ同じになります。
- 秒単位の精細化も、天体の秒ごとの位置を1回だけ計算して全ヒット画素で使い回します(画素側は足し算掛け算だけ)。
- ただし地平座標(方位角/視高度)は観測位置ごとに座標系が少し回転するため(中心から3km離れた画素で最大約0.03°)、**画素ごとの座標系回転補正**(局所鉛直の傾き+子午線収束の一次補正)を入れて精度を確保しています。検証では、ヒット画素を観測点にした通常の辻検索と時刻差0秒・角距離差0.00003°(約0.1秒角)で一致しています。
- 使った技術: Web Worker(CPU数+1のプール並列)・TypedArray(数百万要素の座標/索引を高速に共有)・astronomy-engine(天体位置)・GeographicLib(WGS84測地線)・LeafletのCanvasレンダラー(2万マーカーの軽量描画)・DEMタイルの共有キャッシュ、です。

**機能追加: 行選択で地図移動 → 対応しました**
- 結果の行を選択するたび(行クリック・スライダー・◀▶とも)に、**その行のヒット画素集合が収まる最大ズーム(上限z15)で、集合の重心あたりが画面中心**に来るように地図が移動します。下部の検索結果表示領域に隠れないよう、パネルの高さぶんを避けて配置します。

**機能修正: マーカー数の表示 → 対応しました**
- コントロール領域の日時の横に、選択中の行の**「マーカー数:N」**(その日のヒット画素数)を表示し、行を選ぶたびに更新されます。1日で20,000画素を超えた行は**「マーカー数:20,000以上(省略中)」**と表示します。ステータスの文言も「単位日当たりのマーカー数:20,000以上の日あり(省略中)」に改めました。

**不具合調査: 白マーカーがスライダーで動かない → 半分仕様・半分不具合でした(修正済み)**
- 仕様面: 白マーカーは「全結果行のヒット画素の和集合」なので、行を切り替えても白は動かず、**選択行の画素が金色に変わる**のが意図した動作です。
- 不具合面: ただし白マーカーの上限2万を**先頭の行から順に使い切る**実装だったため、ヒット画素が多い検索では**後半の行の白マーカーが1つも描画されず**、「白がいつも同じ場所」に見えていました。これを**行ごとの均等割り当て**(上限2万を行数で割った件数ずつ、各行から間引いて表示。最良画素は必ず表示)に修正し、すべての行の白マーカーが地図に現れるようにしました。検証で全12行の最良画素が白マーカーとして存在することを確認済みです。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show ea20ceb
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-07)
Claudeさん、ありがとうございます。
白いマーカーが分散して存在するようになりました。
金色のマーカーも、精度フィルタで絞ると数が少なくなりました。
確認できました。

マーカー数の表示位置を変更しました。
デッサン`04-tsujimeshsearch.md`に反映済みです。
実装をお願いいたします。

### 回答 (2026-07-07) 微修正: マーカー数の表示位置をデッサンどおり独立行へ
対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

- デッサン04の更新どおり、コントロール領域の**「マーカー数:」を独立した行(5段目)**に移しました。並びは 4段目=No./日時/辻時刻、5段目=マーカー数、6段目=◀/行番号スライダー/▶ です。表示内容(実数・「20,000以上(省略中)」)と更新タイミングは従来どおりです。
- 検証: マーカー数ラベルが日時行の下・スライダー行の上の独立行にあり、行選択のたびに値が更新されることを確認しました。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 66e1336
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
確認しました。

もう少し修正をします。
1. `04-tsujimeshsearch.md`
マーカーレイヤーの表示/非表示チェックボックスを追加しました。デッサンに反映済みです。
2. `05-soranomado.md`
天体軌跡あたりの項目の並び順を変えました。デッサンに反映済みです。

よろしくお願いいたします。

### 回答 (2026-07-08) 微修正: マーカーレイヤーの表示/非表示チェック・宙の窓チェックボックスの並び替え
両方とも対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. 「:マーカーレイヤー」チェックボックス → 追加しました**
- 辻メッシュ検索のコントロール領域の7段目(行番号スライダーの下)に**「:マーカーレイヤー」チェックボックス(初期値オン)**を追加しました。オフにすると地図のマーカーレイヤー(白・金の両方)が非表示になり、オンで再表示されます。非表示のまま再検索しても非表示が維持され、オンに戻すと検索結果のマーカーがそのまま現れます。

**2. 宙の窓メニューのチェックボックスの並び替え → 対応しました**
- デッサン05の更新どおり、**「:天体軌跡」と「:画面中心(+)」を同じ行**に、**「:目的点(+)」と「:検索中心(×)」を同じ行**にまとめました(4行→2行)。あわせてデッサン05の段目の欠番(49・51段目)をファイル順に振り直しました(メニュー節: 段目0〜50・項番1〜59は不変・単調増加を機械検証済み)。

**検証**: チェックが既定オン・スライダー行の下に配置・オフで白/金レイヤーが地図から消えオンで復帰・非表示中の再検索でも状態維持、宙の窓の2行×2チェックの行構成、をすべて確認しました。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show d5240af
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
確認しました。

微修正をします。
1. `05-soranomado.md`
画像サイズの初期値と制約を変更しました。
デッサンに反映済みです。

よろしくお願いいたします。

### 回答 (2026-07-08) 微修正: 画像サイズ(手動:MB)の初期値・制約の変更
対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

- デッサン05の更新どおり、宙の窓の**「画像サイズ(手動:MB)」の初期値を8→140、制約を1〜100→1〜102400(ステップ0.1のまま)**に変更しました。入力欄のmin/max・ツールチップ・状態の正規化(範囲丸め)・総画像容量(GB)計算のフォールバック値も揃えて更新しています。
- 検証: 既定値140・入力欄の制約1〜102400・200000を入力すると102400へクランプされることを確認しました。保存済みの設定値はそのまま尊重され、範囲外の場合のみ新しい範囲に丸められます。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 0fa3988
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
確認しました。

質問があります。
辻メッシュ検索をする場合、基準の観測点がありますが、観測点には、緯度経度、標高、高さがありますよね。
画素によって、緯度経度、標高は異なるので、その画素の位置での値に置き換えられると思うのですが、高さはどうなのでしょうか。

### 回答 (2026-07-08) 質問への回答: 辻メッシュ検索の画素の「高さ」の扱い
現在の実装では、**画素の観測点の高さは0m(地表面)として扱っています**。つまり各画素の観測点は「その画素の緯度経度 + DEM標高タイルの標高 + 高さ0m」です。位置情報メニューで設定している観測点の高さ(例: 東京タワーの150m)は、辻メッシュ検索の画素には**適用していません**。

- **理由**: 画素は「その場所の地面に立ったら」という仮想の観測点なので、現在の観測点に固有の高さ(タワーや建物の高さ)を59万画素すべてに一律に足すのは不自然と判断しました。カメラの三脚程度の高さ(1〜2m)は、視高度への影響が数十km先の目的点に対して0.001°前後で、精度フィルタ最小の◎×8(±0.0156°)より一桁小さいため、省略しても結果はほぼ変わりません。
- **関連する動作**: ①金色マーカーをクリックして観測点に設定するときも「緯度経度+DEM標高・高さ0m」で設定します。②目的点側は、位置情報メニューの「目的点標高+目的点高」の合計をそのまま使っています(こちらは高さ込み)。③標高オプションの可視判定(OK/NG)も画素の高さ0mで判定しています。
- **もし変更をご希望なら**: 「メニューに『画素の高さ(m)』の入力欄を設けて全画素に一律加算する」(展望台の階の高さでの検索などに有効)か、「現在の観測点の高さを全画素に適用する」のどちらでも対応できます。ビルの屋上や展望台を想定した検索をされる場合は前者がおすすめです。ご希望があればお知らせください。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 96f5543
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
確認しました。

では、ご提案通り、画素の高さを取り入れます。
デッサン`04-tsujimeshsearch.md`に反映をしましたので、よろしくお願いいたします。

### 回答 (2026-07-08) 「画素の高さ(m)」の追加
デッサン04の更新どおり対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. 「画素の高さ(m):」テキストボックス → 追加しました**
- 辻メッシュ検索メニューの1段目(「検索期間(日):」の上)に追加しました。初期値:0、制約:0〜10000、ステップ:1、プレースホルダー:「0〜10000m」です。範囲外の入力は自動で丸めます(-5→0、20000→10000)。ヘルプの辻メッシュ検索の項にも制約を追記しました。

**2. 検索への反映**
- 全画素の観測点に一律に加算します。つまり各画素の観測点は「画素の緯度経度 + DEM標高 + 画素の高さ」になります(展望台の階の高さなどを想定)。
- **金色マーカーのクリック**で観測点に設定するときも「観測点標高=DEM標高・観測点高=画素の高さ」として設定します(例: DEM標高100m+画素の高さ150m→観測点の海抜250m)。
- **標高オプション(OK/NG)** の可視判定も画素の高さ込みで行います。
- **「URLを取得」**にも `tsujiMeshPixHeight` として含め、URLから復元されます(圧縮辞書はv7へ追加・既存の共有URLはそのまま開けます)。

**3. デッサン04の項番**
- 「画素の高さ」を独立した段目に分け、以降の項番を振り直しました(項番4=画素の高さ、5=検索期間、…、73まで。項番2=トグルボタンの欠番と、検索結果表示領域の独立採番0〜12はそのまま)。

**検証**: 画素の高さ150mで検索した最良画素を、同じ観測点(DEM標高+150m)の辻検索で個別に再計算して照合し、時刻差0秒・中心角差0.0000237°で一致しました。clamp動作・金色マーカーの観測点高・URL往復(v7辞書・旧辞書v2/v6の凍結維持)・`?tsujiMeshPixHeight=777` からの復元、をすべて確認しています。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show baac365
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
確認しました。

微修正をします。
ラベル名称をわかりやすく、「画素の高さ」から「観測点画素の高さ」に変更します。
デッサン`04-tsujimeshsearch.md`に反映をしましたので、よろしくお願いいたします。

それから、質問です。
ヘルプに、「結果リストの1行: 「天体×日」のイベント。行の辻時刻・方位角などはその日の最良(最小距離)画素の値です。」とありますが、各画素(位置)によって、最高精度が出る辻時刻は異なります。辻ラインに沿って、最高精度の出る辻時刻は、移動していきます。ですので、結果リストで指定した辻時刻を基準として、辻時刻を秒単位でスライダーで変更した後、再計算ボタンを押下することで、目的の辻時刻の金色のマーカー集合を見つけることができるかと思うのですが、どうなのでしょうか。

ちなみに、マーカーレイヤーの位置を微修正しました。
デッサン`04-tsujimeshsearch.md`に反映をしましたので、よろしくお願いいたします。

### 回答 (2026-07-08) 微修正2件と、辻時刻ごとの金色マーカー集合についての質問への回答
微修正2件は対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. ラベル名称変更 → 対応しました**
- 「画素の高さ(m):」→**「観測点画素の高さ(m):」**に変更しました(メニューのラベルとヘルプの記載。ラベル幅175pxに収まることも確認済みです)。

**2. 「:マーカーレイヤー」チェックボックスの位置 → 移動しました**
- デッサン04の更新どおり、コントロール領域の**6段目(◀/行番号スライダー/▶の上)**に移動しました。行順は「No./日時/辻時刻 → マーカー数 → マーカーレイヤー → スライダー」です。オンオフによる表示切替が引き続き機能することも確認しました。

**3. ご質問への回答: 指定した辻時刻の金色マーカー集合は見つけられるか → はい、可能です**

ご認識のとおりです。結果リストの行に表示している辻時刻は「その日の最良(最小距離)画素」の時刻で、実際には画素ごとに最高精度の出る辻時刻は異なり、時間が進むにつれて最高精度の場所(辻ライン)は地図上を移動していきます。ご提案の「行の辻時刻を基準に、秒単位のスライダーで時刻を選んで、その時刻の金色マーカー集合を表示する」は実現できます。実装方式は2案あります。

- **案A: 保存済みデータの絞り込み(再計算ボタン不要)**
  検索時に、各ヒット画素の「最良辻時刻」を秒単位で既に保存しています。スライダーで秒を動かすと「最良時刻がその秒(±指定幅)の画素」だけを金色表示する方式です。計算不要なのでスライダーに追従して即時に切り替わります。制約は、保存上限(1日20,000画素)を超えた「省略中」の行では保存分しか対象にできない点です。
- **案B: 指定した秒での再計算(ご提案の「再計算ボタン」方式)**
  スライダーで選んだ1秒の天体位置を計算し、全画素(約59万)を方位索引で再評価して「その瞬間に精度(◎の角距離)に入っている画素」を金色表示する方式です。検索本体の1時刻分と同じ処理なので計算は一瞬で、保存上限の影響も受けません。再計算ボタンでもよいですし、スライダーに追従するライブ更新も可能です。

おすすめは**案B**です。「その時刻に辻が成立している画素の集合」がそのまま出るので、撮影地選び(この時刻ならどこから撮ればよいか)に直結します。案Aの「最良時刻の等時線」表示も面白いのですが、意味としてはご質問の意図(目的の辻時刻のマーカー集合)は案Bが正確です。取り入れる場合は、コントロール領域への配置(スライダーの範囲=行の全画素の辻時刻の最小〜最大、精度の扱いなど)をデッサン04へ反映いただければ、それに沿って実装します。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show baac365
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
助かります。

では、B案でいきます。
デッサン04に起こしましたので、ご査収ください。
実装をよろしくお願い申し上げます。

### 回答 (2026-07-08) 辻時刻コントロール(案B: 指定した辻時刻での再計算)を実装
デッサン04の8〜12段目のとおり実装しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. 追加したコントロール(行番号スライダーの下)**
- **「辻時刻:」ラベル**: 再計算の基準となる辻時刻をhh:mm:ssで表示。タイムピッカーとスライダーに連動します。
- **辻時刻タイムピッカー**: 時分を選択。スライダーとは連動しません(スライダーを動かしてもピッカーは変わりません)。
- **辻時刻スライダー**: ±5分の範囲・1秒刻みで秒を指定。**スライダーに追従してライブ再計算**します(再計算ボタンは不要でした。計算が一瞬のため、動かすたびに即座に金色マーカーが更新されます)。
- **「辻時刻の幅(s)±:」**: 初期値1、制約1〜30・ステップ1。指定時刻の前後±この秒数の各秒でも判定し、いずれかの秒で成立した画素を表示します。
- **「精度フィルタ:」**: ◎(±0.125°)〜◎×128(±0.0009765625°)の8択、初期値◎。
- ピッカー/スライダーは行選択のたびにその行の辻時刻へリセットされます。未入力のテキストボックスは元の値を復元します(共通機能)。

**2. 動作(案B)**
- 指定した辻時刻(±幅s)の各秒について天体位置を計算し、**全画素(約59万)を検索本体と同一の判定**(方位角ビン索引+地平座標系の回転補正+検索中心の点/線)で再評価して、精度フィルタ以内の画素を金色マーカーで表示します。検索条件(辻オフセット・検索中心・大気差)は検索実行時の値に固定されます。
- ご確認いただいた2点を反映しています: **行選択と同時にその行の辻時刻で再計算した集合を金色表示**(金色マーカーは常に「指定時刻の集合」)、**「マーカー数:」は表示中の金色マーカー集合の画素数**を表示(5,000超は「(表示5,000まで)」を付記)。
- 保存済みデータの絞り込みではなく毎回再計算するため、「20,000以上(省略中)」の行でも省略の影響を受けません。

**3. デッサン04の修正(2点・報告)**
- 新規8〜12段目の項番が既存の9(◀)〜12(マーカーレイヤー)と重複していたため、**13〜17に振り直しました**(コントロール領域の独立採番0〜17)。
- 精度フィルタの「◎×2(±0.0626°)」は0.0625°の誤記と判断して修正しました。

**検証**: ①行選択直後にピッカー=行の時分・スライダー=行の秒・ラベル=行の辻時刻で初期化され、金色3,877画素とマーカー数表示が一致 ②再計算集合に行の最良画素が含まれ、その精度角距離が結果リストの値と完全一致(差0) ③金色画素を観測点として天体位置を直接計算した検証で差0.0000217° ④スライダー+60秒でピッカー不変・ラベル/マーカー数更新 ⑤幅1→30で集合3,877→5,101(上位集合)、◎×8で610(全て±0.015625°以内) ⑥幅の丸め(50→30、0→1)と未入力復元 ⑦行選び直しでリセット ⑧マーカーレイヤーのチェックで金色も非表示(非表示中の再計算でも維持)、をすべて確認しました。

なお、太陽の場合、辻ラインは約1分で領域(約9km四方)を横切るため、スライダーを大きく動かすとマーカー数:0になる時刻もあります(その時刻には領域内に辻が成立する画素がない、という意味です)。幅(s)±を広げると再び見つかります。

ヘルプの金色マーカーの説明も新しい動作(指定した辻時刻の再計算集合)に更新しました。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show f32a359
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
助かります。

少し修正をしました。
辻時刻の幅の初期値と、辻時刻スライダーの追加コントロールです。
実装をお願いいたします。

それから質問です。
最初に辻メッシュ検索した時の結果リストに表示されている辻時刻と精度は、どの位置での辻時刻と精度になるのでしょうか。
なんとなく、現在の設定してある観測点から離れているような気がします。
緯度経度でソートした結果なのでしょうか。
現在の設定してある観測点と勘違いしてしまうと思いました。
### 回答 (2026-07-08) 辻時刻スライダーの◀▶ボタン・幅の初期値0と、結果リストの辻時刻の位置についての質問への回答
修正2件は対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. 辻時刻スライダーの「◀」「▶」ボタン → 追加しました**
- デッサン04の更新どおり、辻時刻スライダーの両脇に◀/▶ボタンを追加しました。1回押すごとに辻時刻が1秒前/後へ動き、追従して再計算されます(スライダーの±5分の端でそれ以上は動きません)。

**2. 「辻時刻の幅(s)±」の初期値0・制約0〜30 → 変更しました**
- 初期値を1→0、制約を1〜30→0〜30に変更しました。幅0は「指定した1秒だけで判定」の意味です。行選択直後(幅0)でも、その行の最良画素は必ず金色集合に含まれ、精度角距離は結果リストの値と完全一致することを確認済みです。ヘルプの記載も更新しました。

**3. ご質問への回答: 結果リストの辻時刻と精度は、どの位置での値か**

**その日の全ヒット画素(約59万画素の候補)の中で、精度角距離が最小になった1画素=「最良画素」の位置での値です。** 現在設定してある観測点での値ではありません。緯度経度でソートしているわけでもありません(「その日の最小距離」で選んでいます)。

- 辻メッシュ検索は「全画素をそれぞれ観測点とみなした辻検索」なので、結果リストの1行(天体×日)には画素の数だけ辻時刻・精度の候補があります。行に表示しているのは、その中で最も精度が良かった画素の辻時刻・精度角距離・方位角・視高度です。
- 最良画素は9枚のDEM標高タイル領域(約12km四方)のどこにでもなり得るため、**現在の観測点から数kmずれることは正常な動作**です。ご指摘のとおり、現在の観測点の値と勘違いしやすい表示だと思います。
- なお、行選択直後の金色マーカー(幅0)は「最良画素の辻時刻の1秒に辻が成立している画素の集合」なので、この集合のどこかに最良画素が含まれています。

誤解を避けるための改善案です。ご希望があればデッサン04に反映いただければ実装します。
- **案1**: 結果リストに「最良画素の緯度・経度」列(または「観測点からの距離(m)」列)を追加する。
- **案2**: 地図上で最良画素のマーカーを強調表示する(例: 赤縁の大きめ金色マーカー。行選択で常に表示)。
- **案3**: コントロール領域に最良画素の位置情報(緯度経度・観測点からの距離)を表示する行を追加する。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 87b695a
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
助かります。

疑問なのですが、最初に最良画素を決定するためには、辻時刻を仮定しないと、複数最良画素が存在することになりますよね。
この仮の辻時刻を選定するときに、現在の観測点での最高精度出る時の辻時刻を基準にするというのは、何か視点がずれているのでしょうか。

### 回答 (2026-07-08) 質問への回答: 「仮の辻時刻」を現在の観測点の辻時刻に置く案について
視点はずれていません。むしろ本質を突いたご指摘で、とても自然な案だと思います。順を追ってご説明します。

**1. 現在の実装は「仮の辻時刻」を仮定していません**
- 各画素がそれぞれ自分の最良辻時刻(その画素で距離が最小になる秒)を持っており、行の最良画素は「画素ごとの最小距離を、さらに全画素で最小化したもの」です。つまり(画素, 時刻)の組を同時に最小化しており、定義としては時刻を仮定しなくても一意に決まります。

**2. ただし「複数最良画素が存在する」というご理解は、実質的にその通りです**
- 辻ラインが領域を掃いていく間に、ライン沿いの何千もの画素が「ほぼ0°」の距離を達成します(実質的な同率1位が大量に発生する=縮退)。どの1画素が「最良」になるかは、数値上のごく僅かな差で決まるため、**位置としてはほぼ偶然**です。
- これが前回のご質問の「観測点から離れた場所になる」の正体です。行の辻時刻も「偶然選ばれたどこかの場所の時刻」になっており、基準としての意味が薄いというご指摘は的を射ています。

**3. ご提案(現在の観測点で最高精度が出る辻時刻を基準にする)の利点**
- 行の辻時刻が「自分が立つ場所での辻時刻」になり、直感と一致します(前回の「勘違い」も起きません)。
- 行選択時の金色マーカー(その時刻に成立している画素の集合)が、観測点で成立する日なら観測点付近を通ります。
- 縮退の恣意性が消えます(観測点という明確な基準点で時刻が決まる)。
- 計算コストも軽く、既存の辻検索1回分を並行して走らせるだけで済みます(観測点がDEM画素に載らない場合でも、位置情報メニューの観測点で直接計算できます)。

**4. 採用する場合に1点だけ決めが必要です**
観測点自身では精度が出ない日(領域内の他の画素でだけ◎以内になる日)の扱いです。
- **(a) 観測点の値をそのまま表示**: 辻時刻=観測点での最小距離の時刻、精度角距離=その値(◎より大きい値になる日もある)。「自分の場所ではこの日どこまで寄るか」が常に分かる。
- **(b) その日は従来の最良画素の値にフォールバック**: ◎以内の値だけが並ぶが、日によって基準が変わる。
- いずれでも、行の存在条件(領域内のどこかで◎以内なら行になる)は変えません。また、精度記号・方位角・視高度の列を観測点基準の値にするか、従来の最良画素の値を併記するか(列追加)も選べます。

ご希望の形((a)/(b)の選択、列の意味・併記の有無)をデッサン04に反映いただければ、それに沿って実装いたします。個人的には**(a)+従来の最良画素の値も列で併記**が、情報が欠けず分かりやすいと思います。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 87b695a
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
助かります。

では、(a)案でいきます。
そして、現在の観測点で最高精度が出る辻時刻を基準にするとして、その辻時刻での、最良精度の画素に、現在の観測点マーカーと同じ大きさの金色のマーカーを立てるというのはいかがでしょうか。
最良画素位置が大きめのマーカーで指し示されるので、わかりやすいかと思います。
この内容をデッサンにもヘルプにも、記載をお願いいたします。

### 回答 (2026-07-08) (a)案の実装: 行の辻時刻を観測点基準に変更＋最良画素に観測点サイズの金色ピン
(a)案と、最良精度の画素への金色マーカーのご提案を実装しました(ヘッドレスChromiumで検証・JSエラー0件)。デッサン04とヘルプにも記載済みです。

**1. 結果リストの行の値を観測点基準に変更しました((a)案)**
- 行の**辻時刻・精度記号・精度角距離・方位角・視高度**は、「**現在の観測点でその日に最高精度が出る辻時刻と、その時の値**」になりました。メッシュ検索と並行して、現在の観測点での辻検索を全期間ぶん自動実行して求めています(検索時間への影響はほぼありません)。
- 観測点で精度が出ない日は、◎より大きい精度角距離をそのまま表示します。精度記号は辻検索と同じ**◎(±0.125°)/○(±0.25°)/△(±1°)/-** で表します。
- 天体方位角差・視高度差も観測点の基準方位角・基準視高度からの差になりました。月齢・時間帯・行のソートも観測点基準の辻時刻で行います。
- 行の存在条件(領域内のいずれかの画素で精度フィルタ◎以内になる日を行にする)は従来どおりです。
- 行選択時の辻時刻コントロール(ピッカー/スライダー)も、観測点基準の辻時刻で初期化されます。

**2. 最良精度の画素に、観測点マーカーと同じ大きさの金色ピンを立てました**
- 指定した辻時刻において**最良精度になる画素**(精度フィルタに関係なく、その時刻の全画素の最小距離)に、観測点マーカーと同型・同サイズ(24px)の**金色のピン**を表示します。スライダーを動かすとピンも追従して移動します。
- ピンにカーソルを合わせると「この辻時刻の最良画素・精度角距離」を表示し、**クリックでその画素を観測点に設定**できます(観測点高=観測点画素の高さ)。
- 観測点基準の辻時刻では領域内に◎以内の画素がない日もありますが(マーカー数:0)、その場合もピンは「その時刻に一番近い画素」を指し続けるので、どちらの方向にどれだけ離れているかが分かります。

**3. デッサン04・ヘルプへの記載**
- デッサン04の「処理内容の概要」に、行の値が観測点基準であること・金色マーカーの再計算表示・最良画素のピンを記載しました。
- ヘルプの「結果リストの1行」を観測点基準の説明に書き換え、「最良画素のピン」の項を追加しました。

**検証**: ①行の辻時刻・精度角距離・方位角・視高度が、観測点での辻検索(許容範囲180°)の当日最良と**完全一致**(全て差0) ②精度記号が閾値どおり(検証データでは2.22°「-」→0.71°「△」と日ごとに接近) ③方位角差・視高度差が観測点基準 ④行選択時にピンが1本立ち、位置=その時刻の全画素の最小距離の画素と一致 ⑤ピンのクリックで観測点設定 ⑥スライダー移動でピンが追従(常に1本) ⑦マーカーレイヤーのチェックでピンも非表示、をすべて確認しました。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show b80cfeb
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
助かります。

一つ試してみたいことがあります。
白マーカーの表示上限を256×256×9にして全て表示して、金マーカーも同様に、256×256×9にして、計算に時間がかかる場合は、進捗バーで表示することはできないでしょうか。
なんとなくですが、そんなに計算待ちが発生しないような気がします。
いかがでしょうか。
そう思ったきっかけは、金の観測点マーカーを辻時刻スライダーで変化させたときに、その日の精度内の動かないはずの金マーカーの面積が変化をしていったのです。
色々考えた末、きっと表示条件で上位5000件のマーカーが再選択されているのではないかと思いました。
それなら、全ての金マーカーと白マーカーを表示してみてはどうかと思ったのです。
いかがでしょうか。
デッサンとヘルプにも、内容の反映をお願いいたします。

### 回答 (2026-07-08) 白/金マーカーの表示上限を256×256×9(全画素)に撤廃
できました。ご提案どおり両マーカーとも上限を256×256×9=589,824(領域の全画素)にして、全て表示するようにしました(ヘッドレスChromiumで検証・JSエラー0件)。デッサン04とヘルプにも反映済みです。

**1. 実現方式: 画像オーバーレイに切り替えました**
- Leafletのマーカーを58万個並べる方式はメモリと描画で破綻するため、9タイル領域(768×768)の画像を生成して地図に重ねる方式にしました。**1画像画素=DEM標高タイル1画素(約10m四方)の実寸表示**になり、ズームインすると画素の実面積、ズームアウトすると面として見えます。
- **白マーカー**: 全結果行のヒット画素の和集合を全て表示(従来の計20,000上限・行ごと間引きを廃止)。
- **金マーカー**: 指定した辻時刻の再計算集合を全て表示(従来の5,000上限を廃止)。
- ワーカーの**1日あたり20,000画素の保存上限も撤廃**したので、「20,000以上(省略中)」の表記もなくなりました。
- オーバーレイ化後も、**金の画素にカーソルを合わせると精度角距離を表示**し、**クリックでその画素を観測点に設定**(DEM標高+観測点画素の高さ)できます。最良画素のピンも従来どおりです。

**2. 進捗バーは不要でした**
- ご推察どおり計算待ちはほぼ発生しません。再計算の判定を弦距離(角距離と数学的に同値)に再定式化して高速化したため、幅±30秒・55,684画素の集合でも再計算+描画で約0.2〜0.3秒、通常の幅0なら数十ミリ秒です(既存の検索中の進捗バーはそのままです)。

**3. 「金マーカーの面積が変化した」原因: ご推測のとおりでした**
- 従来は集合の先頭5,000画素だけを描いていたため、スライダーで集合が増減するたびに「どの5,000が描かれるか」が入れ替わり、面積が不自然に変わって見えていました。全数表示にした今は、見えているものがそのまま真の集合です。
- なお、集合そのものもスライダーに追従して毎秒再計算される仕様なので、時刻とともに金の帯が地図上を移動していくのは正しい動作です(辻ラインの移動)。

**検証**: ①14日検索で全行が省略なし(最大行47,557画素・のべ382,732画素) ②白オーバーレイの画像画素数=全行和集合382,732と完全一致 ③金55,684画素の行も全数描画・マーカー数表示一致 ④スライダー移動で集合が更新(画像画素数も一致・約0.23秒) ⑤金の画素クリックで観測点=DEM標高+画素の高さ・ホバーで精度角距離表示 ⑥マーカーレイヤーOFFで白/金/ピンとも非表示(非表示中はクリックも無効)、をすべて確認しました。

**メモリの注意**: 保存上限の撤廃により、ヒット画素1つあたり16バイトを保持します。検索期間が非常に長くヒット日が多い場合(例: 100年検索で毎日数万画素ヒット)はメモリ使用量が増えますので、動作が重い場合は検索期間か許容範囲を絞ってください。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show a7e3bad
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
助かります。

一つ疑問があります。
「再計算の判定を弦距離(角距離と数学的に同値)に再定式化して高速化したため」とありますが、この精度角距離の算出方法は、変更してはいけないような気がします。
なぜなら、精度を測るベースがずれてしまうから。

たまたまなのですが、太陽で、東京タワーと富士山で、辻メッシュ検索をしました。
太陽	◎	0.07974°	2026年11月01日	(日)	16:32:39
検索結果の辻時刻は、16:32:39なのですが、辻時刻スライダーを変化させて、金の雫マーカーが現在の観測点マーカーに物理的に最も近づいたのは、16:32:27で、ずれがありました。
計算時間がかかっても良いので、精度角距離の計算方法は、ベースを揃えた方か良いと思います。
いかがでしょうか。

### 回答 (2026-07-08) 質問への回答: 弦距離とベースのズレ・ピン最接近時刻が辻時刻とずれる件
「精度を測るベースを揃えるべき」というご指摘の原則はまったくその通りです。その上で調査した結果、**弦距離への変更はベースをずらしておらず、12秒のズレの原因は別にありました**。順にご説明します。

**1. 弦距離は「近似」ではなく「同値の式変形」です(ベースは揃ったままです)**
- 2点の角距離θと弦の長さは 弦=2×sin(θ/2) の関係で厳密に1対1対応(単調)しており、弦からasinでθへ正確に戻せます。判定・表示している精度角距離の定義は変わっていません。
- 実際に数値でも照合しました。行の辻時刻の金色集合の全画素(22,213画素と43,010画素の2ケース)について、旧式(asin/atan2+球面角距離)と新式(弦距離)で精度角距離を二重計算して比較したところ、**最大差0.0000014°**でした。表示桁(0.00001°)の1/10以下、最も厳しい◎×128(±0.00098°)の1/700で、実用上の差はありません(この微小差は新旧どちらの式にも同程度に乗る丸め誤差のオーダーです)。

**2. 16:32:39 と 16:32:27 のズレの本当の原因: 「高さ」の基準の違いです**
- 行の辻時刻(16:32:39)は、**現在の観測点=東京タワー(観測点高込み、例:150m)**での最適時刻です。
- 一方、金マーカーとピンの画素は「**DEM標高+観測点画素の高さ(既定0m)**」、つまり地面の高さで計算しています。
- 150m低い地上の画素から見ると富士山の見かけ高度は約0.09°高くなるため(≒高さの差÷距離)、沈んでいく太陽は**地上の画素の方に十数〜数十秒早く**整列します。ピンが観測点マーカーへ物理的に最接近した16:32:27が辻時刻より12秒早かったのは、この効果です(ズレの向きも一致します)。
- 再現実験でも確認しました(合成地形・観測点高150m):
  - **画素の高さ0m**: ピンの最接近は行の辻時刻の**26秒前**(その時116m)。行の辻時刻の瞬間には辻ラインは観測点から2,171m先。
  - **画素の高さを150mに揃える**: ピンの最接近は行の辻時刻**+2秒**(76m)となり、ズレがほぼ消えました(残る±2秒は斜め横断の幾何と1秒刻みによるものです)。

**3. ベースを揃える方法(いまの機能でできます)**
- 「**観測点画素の高さ(m)**」に観測点高と同じ値(東京タワーなら150)を入れて検索してください。全画素が観測点と同じ高さ基準で評価され、行の辻時刻・金マーカー・ピンの時刻が揃います。
- ご希望であれば、「観測点画素の高さ」の**初期値を観測点高に自動で連動**させる(または「観測点高と同じ」チェックボックスを設ける)ことも可能です。採用される場合はデッサン04に反映いただければ実装します。

計算方法自体は同値なので戻す必要はないと考えていますが、もし旧式に戻すご希望があればすぐ対応します(その場合は幅±30秒の再計算が約1秒に戻ります)。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 144f7ce
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、素晴らしいです！
ご明察です！
私の観測点画素の高さを150mに設定し忘れが原因でした😓
すみません🙇

観測点画素の高さは、紛らわしいですし、観測点の高さがそもそもあるので、別途指定し直すこともないので、辻メッシュ検索をするときに、観測点の高さも含めた形で計算をしていただけないでしょうか。つまり、普通の辻検索と同じように、観測点標高と観測点高を含めて観測点の高さとして計算をするです。

デッサン04は、項目を削除しただけなので、項番は、ずれたままです。
修正もお願いいたします。
ありがとうございます！

### 回答 (2026-07-08) 「観測点画素の高さ」を廃止し、観測点高を自動適用＋デッサン04の項番修正
対応しました(ヘッドレスChromiumで検証・JSエラー0件)。原因の切り分けができて何よりです😊

**1. 「観測点画素の高さ(m)」を廃止し、観測点高を自動適用しました**
- メニューの入力欄・内部の状態・URLパラメータの出力をすべて削除しました。
- 辻メッシュ検索は、普通の辻検索と同じく「**画素のDEM標高 + 観測点高(位置情報メニューの値)**」を観測点の高さとして全画素を計算します。設定し忘れが起きない形です。
- 金色マーカー/ピンをクリックして観測点に設定するときも、**観測点高はそのまま維持**されます(観測点標高=その画素のDEM標高に置き換わるだけ)。
- 標高オプションの可視判定も同じ高さ基準で行います。
- 互換性: URL圧縮辞書(v7)は凍結ルールに従いそのまま残しています。旧URLに `tsujiMeshPixHeight` が含まれていても、エラーなく無視されます。ヘルプも「画素の高さ基準」の説明に書き換えました。

**2. デッサン04の段目・項番を修正しました**
- 削除された1段目と項番4の欠番を詰めました: 段目は0〜33の連番、項番は「1、2(トグルボタン)、3(▼)、4(検索期間)…72(URL取得)」の連番です(2の欠番はメニュー節の予約どおり)。
- 本文中の項番参照(時間フィルタ23、標高オプション69など)も、メニュー節・仕様節ともに一致するよう振り直しました。検索結果表示領域の独立採番(0〜17)は不変更です。

**検証**: ①入力欄・状態・「観測点画素の高さ」の文言が残っていないこと ②観測点高150mで検索すると自動で全画素に150mが適用され(前回の再現実験と同じ掃引で)**ピンの地上最接近時刻が行の辻時刻+2秒(76m)** — 何も設定しなくても高さの基準が揃うこと ③金色画素の精度角距離を「DEM標高+150m」の直接計算と照合して差0.0000009° ④金色クリックで観測点標高=DEM標高・観測点高=150m維持 ⑤取得URLにパラメータが含まれず、旧URLも無害 ⑥デッサンの項番の連番・参照一致、をすべて確認しました。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show b5c9553
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。

少し試してみたのですが、不具合かどうか、わかりません。
調査をお願いいたします。
何故だかわかりません。

太陽で、東京タワーと富士山で、辻メッシュ検索をしました。
太陽	◎	0.07974°	2026年11月01日	(日)	16:32:39
検索結果の辻時刻は、16:32:39なのですが、辻時刻スライダーを変化させて、金の雫マーカーが現在の観測点マーカーに物理的に最も近づいたのは、16:32:54で、ずれがありました。
これは、何故なのでしょう。
本来なら、検索結果の方の辻時刻に16:32:54と出るのではないでしょうか。
これは、位置が異なるから、なのでしょうか。
よろしくお願いいたします。

### 回答 (2026-07-08) 調査結果: ピンの地上最接近(16:32:54)と行の辻時刻(16:32:39)のズレについて
調査しました。**不具合ではありません**。両者は「別のもの」を測っており、観測点が辻ラインの真上に乗らない日(精度角距離が0でない日)には、原理的に一致しません。ご質問の「位置が異なるから?」は本質を突いていて、正確には「観測点が辻ラインから角度で0.07974°ずれた位置にあるから」です。

**1. ふたつの時刻が測っているもの**
- **行の辻時刻(16:32:39)** = 観測点の方向ベクトルに、太陽が**角度的に**最も近づく瞬間です。このとき精度角距離が最小(0.07974°)になり、**あなたの場所での構図の精度が最高**になります。
- **ピンの地上最接近(16:32:54)** = 辻ライン(その瞬間に辻が完全成立する場所の帯)が、観測点の**地面上の位置**の最も近くを通る瞬間です。

**2. なぜ一致しないのか: 地上距離は「視高度のズレ」に桁違いに敏感だからです**
- 方位角0.1°のズレは、地図上では横に**約170m**の違いにしかなりません。
- ところが視高度0.1°のズレは、それを地形で補うには富士山方向に**数km**移動する必要があります(見かけ高度は距離にゆっくりしか依存しないため)。
- そのため「地上で最も近い」瞬間は、ほぼ「**太陽の視高度が観測点の基準視高度とちょうど一致する瞬間**」になります。角度の最小(方位と視高度のバランス点)とは別の瞬間です。沈む太陽では、角度最小の時点でまだ太陽が基準視高度より上にある場合、視高度が一致するのはその後 — 今回の**+15秒**です。

**3. 実測(合成地形・観測点高込みで再現)**
- 残差0.326°の行: 行時刻の残差=(方位+0.258°・視高度+0.199°)。ピンの地上最接近は**+64秒後**(713m)で、その瞬間の視高度残差はほぼ0(−0.006°)。ただし観測点自身の精度角距離は**0.416°に悪化**していました。ズレ秒数は「行時刻の視高度残差 ÷ 太陽の高度変化率」で説明でき、0.199°÷0.0031°/s=64秒と完全一致します。
- 残差0.002°の行(ほぼライン上): ズレは**+2秒**(76m)まで縮みます。
- ご報告の15秒も、0.07974°の残差のうち視高度成分(約0.04°)÷高度変化率に相当します。

**4. 「本来なら行の辻時刻に16:32:54と出るのでは?」→ 出さない方が正しいです**
- 16:32:54の時点では、観測点自身の精度角距離は0.07974°より**悪く**なっています(方位のズレが大きくなっているため)。写真の構図として最良なのはあくまで16:32:39です。
- 整理すると: **行の辻時刻=「自分の場所」の最良の「瞬間」**、**ピン=「その瞬間」の最良の「場所」**です。観測点が辻ラインの真上に乗る日(精度角距離≈0)には、この2つは一致します(実測+2秒)。ピンが近くを通るのに精度が上がらない日は、「その日はもう少し富士山寄り/離れた場所から撮ると完璧になる」というサインとして読めます。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 5f19cd1
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
分かりやすい説明ですね。
理解できました。

私が求めるのは、観測点近傍の最高精度地点なので、それを検索しやすいようにデッサンを修正しました。
1. 検索結果の選択中の行番号の情報、マーカー数、行番号スライダーは、削除しました。
2. 「精度フィルタ」(◎〜◎×8)を「精度フィルタオプション」(◎〜◎×8)と名前を変更し、辻検索と同じ「精度フィルタ」(◎○△-)を取り入れました。
3. 検索後オプションを追加しました。
「精度フィルタ」(◎○△-)を取り入れたので、検索結果リストの不要な日付をフィルタできます。

デッサンには書いていないのですが、
3. 金マーカー(辻マーカー)をマウスオーバーした時のポップアップの内容を精度角距離だけでなく、辻時刻も表示して欲しいです。必要であれば、辻時刻の秒を小数点以下2桁で表示しても良いです。例:辻時刻 16:56:39.28
4. 複数の天体を辻メッシュ検索した時に辻マーカーが分かりやすいように、辻マーカーは、天体色で表示してください。
5. 表示天体をオンオフした時に、天体色の辻マーカーが表示/非表示されるようにしたいです。

それから、不具合のようです。
辻メッシュ検索をすると、必ず、初期表示で、コントロール領域が画面1/3を占めます。
理想は、検索結果表示領域を最大化している・していないに関わらず、検索結果表示領域を結果リスト領域とコントロール領域で2分する、です。

あと、「**行の辻時刻=「自分の場所」の最良の「瞬間」**、**ピン=「その瞬間」の最良の「場所」**」の説明がわかりやすかったので、この辺りのことをヘルプに書き加えてあると嬉しいです。
またヘルプのどこかに、以下の精度フィルタでも、検索できることを書いていただけると嬉しいです。
- ◎(±0.125°)
- ◎×2(±0.0625°)
- ◎×4(±0.03125°)
- ◎×8(±0.015625°)
- ◎×16(±0.0078125°)
- ◎×32(±0.00390625°)
- ◎×64(±0.001953125°)
- ◎×128(±0.0009765625°)

よろしくお願いいたします。

### 回答 (2026-07-08) 大型修正: 精度フィルタ(◎○△-)・検索後オプション・天体色の辻マーカー・コントロール整理・レイアウト不具合修正
すべて対応しました(ヘッドレスChromiumで検証・JSエラー0件)。デッサン04の段目・項番も振り直し済みです。

**1. コントロール領域の整理(デッサン04どおり)**
- No./日時/辻時刻の行、マーカー数の行、行番号スライダー(◀/▶)の行を削除しました。行の選択は結果リストのクリックで行います。

**2. 「精度フィルタ」(◎○△-)の新設と「精度フィルタオプション」への改名**
- メニューに精度フィルタを新設しました: 親チェックと「:◎」は常時オン(読み取り専用)、「:○」「:△」「:-」は任意で追加できます(初期値オフ)。**行の精度記号(観測点基準)で結果リストを絞り込みます**(初期状態では◎の行だけが並びます)。
- 従来の「精度フィルタ」(◎〜◎×8)は「精度フィルタオプション」に改名しました(検索の当たり判定εの意味は不変)。
- 設定は保存され、「URLを取得」にも含まれます(圧縮辞書v8を追加・旧辞書は凍結のまま)。

**3. 「検索後オプション」を追加しました**
- **「:近傍の最高精度点(≠辻時刻)」(初期値オン)**: 行を選択すると、最高精度(◎×128以内、無い場合はその行の最小距離)の画素のうち**現在の観測点に地上距離が最も近い画素**に優辻マーカー(金の雫ピン)を立て、**辻時刻コントロールもその画素の辻時刻に移動**し、ピンが画面中央(下部パネルを除いた可視領域の中央)に来るよう表示します。検証では観測点から36mの画素が選ばれました。
- **「:辻時刻での最高精度点」**: 行の辻時刻でのその瞬間の最良画素にピンを立てます(従来動作)。

**4. デッサン外のご要望3件**
- **ツールチップに辻時刻**: 辻マーカー(旧金マーカー)や優辻マーカーにカーソルを合わせると、**その画素の辻時刻を0.01秒精度**(例: 17:03:13.42)と精度角距離を表示します。ホバーした画素だけ、その場で±120秒を1秒スキャン+放物線補間して精細化しています(検証: 検索データとの差0.18秒)。
- **辻マーカーは天体色**: 再計算集合のマーカーを選択行の**天体色**で描くようにしました(太陽の行は赤、月の行は月の色。複数天体の検索で見分けられます)。白マーカーと優辻マーカー(金)は従来色です。
- **表示天体と連動**: 表示天体メニューで選択行の天体をオフにすると、その天体色の辻マーカーとピンが非表示になり、オンで復帰します。

**5. レイアウト不具合を修正しました(原因判明)**
- 原因は、コントロール領域の本体を閉じるためのCSS(`#tsujimesh-ctrl-body.hidden`)が**定義されておらず、閉じた状態でも本体が常に表示されていた**ことでした。行が増えたことで本体がパネルからあふれ、結果リストが押しつぶされていました。
- 修正後: 閉じた状態はヘッダのみ(24px)、開いた状態は**最大化の有無に関わらず結果リスト領域とコントロール領域がちょうど半々**になります(検証: 通常パネル300pxでctrl149px、最大化600pxでctrl300px)。

**6. ヘルプの追記**
- 「**行の辻時刻=『自分の場所』の最良の『瞬間』、優辻マーカー=『その瞬間(近傍モードでは観測点近傍)』の最良の『場所』**」の説明を追加しました。
- 辻時刻コントロールの精度フィルタが**◎(±0.125°)〜◎×128(±0.0009765625°)の8段階**で検索できることを一覧で記載しました。新しい各機能の説明も反映済みです。

**7. デッサン04の整合**
- 新設の精度フィルタ(64〜68)と重複していた段目・項番を振り直しました(精度フィルタオプション=69〜73、標高オプション=74、URL取得=77。メニュー項番1〜77・段目0〜36の連番)。コントロール領域も削除行を詰めて項番0〜15・段目0〜12の連番にしました。

**検証**: 新旧UIの有無、レイアウト(閉24px・開50/50・最大化50/50)、既定◎のみ→△/-追加で1行→13行、近傍モードのピン=独立計算の「最高精度かつ観測点最近傍」画素と一致(36m)・時刻・画面中央、辻時刻モード切替、ツールチップの辻時刻(差0.18秒)、天体色(太陽=赤の実測)、表示天体オフでの非表示、URL往復(~8~辞書・v7以前凍結)、をすべて確認しました。

なお、「検索後オプション」の選択はセッション内のみ有効(保存なし・毎回「近傍」で開始)としています。保存をご希望ならお知らせください。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 3c04265
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
不具合は解消されました。

あと、微修正で、コントロール領域のテキストボックスとリストボックスの幅なのですが、辻メッシュ検索の縦に並んでいるテキストボックスの幅と同じにできないでしょうか。
そのほうが、見た目が揃っていて、感じかいいです。
よろしくお願いします。

また、デッサン04は、文言を少しだけ修正しました。
実装をお願いいたします。

それから、ふと思ったことがあります。
月と槍ヶ岳が辻るパール槍ヶ岳を調査しているのですが、
- 観測点
  - 大天井岳
  - 緯度経度:36.3649903604405, 137.7011117064
  - 標高:2920.5
  - 高さ:0
- 目的点
  - 槍ヶ岳
  - 緯度経度:36.34207558246137, 137.64764070510864
  - 標高:3178.6
  - 高さ:0
で、辻メッシュ検索をしたのですが、白マーカーで、地図上に尾根筋のようなものが描かれました。
辻時刻スライダーを変化させて、優辻マーカーを移動させたのですが、全ての尾根筋のような場所を巡ることができませんでした。
辻メッシュ検索の検索結果には、行が、8件ありましたが、全ての行で、辻時刻スライダーを変化させても、全ての尾根筋を巡ることができませんでした。
スライダーを動かすことで、全ての白マーカーを移動するような白い雫マーカーを作成することはできるのでしょうか。
全ての白マーカーの位置での、辻時刻と精度角距離が気になります。
そのような探索はできるのでしょうか。

よろしくお願いいたします。

### 回答 (2026-07-08) 微修正2件と、全ての白マーカーの辻時刻を探索する方法についての回答
微修正2件は対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. コントロール領域の入力幅を揃えました**
- 辻時刻タイムピッカー・「辻時刻の幅(s)±」・「精度フィルタ」リストボックスの幅を、辻メッシュ検索メニューの縦並びテキストボックスと同じ**155px**に統一しました(実測で全て一致することを確認済みです)。

**2. デッサン04の文言修正を反映しました**
- ラジオボタンのラベルを「:表示辻時刻での最高精度点」に変更しました(ヘルプの表記も追随)。

**3. ご質問への回答: 全ての白マーカーの辻時刻・精度角距離の探索はできます**

まず、スライダーで巡りきれなかった理由です。
- 白マーカーは**全8行(日)の和集合**です。尾根筋のように見える帯は、それぞれ**別の日の辻の帯**なので、1つの行の辻時刻スライダーではその日の帯しか掃けません。
- さらにスライダーの範囲は辻時刻±5分ですが、月が領域を横切るのにかかる時間はそれより長いことがあり、**その日の帯ですら端まで届かない**ことがあります。パール槍ヶ岳で巡りきれなかったのはこの2つの重なりです。

その上で、「全ての白マーカーの位置での辻時刻と精度角距離」は、**検索時に全て保存済み**です(各画素の「その日の最良辻時刻・最良精度角距離」)。新しい計算は不要で、見せ方だけの問題です。ご提案です。

- **提案1(おすすめ)**: 白マーカーにも、辻マーカーと同じように**カーソルを合わせると「日付・辻時刻・精度角距離」を表示**します(複数の日にヒットする画素は最良の日を表示)。さらに**白マーカーをクリックすると、その行(日)を選択して、辻時刻コントロールがその画素の辻時刻へ移動**し、優辻マーカーがそこに立ちます。地図上のどの筋でも「クリック一発でそこに立った時の辻へ飛ぶ」ことができるので、白い雫マーカーをスライダーで動かすのと同じ探索が、行とスライダーの往復なしで実現できます。
- **提案2(代案・併用可)**: 辻時刻スライダーの範囲を±5分固定から「**その行の全ヒット画素の辻時刻の範囲**」に自動で広げます。1本のスライダーでその日の帯全体を端から端まで掃けるようになります(日をまたぐ探索は提案1が必要です)。

ご希望の形(提案1 / 提案2 / 両方)をデッサン04に反映いただければ、それに沿って実装いたします。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 590c1e3
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
提案助かります。

では、提案1と2の併用案でお願いいたします。
マウスオーバー時のポップアップのデッサンは、こちらでお願いいたします。
- 優辻マーカー(最優良画素)(太字)
  - 辻日付: 2026年03月26日(木)
  - 辻時刻: 03:38:15.35
  - 精度角距離: 0.00016°
  - クリックで観測点に設定
- 辻マーカー(太字)
  - 辻日付: 2026年03月26日(木)
  - 辻時刻: 03:38:15.35
  - 精度角距離: 0.00016°
  - クリックで観測点に設定
- 白マーカー(太字)
  - 辻日付: 2026年03月26日(木)
  - 辻時刻: 03:38:15.35
  - 精度角距離: 0.00016°
  - クリックで観測点に設定
デッサンにもこの記載と提案1と2の内容をお願いいたします。

### 回答 (2026-07-08) 提案1+2の実装: 白マーカーのホバー/クリック探索・辻時刻スライダーの範囲自動拡大・ポップアップ書式統一
提案1と2の併用と、ご指定のポップアップ書式を実装しました(ヘッドレスChromiumで検証・JSエラー0件)。デッサン04にも記載済みです。

**1. ポップアップの書式(ご指定どおり3種)**
- **優辻マーカー(最優良画素)**(太字) / 辻日付 / 辻時刻(小数2桁) / 精度角距離 / クリックで観測点に設定
- **辻マーカー**(太字) / 辻日付 / 辻時刻 / 精度角距離 / クリックで観測点に設定
- **白マーカー**(太字) / 辻日付 / 辻時刻 / 精度角距離 / クリックで観測点に設定
- 辻時刻は、その画素の最良辻時刻をその場で0.01秒精度に精細化して表示します(白マーカーは、複数の日にヒットする画素の場合は最良の日の値)。

**2. 提案1: 白マーカーのホバーとクリック**
- 白マーカーにカーソルを合わせると、上記の書式で**その画素の辻日付・辻時刻・精度角距離**が分かります。どの尾根筋(帯)がどの日のものかが一目で確認できます。
- 白マーカーを**クリックすると、その画素を観測点に設定**し(DEM標高+観測点高)、あわせて**その行(日)が選択されて辻時刻コントロールがその画素の辻時刻へ移動**、優辻マーカーがその画素に立って画面中央に表示されます。地図上のどの帯でも、クリック一発で「そこに立った時の辻」へ飛べます。
- 辻マーカー(色付きの集合)のクリックは従来どおり観測点の設定のみです(行は変わりません)。

**3. 提案2: 辻時刻スライダーの範囲を自動拡大**
- スライダーの範囲を「辻時刻±5分」固定から「**その行の全ヒット画素の辻時刻の範囲(最小〜最大)**」の自動調整に変更しました。1本のスライダーでその日の帯全体を端から端まで掃けます。ステップは1秒のままです。

**検証**: ①スライダー範囲が行のヒット画素の辻時刻の[最小,最大]と一致 ②白マーカーのホバーで白マーカー太字・辻日付=その帯の日付・辻時刻の精細化が保存値と0.05秒一致・末尾「クリックで観測点に設定」 ③白マーカークリックで観測点=その画素(観測点高150m維持)・行切替・ピッカー/スライダー=その画素の辻時刻・優辻マーカー=その画素 ④辻マーカーのホバーと優辻マーカーのポップアップの書式 ⑤辻マーカークリックは観測点設定のみで行は不変 ⑥既存機能の回帰(レイアウト・近傍モード・天体色・URL等)、をすべて確認しました。

これでパール槍ヶ岳の尾根筋も、白マーカーをなぞる(ホバー)だけで各地点の辻日時と精度が分かり、気になる地点はクリックで即座にその日時へ飛べます。次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show f544a44
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
素敵です！
良い感じになってきました。

少し修正を加えます。
白マーカーをメッシュマーカーと名づけました。
1. 精度フィルタの初期値を全てオンにしました。
2. 検索エリアオプションを追加しました。
3. メッシュマーカー色オプションを追加しました。
その他、検索後の動作あたりをデッサンに反映しました。

あと、私の伝え方が悪かったようなのですが、コントロール領域の
1. 辻時刻の幅
2. 精度フィルタ
3. 検索後オプション
のラベル位置がずれています。
辻メッシュ検索メニューと同じように揃えていただけないでしょうか。

その他、細かい文言の修正もありますが、よろしくお願い申し上げます。

### 回答 (2026-07-08) メッシュマーカー改名・精度フィルタ全オン・検索エリア・メッシュマーカー色・検索後動作・ラベル位置揃え
すべて対応しました(ヘッドレスChromiumで検証・JSエラー0件)。

**1. 精度フィルタの初期値を全てオンにしました**
- ○/△/-の初期値をオンに変更しました(◎は従来どおり常時オン)。検索直後から◎○△-の全記号の行が並びます。

**2. 「検索エリア」を追加しました**
- コントロール領域に3×3(初期値)/4×4/5×5のラジオボタンを追加しました。変更すると自動で再検索され、**実行中に変更しても前の処理はキャンセル**されます(世代カウンタ)。検索の全機構(グリッド・オーバーレイ・ホバー/クリック判定・ワーカー保存上限)をタイル数に合わせて一般化しています(5×5=約164万画素まで)。

**3. 「メッシュマーカー色」を追加しました**
- 白(0%)〜黒(100%)のグレースケールをスライダーで指定できます(初期値0・ステップ1)。%表示ラベルと色味の分かる■サンプル付きで、スライダーに追従してマーカーが再描画されます。

**4. 検索後の動作(デッサン反映分)**
- 検索後は、**複数の表示天体を含めて最も精度角距離が優良な行を自動選択**し、日時もその行へ移動します(**辻ラインがオンの場合は辻ラインも移動**します)。
- 検索後の画面ズームの初期値を**最大ズーム値−3**にしました(行選択時も同じズームで優辻マーカーを画面中央に表示します)。

**5. コントロール領域のラベル位置を揃えました**
- 「辻時刻の幅(s)±」「精度フィルタ」のラベルを、メニューの縦並びラベルと同じ**幅175px・右寄せ**に統一しました(原因は、後方で定義された汎用ラベルの105px幅が優先されていたことでした)。
- 「検索後オプション」はコロンを外し、メニューの「検索中心オプション」と同じ**グループラベル(左寄せ)+選択肢行**の構成にしました。新設の「検索エリア」「メッシュマーカー色」も同じ構成です。

**6. 改名と文言**
- 白マーカー→**メッシュマーカー**にポップアップのタイトル・ヘルプを追随させました。デッサン04のポップアップ書式ブロックに残っていた「白マーカー」表記もメッシュマーカーに整合させ、4×4/5×5ラジオの説明文(「3×3=9」のままのコピペ)を16/25に、マーカー上限の「256×256×9」を「検索エリアのタイル数×256×256」に修正しました(ご報告)。

**検証**: ①ラベル幅175px=メニューと一致・3グループラベルの構成 ②既定で◎△-混在12行・自動選択=最小距離の行・日時移動・ズーム=最大−3 ③メッシュマーカー色50%でrgb(128,128,128)・%ラベル・■一致・0%で白に復帰 ④検索エリア4×4で自動再検索(グリッド1024・オーバーレイ画素数=和集合370,850・ホバー動作)・5×5(グリッド1280)も完走 ⑤ポップアップタイトル「メッシュマーカー」 ⑥既存機能の回帰(スライダー範囲・クリックジャンプ・優辻/辻ポップアップ)、をすべて確認しました。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 24b404f
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、ありがとうございます。
確認しました。

疑問があります。
辻メッシュ検索で、先のパール槍ヶ岳の調査を行っているのですが、辻メッシュ検索メニューの標高オプションで、OKだけにチェックを入れると、検索結果リスト36件になりますが、その前の段階の標高可視判定中の件数が192件で、最終的に残った、36件には、標高オプションをチェックしていない時に表示される以下の精度記号◎の検索結果が表示されません。
月	◎	0.01207°	2026年07月30日	(木)	04:45:46
また、メッシュマーカーの数も、かなり少なくなっているようです。
どのような基準で、標高オプションの可視判定をしているのでしょうか。
地形的に谷間にあって、ここは可視判定NGだと思われる場所が、メッシュマーカーが存在します。
192件は、標高オプションでNG判定も含まれる件数だと思うのですが、標高オプションでOKをチェックした時のユーザーの期待は、全画素で、標高オプションの可視判定をして、その画素の位置で、可視でOKであることを期待しています。
そのようになっていないようです。
また、もし、全画素の可視判定をするならば、処理順としては、全画素の辻メッシュ検索をする前に、フィルタをするのが良いかと思われます。
いかがでしょうか。
あと、標高オプションの判定中は、時間がかかるので、進捗を表示していただきたいです。

よろしくお願いいたします。

### 回答 (2026-07-08) 標高オプションの可視判定の基準について＋判定中の進捗表示を追加
進捗表示は実装しました。可視判定の仕組みは以下のとおりで、ご指摘の「全画素で判定してほしい」というご期待とは異なる動作をしています。ご提案の方式(検索前の全画素フィルタ)は実装可能ですので、設計案とあわせてご説明します。

**1. 現在の可視判定の基準(ご質問への回答)**
現在の標高オプションは「**行(天体×日)単位**」の判定です。
- 各行について、**その行の最良画素→目的点の1経路だけ**を標高グラフと同じアルゴリズムで可視判定し(先頭300行まで)、OK/NGチェックで**行を**絞り込みます。画素は判定していません。
- ご覧になった事象はすべてこれで説明できます:
  - **192件** = 判定対象の行数です(NG含む、300件上限内)。**36件** = 最良画素の経路がOKだった行です。
  - **「月◎ 0.01207°」の行が消えた** = その行の最良画素の位置がたまたま不可視(NG)だったためです。行の精度(観測点基準)とは無関係に、最良画素の場所次第で行ごと消えます。
  - **谷間にメッシュマーカーが残る** = マーカー(画素)自体は可視判定で絞っていないためです(行が絞られただけ)。
  - **マーカー激減** = 行が36件に減り、その和集合だけが表示されるためです。

**2. ご提案(全画素の可視判定を検索前に)について → その通りで、実装可能です**
- ご指摘のとおり、処理順は「**検索前の事前フィルタ**」が最適です。目的点は固定なので、画素ごとに経路を判定する必要はなく、**目的点からの放射状ビューシェッド(可視領域計算)**で全画素を一括判定できます:
  - 目的点から画素解像度相当の方位レイ(約1,200〜2,500本)を放射し、各レイに沿ってDEM標高を歩きながら「遮蔽仰角の累積最大」を前計算 → 各画素は(目的点からの方位・距離)から**O(1)で可視/不可視**が引けます。
  - 計算量はレイ×歩数で数百万サンプル=**数秒**(もちろん進捗表示付き)。判定モデルは標高グラフと同一(DEM標高+観測点高 → 目的点標高+目的点高の直線見通し)です。
  - 検索前に不可視画素を候補から除くので、**結果リストの行・メッシュマーカー・辻マーカー・優辻マーカーのすべてが「実際に見える場所」だけ**になります。谷間のマーカーは消えます。
- 1点だけ設計の確認をお願いします。「:OK」「:NG」チェックの意味です。
  - **案A(おすすめ)**: OK/NGチェックを廃止し、「**標高オプションON=可視画素のみを検索**」のシンプルな仕様にする(ご期待の動作そのもの)。
  - **案B**: 「:OK」=可視画素のみ(既定)/「:NG」=不可視画素のみ(遮蔽側の確認用)/両方オン=全画素。
- 採用される案をデッサン04に反映いただければ、それに沿って実装します。

**3. 判定中の進捗表示 → 追加しました**
- 現在の行単位判定のあいだ、ステータスに「(標高オプション判定中… 12/192件)」の逐次カウントと、進捗バーの更新を表示するようにしました(全画素判定を実装する際も同様に進捗を出します)。

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 5647d68
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-08)
Claudeさん、提案ありがとうございます。
助かります。

私の好みとしては、B案ですね。
可視、不可視、の両方で、確認が取れるので、裏が取れます。
つまり、メニューの標高オプションのデザインのデッサンは、そのままですね。
処理内容のデッサンは、デッサン04に反映しました。

それと、コントロール領域のコントロールラベルが、宙の窓と同じように、真ん中寄りにできないでしょうか。
あと、先のプランで気になったことは、「画素解像度相当の方位レイ(約1200本)」は、検索エリアが広くなっても、変更されるのか、という疑問です。
それから、検索エリアオプションの選択肢を増やしました。

いつも、ありがとうございます。
実装をよろしくお願いいたします。

### 回答 (2026-07-08) 案B実装完了: 検索前の全画素ビューシェッド可視判定＋6×6＋コントロールラベル中央寄せ
案Bで実装しました。あわせてコントロールヘッダの中央寄せ・検索エリア6×6・レイ本数のご質問への回答です。

**1. 標高オプション(案B): 検索前の全画素可視判定**
- デッサン04の処理内容どおり、標高オプションONの場合は**検索前に**、目的点からの放射状ビューシェッドで**検索エリアの全画素の可視(OK)/不可視(NG)を判定**し、チェックで選んだ対象の画素だけで辻メッシュ検索を行います:
  - **OKのみ** = 可視画素だけを検索 / **NGのみ** = 不可視画素だけを検索(裏取り用) / **両方オン、またはどちらもオフ** = 全画素
- 判定モデルは標高グラフと同一です(直線見通し・地球曲率/屈折なし・基本オプションの「除外範囲」=目的点近傍の遮蔽無視も反映)。DEM標高タイルはキャッシュして使い回します。
- 旧方式の「行の最良画素だけ判定(先頭300行まで)」は**廃止**しました。谷間の不可視画素にメッシュマーカーが残る事象・行ごと消える事象は解消されます。
- 結果リストの**標高グラフ列**は、デッサンどおり「その行の表示されている精度角距離(観測点基準)を出した画素の地点=**観測点の位置**の可視判定」を表示します。そのため**全行同じ値**になります(観測点を移して再検索すると変わります)。
- 進捗表示: 実DEMでは「(標高オプション 標高タイル取得中… i/N)」→「(標高オプション可視判定中… i/Nレイ)」の順にステータスと進捗バーが進みます。タイルは初回のみ取得で、2回目以降はキャッシュで高速です。

**2. ご質問「方位レイの本数は検索エリアが広くなると変わるのか」→ 変わります(自動追従)**
- レイ本数 = **2 × 方位スパン × 最大距離 ÷ 画素サイズ(約7.8m)** で毎回自動計算します(下限720本・上限9,000本)。
  - **方位スパン** = 目的点から見た検索エリア4隅の方位の広がり(目的点が検索エリア内にある場合は360°全周)
  - **最大距離** = 目的点から検索エリア最遠隅までの距離
- つまり**検索エリアを広げても、目的点が遠くても、検索エリアの最遠部で約半画素間隔の角度分解能**になるよう本数が増えます。検証実測(観測点=東京タワー付近、目的点=富士山、距離約101km): 3×3=スパン4.5°で**2,056本**、5×5=スパン7.5°で**3,512本**と、エリアに応じて自動で増えました。約1,200本という以前の見積りは固定値ではありません。

**3. コントロールラベルの中央寄せ**
- 宙の窓と同じ方式(`max-width:360px; margin:0 auto`)で、「コントロール ▼」ヘッダ行をコントロール領域の中央(コンテンツ幅360px)に寄せました。

**4. 検索エリア 6×6**
- 「:6×6」ラジオボタンを追加しました(6×6=36タイル・約24km四方・全2,359,296画素)。ワーカーの画素上限も36タイル分に拡張済みです。

**5. 検証(Playwright・合成DEM: 目的点と検索エリアの間の一部方位に高さ4,000mの壁)**
- ビューシェッド判定 vs 直線2000点サンプリング(標高グラフ方式)の一致率: ランダム200画素で**200/200(100%)**。観測点地点の判定も一致
- 画素数の分割整合: 全画素589,824 = OKのみ388,212 + NGのみ201,612(**和が完全一致**)
- 標高グラフ列: 全行が観測点地点の判定と同値(OKのみ/NGのみ/全画素のどのモードでも同じ)
- 「可視判定中… i/Nレイ」の進捗ステータスが判定中に進むことを確認
- 6×6でgridW=1536・検索完走(8件)。ctrlヘッダ実測360px・中央配置
- 回帰: verify48全節PASS(既定検索・自動選択/ズーム・メッシュマーカー色・4×4/5×5)・JSエラー0

次のご指摘をお待ちしています。引き続きよろしくお願いいたします。
