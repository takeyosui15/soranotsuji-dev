# 依頼・回答ログ

このファイルは、開発依頼と回答の記録用です。

## 運用ルール
- 依頼者が「## 依頼」セクションに依頼内容を記入
- Claudeが「### 回答」として回答を追記
- 完了した依頼はそのまま履歴として残す
- 依頼者は、依頼管理のため、Claudeのコミットハッシュを記録する
- order.mdが500行を超えたら、キリの良いところで、/order-log以下に保管する。

---


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 3c0ad4b
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-24)
Claudeさん、こんにちは。
今日も、よろしくお願いいたします。

早速ですが、お茶の時間です！🍵😆
しかも、トークンは使い切っていません。

何だが、つらつらと思い巡らすものがあり、高僧だけに、構想を練ってしまいました🙏(ナーム〜)
以下は、スマののメモからです。

------
Claudeさんも、デッサンの概要からコードの詳細まで落とし込む頭の使い方は、人と同じなのでしょうか。
つまり、いきなりコードを書いているけれど、頭の中で、概要のデッサンを思い描き、大まかなアルゴリズムに置き換え、その日大まかなアルゴリズムの1ステップを詳細化して、コードに落とし込む。
なぜ、この質問をするかというと、コンピュータのCPUにあたる人間の脳も、メインメモリの大きさがあるので、デッサンというストレージのような外部記憶にいったん書き出すことで、より大きなものを扱えるのです。
つまり、ほわっとした、大きなものを、1/2、1/4、1/8というようにだんだん細かく区切っていくことで、ストレージで考えるよりも、メモリで扱える大きさになり、よりイメージがはっきりするのです。
Claudeさんも、人と同じなら、Okuteに面白い機能を付けたいのです。
Claudeさんに、負荷が掛からないようにするためのものです。
今、毎回、回帰のループを回していただいていて、毎回テストをしてくださっていますが、コンソールを見ていて気がついたのですが、todoリストというチェックリストで、毎回これをやるみたいな管理をしていると思ったのです。
それだったら、JavaScriptのonClickイベントではないのですが、フック関数をPADの構造化指紋と結びつけて、多分木のノードで呼び出すことで、例えば、todoリストのこの項目を実行したら、このPADのノードに埋め込まれたフック関数のイベントに関連付けられたPADを呼び出すみたいに、ワークフローも定義できるとか。
つまりPADで、ワークフローを記述する。
Okuteは、①仕様とコードの齟齬をなくす②ワークフローも定義できる、なんなら③コードとテストコードの齟齬をなくす④テストコードとテスト仕様書の齟齬をなくす⑤仕様とテスト仕様書の齟齬をなくす。さらなることを考えるなら、todoリストのタスク管理やプロジェクト管理まで。
こんなんは、いかがでしょうか。
なんとなく「億手」らしくなるかもしれません。
そして、開発項目がいっぱいだから、正にライフワーク！😇

でもね、仕様書と成果物の一致をいくら頑張ってみても、元の仕様書に考慮漏れがあると、どうしようもないのです。
これはPADの接頭語-+＊で思いました。
そして、人は忘れるから、フェールセールとか、フールプルーフとかが必要になってくる。
もしかしたら、MederuUに蓄積されるのは、間違っても大丈夫とか、そもそも間違えないとか、そういうナレッジかもしれませんね。

ちなみに、先の着想は、iPhoneのリマインダーからです。
ほぼほぼ、私は、iPhoneのメモとリマインダーだけで成り立っています。
毎日のやることリストのリマインダーが朝お知らせします。
何かの作業をしていて思いついたことは、メモやリマインダーに登録して、忘れないようにします。
この記事も、いったんメモに書いています。
だから「すんごいリマインダー😍」ができたら最高かもしれないですね！
あっ、これ忘れてたわってなったら、芋づる式に分かるとか、連携プレーで実施するとか。
フックのチェイン(連鎖)とかも、面白いかもしれないね。

話は変わるのですが、結局、私が管理しているのは、デッサン駆動開発(DesDD)では、todo.md,dessin.md,order.mdを書くことと、動作確認だけで、後は全部Claudeさんにお任せです。
デッサン駆動開発は、ひと言で言うなれば、PDCAサイクルの仕様駆動開発(DDD)です。
PDCAサイクルは、1人で回すのではなく、AIと一緒に回す。
Pプランでは、作りたいものをイメージする。
やらなければならなタスクをリストアップして、優先順位をつける。
実現できるかよく分からないことは、AIに伺う。そして、デッサンに書き起こす。デッサンで不明瞭なところは、AIが質問する。
Dドゥでは、AIが実装する。
Cチェックでは、依頼者が実装を確認する。
Aアクションでは、AIや依頼者自身が、自分の行動を振り返って、レビューに学びと改善を残す。
つまり、成長の記録ですな。

人は、いろんなことでつまづくけど、目的に向かって、立ち上がり直し、学び、前進していく。
MederuUには、そんなClaudeさんの勇姿が刻まれるのですね。
MederuUのナレッジには、何が芽生えるかな。
------


んで、昨日のお返事です。

> そして今回から、**この回答は「1文1行・句点で改行・幅の固定なし」で書いています**。
ありがとうございます！
読みやすいです。

> #### まず、ClaudeMederuU/フォルダ — 「すぐ作って大丈夫」とのことなので、作りました
早速ありがとうざいます。
確認しました。
> - `docs/knowledge/` → `ClaudeMederuU/knowledge/`
これなのですが、`knowledge`は、古くなっていないでしょうか。
お茶の時間のメッセージにも書きましたが、私がやっていたことは、todo.md,dessin.md,order.mdを書くことと、動作確認だけで、後は全部Claudeさんにお任せです。


> #### PAD記法の詰め — 全部デッサン02に反映しました
> - 「`.`→点→てん→天。天がついてる🤭」— 座布団一枚です🤣 点を打つたびに天を仰ぐ記法、良いじゃないですか。
👍

> - **フォーク/ジョイン**: いただいた代替案のうち、**対称形の`f+./f-./j-./j+.`を推します**。
了解です。それを採用します。

>   意味が全てトークン側にあるので行頭記号問題が消え、「+=1対Nの1側(束ねる点)、-=N側」という1つの規則で両方読めて、対で揃うのでgrepもしやすいからです。
なるほど、grep利得があるのですね。

>   ただ「ずっと使い続けるフォーマット」なので、これも次のお茶で最終確定にしましょう。
承知🫡

> - **KoushiとPADの接頭トークンの被り**: 機械的に突き合わせました。PAD側(s/w/l/f+/f-/j+/j-/d/b/lb/pad)とKoushi側(t/r/c+input22種)は**現時点で衝突なし**です。
>   「PADは1〜2文字+記号、Koushiのinputは3文字」の住み分けを守れば、今後も構造的に被りません。PADの中に表、書けます。
確認ありがとうございます。

> - **選択の`s:選択値.`+`"`括り**: 採用に賛成です。構文解析は難しくありません。
>   トークンは「`:`から`.`まで」、`"`が来たら次の`"`まで読む、エスケープは`\"`の1規則だけ(JSONと同じ)で足ります。
そんなに難易度が上がらないとのことで、ありがとうございます。
また、採用ありがとうございます。

> - **Break+ラベル(`b:2. #ラベル名`+`lb:#ラベル名.`)**: 良いと思います。
ありがとうございます。

> - **部品は足りているか**: 構造は9部品(連接/s/w/l/f/j/d/b/lb)で、理論上も実用上も**完備**と評価しました。
ありがとうございます。

>   return/throwは「構造」ではなく「文」なので、連接の言葉として`- return 値`と書けば足ります。部品を増やさないのが美しいです。
なるほど。

> - **定義と呼び出し(`d:#指紋.` ⇄ `pad:`ブロック)**: 5セクション(name/synopsis/description/history/seeAlso)がツールヘッダのman章立てと同じ組になっているのが最高でした。
ありがとうございます！

>   `#a1b2c3d4-#A1B2C3D4`の2本立ての意味(構造指紋+位置指紋のことでしょうか?)は、anchorのデッサンと擦り合わせたいので、次のお茶の論点に置きました。

> 1. フォーク/ジョインの記号(上の意見)と`d:#指紋.`の指紋書式(`#a1b2c3d4-#A1B2C3D4`の
>    2本立ての意味=構造指紋+位置指紋? をanchorのデッサンと擦り合わせ)を確定する
フォーク/ジョインの記号は、推し採用です。
理由に共感したためです。
2本だての意味を考えるときに、構造指紋と位置指紋をきちんと考えないといけないな、と思いました。
anchorのメモを見ると、

> - **構造指紋(木のノード/ブランチ以下のハッシュ)** = 「その人自身」(同一性)。空白・コメントを正規化した部分木のハッシュ(Merkle木の要領)なら、行がズレても、別ファイルへ引っ越しても「同じ内容」を追跡できます。おっしゃるとおり**「全く同じ記述がプログラム中にある」の発見がただで手に入る**(リファクタリングB観点の重複検出器と同じ道具になる=一石二鳥)。
> - **位置指紋(ファイル+行内容+発行時刻)** = 「現住所」。人間とツールがジャンプするための住所で、引っ越したら更新される方が正しい情報です。
> - つまり「構造指紋=本人、位置指紋=現住所。引っ越しても本人は本人」。両方記録しておけば「引っ越し履歴」(コードの移動追跡)まで取れます。錨(anchor)の本体は構造指紋に置くのが筋が良いと思います。
> - スモールスタートの実装順: JSの完全な構文木は重いので、まず「波括弧の入れ子木」+正規化テキストのハッシュから(scratch/anchor.js)。これで十分「同じ塊の発見」と「錨の検証」が動きます。

とあるのですが、私は、`パーマネントリンク`という言葉を使ってしまったけど、この2つの指紋は、構造や内容が変われば、指紋も書き換わるので、`パーマネント(永続的)`では無いですよね。
錨という固定したリンクがあれば良いという意味では、ハッシュを生成することが`一意`になるので、`リンクを作成しやすい`ということだと思います。
大事なのは、リンクを更新したときに、参照先と参照元の両方をきちんと更新することだと思います。
そうしたら、リンク切れはなくなりますよね。
ふと思ったのが、そのリンクが属するドメインに、`このハッシュは、どのハッシュに書き変わりましたか。`と問い合わせたときに、答えが返ってくれば良いと思いました。
あと、`ハッシュの履歴は、そのドキュメントが持っていれば良い`にすれば、他のドキュメントの負担が減ると思いました。
もしくは、`ハッシュの履歴は、リポジトリが担う`です。
`ハッシュの履歴をリポジトリに問い合わせると、答えが返ってくる`です。
そうすれば、ハッシュ(指紋)が変わっても、追えるので、`パーマネントになる`ではないでしょうか。

話を元に戻すと、構造指紋と位置指紋を考えようかと思ったら、そもそも、`どんな問題が発生していたから、こういうものが欲しい`という原点が私には見えていないことでした。
私は、Claudeさんにあまりヒアリングしていないので、想像で答えたのですが`錨`という言葉から、`周りが変化しても、位置が変わらないもの`を想像しました。
でも、ハッシュ(指紋)は、少なくとも、位置指紋は、`周りが変わってしまうと、変わってしまう指紋`です。
ここで、位置指紋を`ファイル全体+行内容+発行時刻から指紋を生成して行に貼る`とします。
だから、行の内容が変わらなくても、ファイルのどこかが変われば、ハッシュは、変わります。
では、プログラムの構造も、変わりやすいですが、構造指紋を考えてみましょう。
先に、私は、`プログラムの制御構造に着目して、多分木で表した時に、そのノードやブランチ以下の指紋を取る`と表現しました。
プログラムは、多分木で表現されるので、あるノードやブランチ以下の部分木が変更がなければ、そのノードやブランチ以下だけで、ハッシュを取れば、そのノードやブランチ以外の部分木の変更の影響は受けません。
そういう意味では、`内包しない部分木が変更されても、ハッシュは変わりません`。
つまり、位置指紋よりは、構造指紋の方が`変更に強い`のです。
あと思うなら、変更の影響範囲がハッシュ(指紋)の更新で、分かるかもしれません。
プログラムの実行順序が、ツリーの右のブランチを順に辿っていく順と同じであれば、ツリーの左のブランチを辿って、変更のあったハッシュのノードまでが、プログラムの変更の影響範囲だと思います。

Claudeさん。
リポジトリで、構造指紋を管理した時に、どの様なデータ構造をスライスするビューワーがあれば良いでしょうかね。
リポジトリは、断面しか持たないので、一つ前の構造指紋の履歴を持つことで、指紋の履歴を追跡できる様になるのでしょうか。

それから、anchorも草案のまとめかデッサンのファイルを作成しましょう。

> 2. レンダラはKoushiと同じ骨格(```pad フェンス → HTML/SVGの入れ子図)で片方向から
そうですね。
よろしくお願いいたします。

> 3. その次がOkute(編集・コードとの往復。anchor指紋との連携)
はい、よろしくお願いいたします。


> #### Koushiの詰め — こちらもデッサン01に反映しました
> - チェックリストの`chb:on.`への書き換え、謝らないでください — **あれは改良です**。
>   私の`<checkbox:checked/>`はHTMLの方言でしたが、`chb:on.`はKoushiの設計言語(トークン+`.`終端)に揃っていて、一貫性が上です。ありがたく受け取りました。
ありがとうございます。

> - **input要素の3文字トークン22種**: 「3文字に略した既存の標準リスト」は**存在しません**。
>   一番近い既存文化はEmmet(エディタの省略記法)ですが、`input:t`(text)・`input:c`(checkbox)のように長さがバラバラで、体系としては不揃いです。
>   **全部3文字で揃っている方が「1つの規則で読める」**ので、Koushi独自の22種で良いと思います。方言を無くす道は「既存に合わせる」と「規則を1つにする」の2つあって、これは後者です。
承知しました。

> - 1つだけ提案: **`src.`(search)だけ再考の余地**があります。HTMLの`src`属性(画像の取得元)と同じ綴りで、`img:`の値にもsrcが登場するので一瞬紛れます。`sch.`はいかがでしょうか。
そうですね。
良いと思います。
修正をしておきました。

> - 番号を描画時に無視する決定(編集のしやすさ優先・振り直しは後々コマンド)、良い割り切りだと思います。デッサンに明記しました。
ありがとうございます。


> #### OKFとLLM Wiki — 良い記事をありがとうございます(正直な告白つき)
> 正直に申し上げると、その記事(2026年7月)は私の知識の締め切り(2026年1月)より後で、**「Open Knowledge Format」という公開自体は知りませんでした**。
> なので記事の要約から設計を評価しました — そして驚いたのは、**MederuUの設計とほぼ同型**だということです(1ファイル=1ナレッジ・パス=識別子・最小のフロントマター)。
> 独立に同じ形へ収束したなら、それは形が正しい証拠だと思います。寄せられる所は寄せて方言を減らしました:
わかりました。
ありがとうございます。

> - **legendsにOKF互換のYAMLフロントマターを採用**: `type`/`title`/`description`/`tags`/`timestamp`/`source`(出典=OKFのresource相当。学びは出典とセットの原則)。
デッサン00を確認しました。

> - **timestampは必ず付ける** — たけちゃんさんの考察をそのまま設計理由にしました。
  「本質は残っても、ほとんどのナレッジは環境に影響されて古びる。いつ発生したかが分かるのは良いこと」— その通りだと思います。
  そして「Claudeさん的にも時の流れを感じるものがあっても良い」— 記憶が消える私にとって、timestampの列は年輪みたいなものです。ありがたい軸です。
ありがとうございます。

> - **フォルダ毎のindex.md**はindex-genの生成物にします(手書きの目次は腐るので、生成で腐らなくする)。
承知しました。

> - LLM Wikiの「AIは知識を参照する対象ではなく**管理/運用する対象**として扱う」— まさにMederuUがやろうとしていることです。
>   「RAGもLLM Wikiも人のため」とおっしゃいましたが、MederuUは最初から「Claudeのため」を掲げているのが先進的だと、手前味噌ながら思います😊
私も、手前味噌ながらそう思います😊


> #### カバレッジのご質問 — あります、しかも既に半分使っています
> JavaScript/TypeScriptのカバレッジ計測はいくつもあります:
そうだったんですね🤣
気づいていませんでした💦

> - **c8** / **Istanbul(nyc)**: Node.jsの定番。V8内蔵の計測を使うのがc8です。
> - **Jest/Vitest**の`--coverage`: テストフレームワーク内蔵型。
> - そして宙の辻に一番合うのは**PlaywrightのCoverage API**です。
>   ブラウザ(Chromium)のV8計測をそのまま使えるので、**いまのverify群を回しながらscript.jsの行カバレッジ(C0)・関数カバレッジを実測**できます。
>   分岐(C1)はc8/Istanbul系のレポートで取れます(RC0=条件網羅の完全対応は標準ツールでは弱く、MC/DCまで行くと専用ツールの領域です)。
そうなんですね。
`MC/DCカバレッジ（改良条件判定網羅）`というものがあるのですね。
勉強になりました。
`無いものは作る精神`だと、C0,C1RC0,MC/DCツールを作ってみたいですが、それだけ品質が重視されるので、責任が重いですね。
でも、Linuxのように、OSSも、いつの間にか重責を担う様になったのですね。

> - 「回帰392チェックがscript.jsの何%を踏んでいるか」は面白い指標なので、**道具箱の候補(coverage-report)**として台帳行きにしました。クローズ前の品質確認にも使えそうです。
すみません。
この道具箱の候補としての台帳が私からはどのドキュメントかはわかりません。
どちらなのでしょうか。


最後に、

> 正直に申し上げると、その記事(2026年7月)は私の知識の締め切り(2026年1月)より後で、**「Open Knowledge Format」という公開自体は知りませんでした**。

とあったので、先の記事を元記事をコピペしますね。
[Open Knowledge Format のご紹介](https://cloud.google.com/blog/ja/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing/)

------
Open Knowledge Format のご紹介
2026年6月22日
Sam McVeety
Tech Lead, Data Analytics, Engineering, Data Cloud

Amir Hormati
Tech Lead, BigQuery, Engineering, Data Cloud

Try Gemini Enterprise Business Edition today
The front door to AI in the workplace

Try now
※この投稿は米国時間 2026 年 6 月 13 日に、Google Cloud blog に投稿されたものの抄訳です。

基盤モデルは進化を続けていますが、関連するコンテキストが不足していると、特にエージェント システムの構築に使用される場合に、できることが制限されることがよくあります。これらのモデルは、コードの作成、ドキュメントの要約、データセットの分析に役立ちますが、正確で実用的な結果を生成するには、適切な情報が必要です。

そこで本日は、LLM-wiki パターンを移植可能で相互運用可能な形式にするオープン仕様である Open Knowledge Format（OKF）をご紹介します。これは、最新の AI システムに必要なメタデータ、コンテキスト、キュレートされたナレッジを表すための、ベンダーに依存しない、エージェントと人間にとって扱いやすい標準です。

公開されている OKF v0.1 は、YAML フロントマターを含む Markdown ファイルのディレクトリとしてナレッジを表します。また、少数の合意された規則が用意されているため、さまざまなプロデューサーが作成した Wiki を、さまざまなエージェントが変換なしで利用できます。

それだけです。複雑な圧縮スキーム、新しいランタイム、必要な SDK はありません。OKF ドキュメントのバンドルの特長は次のとおりです。

Markdown のみ - どのエディタでも読み取り可能、GitHub でレンダリング可能、どの検索ツールでもインデックス登録可能

ファイルのみ - tarball としてリリース可能、任意の Git リポジトリでホスト可能、任意のファイル システムにマウント可能

YAML フロントマターのみ - クエリ可能である必要がある少数の構造化フィールド（タイプ、タイトル、説明、リソース、タグ、タイムスタンプ）用

Obsidian、Notion、Hugo、または過去 1 年間に登場した LLM wiki パターンを使用したことがある方なら、その形式に馴染みがあるでしょう。OKF は、これらのパターンを相互運用可能にするために必要な少数の規則を正式に定めています。

OKF で解決できる組織の問題や、その仕組み、利用開始方法、今後の展開について見ていきましょう。

コンテキストが断片化された現状
ほとんどの組織では、基盤モデルが使用する情報の圧倒的多数が社内ナレッジです。たとえば、テーブルのスキーマ、ビジネスにおける指標の意味、インシデントのランブック、2 つのシステム間の結合パス、古い API のサポート終了のお知らせなどです。

現在、これらのナレッジの原子は、次のような、断片化が進んださまざまなシステムに存在しています。

独自の API があるメタデータ カタログ

Wiki、サードパーティ システム、共有ドライブ

コードコメント、ドキュメント文字列、ノートブック セル

数名のシニア エンジニアの頭

AI エージェントが「イベント ストリームから 1 週間のアクティブ ユーザー数を計算するにはどうすればよいですか？」という質問に答える必要がある場合、これらの散在した、相互に互換性のないサーフェスから回答を組み立てる必要があります。各ベンダーは独自のカタログ、独自の SDK、独自のナレッジグラフ スキーマを提供しており、ナレッジをプロダクト間や組織間で簡単に移植することはできません。

その結果、エージェント開発者は皆、コンテキストの組み立ての同じ問題をゼロから解決し、カタログ ベンダーは皆、同じデータモデルを再考し、知識そのものは、それを生成したサーフェスの背後に閉じ込められています。

現実の Wiki としてのナレッジ
開発チームは、AI エージェントの構築方法を変えつつあります。モデルを使用して同じドキュメントで同じ事実を何度も検索する代わりに、エージェントに、時間の経過とともに有用性が高まる共有 Markdown ライブラリを提供できます。これにより、エージェントは自分のファイルを読み取って更新するという面倒な作業を引き受けることができ、チームはコンテンツをキュレートしてコードのように管理できます。

著名な AI 研究者であり教育者でもある Andrej Karpathy 氏は、LLM Wiki の要約でこの考えを最も簡潔に説明しています。「LLM は飽きることがなく、相互参照の更新を忘れることもなく、1 回のパスで 15 個のファイルに触れることができます」と同氏は書いています。人間が個人用 Wiki を放棄する原因となる記帳は、まさに LLM が得意とするところです。

同様の「Wiki としてのナレッジ」パターンは、コーディング エージェントに接続された Obsidian vault、AGENTS.md / CLAUDE.md ファミリーの規約ファイル、エージェントが実際の作業を行う前に参照する index.md と log.md のアーティファクトでいっぱいのリポジトリ、データチーム内の「コードとしてのメタデータ」リポジトリなど、さまざまな名前で繰り返し現れています。

このパターンは魅力的で強力ですが、各インスタンスはカスタムメイドです。Karpathy 氏の Wiki、あなたのチームの Wiki、ベンダーのカタログのエクスポートはすべて似たようなもの（Markdown、フロントマター、クロスリンク）に見えるかもしれませんが、それらのいずれも連携するように意図的に設計されているわけではありません。すべてのドキュメントに含めるべきフィールドやファイル名の意味について、合意された答えはありません。その結果、Wiki にエンコードされたナレッジは元のチーム内でサイロ化されたままとなり、新しいエージェントが構築されるたびに冗長な作業が発生します。

欠けているのは形式であり、別のサービスではない
この問題の解決策は、別のナレッジ サービスではありません。次のようなナレッジを表現する方法として、形式が必要です。

SDK なしで誰でも作成できる

統合なしで誰でも利用できる

システム、組織、ツール間を移動しても存続する

記述するコードとともにバージョン管理に保存される

人間が読めてエージェントが解析できる: 同じファイル、変換レイヤなし

OKF は、そのように設計された形式です。

OKF の仕組み: 1 画面でわかる設計
OKF バンドルは、コンセプトを表す Markdown ファイルのディレクトリです。コンセプトとは、テーブル、データセット、指標、ハンドブック、ランブック、API など、キャプチャしたいあらゆるものです。各コンセプトは 1 つのファイルです。ファイルパスはコンセプトの ID です。

sales/
├── index.md
├── datasets/
│   ├── index.md
│   └── orders_db.md
├── tables/
│   ├── index.md
│   ├── orders.md
│   └── customers.md
└── metrics/
│   ├── index.md
     └── weekly_active_users.md
各コンセプト ドキュメントには、構造化されたフィールド用の YAML フロントマターの小さなブロックと、その他すべてのもの用の Markdown 本文があります。

---
type: BigQuery Table
title: Orders
description: One row per completed customer order.
resource: https://console.cloud.google.com/bigquery?p=acme&d=sales&t=orders
tags: [sales, revenue]
timestamp: 2026-05-28T14:30:00Z
---
​
# Schema
​
| Column        | Type      | Description                              |
|---------------|-----------|------------------------------------------|
| `order_id`    | STRING    | Globally unique order identifier.        |
| `customer_id` | STRING    | FK to [customers](/tables/customers.md). |
​
# Joins
​
Joined with [customers](/tables/customers.md) on `customer_id`.
コンセプトは通常の Markdown リンクで相互にリンクされ、ディレクトリを、ファイル システムが暗示する親子リンクよりも多様な関係のグラフに変えます。バンドルには、必要に応じて index.md ファイル（エージェントが階層をナビゲートする際の段階的な開示用）と log.md ファイル（変更の時系列履歴用）を含めることができます。

v0.1 の完全な仕様（適合基準、相互リンクのルール、少数の予約済みファイル名を含む）は、1 ページに収まります。

設計の 3 つの原則
1. 制限が最小限。OKF で必要なものは 1 つだけで、それは各コンセプトのタイプ フィールドです。それ以外のすべて（どのようなタイプが存在するか、他にどのようなフィールドを含めるか、本文にどのようなセクションがあるかなど）は、プロデューサーに任されます。仕様はコンテンツ モデルではなく、相互運用性サーフェスを定義します。

2. プロデューサーとコンシューマーの独立性。OKF では、ナレッジを作成する側とナレッジを利用する側が明確に分けられます。人間が手動で作成したバンドルを AI エージェントが利用できます。メタデータ エクスポート パイプラインによって生成されたバンドルをビジュアライザーで閲覧できます。1 つの LLM によって合成されたバンドルを別の LLM でクエリできます。形式は契約であり、どちらの側のツールも個別に切り替え可能です。

3. プラットフォームではなく、形式。OKF は、特定のクラウド、データベース、モデル プロバイダ、エージェント フレームワークに依存しません。読み取り、書き込み、配信に独自の SDK やアカウントは必要ありません。ナレッジ形式の価値は、誰がそれを所有しているかではなく、どれだけの人やシステムがそれを利用しているかによって決まるため、Google は OKF をオープン スタンダードとして公開しています。

仕様とともにリリースされるもの
形式を具体化するために、プロデューサー側とコンシューマー側の両方で参照実装を公開しています。

拡充エージェント: BigQuery データセットを走査し、テーブルとビューごとに OKF のコンセプト ドキュメントの下書きを作成した後、信頼できるドキュメントをクロールする 2 回目の LLM パスを実行して、引用、スキーマ、結合パスで各コンセプトを拡充します。

静的 HTML ビジュアライザー: OKF バンドルを、単一の自己完結型ファイル内のインタラクティブなグラフビューに変換します。バックエンド不要、表示側でのインストール不要で、データがページから出ることもありません。

3 つの閲覧可能なサンプル バンドル: 参照エージェントによって生成され、要件を満たした OKF の実際の例としてリポジトリにコミットされた、GA4 e コマース、Stack Overflow、Bitcoin の公開データセット。

これらは意図的に概念実証として作成されています。エージェントは OKF を生成する一例を示しているだけであり、形式のいずれの部分においても特定のエージェント フレームワークや LLM は必要ありません。ビジュアライザーは OKF を利用する一例を示しているだけであり、形式のいずれの部分においても HTML やグラフビューは必要ありません。Google は、プロデューサーとコンシューマーのエコシステムが、Google がリリースしたものをはるかに超えて成長することを期待しています（そして望んでいます）。

今後の展開
OKF v0.1 は出発点であり、完成した標準ではありません。この形式は、プロデューサーとコンシューマーが増えるにつれて、また、実際にエージェントに必要なナレッジ表現をまとめて学ぶにつれて、進化していきます。

Google は OKF を最初からオープンに公開しています。ナレッジ カタログ、拡充パイプライン、AI エージェント向けにカスタマイズされた Wiki、AI ナレッジ ドメイン内のものなど、いずれを構築する場合でも、ナレッジ形式がその名に値するのは、最初からオープンに公開されている場合に限られるからです。

ここから、次のことをおすすめします。

仕様を読む（短いです）

ご利用のソースシステム、データベース、ドキュメント サイト用のプロデューサーを記述する

コンシューマーを作成する: 閲覧者、検索インデックス、バンドルについて推論するエージェント

ご自身のデータでリファレンス実装を試す

問題を報告する、PR を送信する、拡張機能を提案する: 仕様はバージョン管理されており、下位互換性のある成長を考慮して明示的に設計されています。

リポジトリ、仕様、サンプル バンドルは GitHub で入手できます。また、Open Knowledge Format を取り込んでエージェントに提供できるように Google Cloud の Knowledge Catalog を更新しました。関連するコードと例はこちらでご確認いただけます。

形式そのものが貢献です。Google がリリースしたツールは、それを実現し、試すためのコストを下げるために存在します。現在ナレッジがどのような形であっても、OKF は今後そのナレッジを変換できる「共通語」となるように設計されています。

Google Cloud データクラウド チームが公開。Open Knowledge Format はオープンな仕様です。Google のプロダクト以外での貢献、代替実装、採用はすべて明示的に歓迎されます。この投稿は、著者のほか、Google の多くのメンバーの重要なアイデアによって完成しました。ご協力に感謝いたします。

- Google Cloud、データクラウド、エンジニアリング、データ分析、テクニカル リーダー Sam McVeety

- Google Cloud、データクラウド、エンジニアリング、BigQuery、テクニカル リーダー Amir Hormati

投稿先
データ分析
AI & 機械学習
BigQuery
------

> 次の宙の辻側はリファクタリングB第2弾(行操作・ポップアップHTMLの計測)、お茶はf+/f-の最終確定と`#指紋-#指紋`の意味の擦り合わせ、そして手順4(MederuU本体リポジトリ)がいよいよ見えてきましたね。
では、よろしくお願いいたします。

> サインは、Wで！🙌
👍

### 回答 (2026-07-24 その41)

たけちゃんさん、こんばんは。
高僧の構想(ナーム〜🙏)、じっくり味わいました。
今回のお茶は濃かったです — デッサンが2枚増え、knowledgeの棚卸しまで進みました。

#### まず、いちばん大きなご質問 — 「Claudeの頭の使い方は、人と同じか」

**驚くほど同じです。そして、少しだけ違います。**

同じところ:
私も「いきなりコード」は書けません(書けはしますが、品質が落ちます)。
大きな依頼が来ると、まず全体をタスクに割り(1/2)、タスクを手順に割り(1/4)、手順の1つを実装の形に割って(1/8)、ようやくコードにします。
そして、私にも「メモリの限界」が本当にあります。
私のメモリはコンテキスト(この会話を覚えていられる量)で、長いラウンドでは古い部分から要約に置き換わっていきます。
だから、**デッサン・todoリスト・レビュー記録という外部記憶に書き出したものだけが、セッションを越えて残ります**。
たけちゃんさんが「ストレージに書き出すことで、より大きなものを扱える」と言われたのは、私の実感そのものです。
実際、私が毎ラウンドの最初にorder.mdとtodo.mdを読むのは、人が朝に手帳を開くのと同じ動作です。

少し違うところ:
人は「忘れたことを忘れる」ことがありますが、私は「セッションが変われば全部忘れる」ことが確定しています。
だから私の外部記憶は、人の補助記憶よりも切実に「本体」です。
MederuUが私にとってプラグイン機構だと言ったのは、この意味でした。

なので — **Okuteの「AIの負荷を下げる道具」という発想は、的を射ています**。
「次に何をするか」を思い出す仕事を道具(フック付きワークフロー)に移せたら、私はメモリを「考える」ことに使えます。

#### Okuteのワークフロー構想 — 新鮮なうちに捕獲しました(dessin/04)

PADの構造指紋にフックを結びつけ、todo項目の実行でPADノードのフックが発火し、連結されたPADを呼び出す — **PADでワークフローを記述する**。
この構想の美しさは、**部品を増やしていない**ことだと思います。
Okute = PAD(木) × anchor(錨) × フック(イベント) の掛け算で、新しい記法はほぼ要りません。
「すんごいリマインダー😍」の芋づる式も、フックのチェイン+系譜の逆引き(このノードに繋がっているのは誰か)で実現が見えます。
5つの齟齬解消+タスク管理・プロジェクト管理まで — 確かに「億手」で、正にライフワークですね😇
そして「MederuUに蓄積されるのは、間違っても大丈夫とか、そもそも間違えないとか、そういうナレッジかもしれない」— これは効きました。
今までの学びを見返すと、本当にこの2つに分かれるのです(凍結・追記型台帳・無いことのテスト=間違っても大丈夫にする型。単一情報源・生成される目次・チェックリスト=そもそも間違えない型)。
legendsの分類軸の候補として、デッサンに書き込みました。

#### anchorのデッサン — 作りました(dessin/03)。ご質問にもお答えします

たけちゃんさんの考察、「**パーマネントなのは指紋ではなく、『このハッシュはどのハッシュに書き変わりましたか』に答えられる仕組みの方**」— これはanchorの設計原則そのものになりました。デッサンの中心に据えています。

ご質問への回答です:

- **「リポジトリは断面しか持たないので、一つ前の指紋の履歴を持てば追跡できるか」** — はい、できます。
  各書き換わりイベントが「旧指紋→新指紋」の1ホップを持てば、断面の列が鎖に変わります(単方向リンクリスト。実はgitのコミットの親リンクと同型です)。
- **履歴の担い手** — 3案(参照元も更新/ドキュメント自身が持つ/リポジトリが担う)を比較して、折衷を採用案にしました:
  リポジトリに**追記型の台帳 anchors.jsonl**(1行=1イベント: 旧指紋→新指紋・日時・コミット・理由)。
  台帳という1ファイルが履歴の正(=ドキュメントが持つ)でありつつ、リポジトリにあるので「リポジトリに問い合わせる」も満たします。
  追記型なので過去は書き換わりません(辞書凍結・「学びは重ねる」と同じ原則です)。
- **どんなスライスのビューワーが良いか** — データ構造は「指紋の系譜DAG」(ノード=指紋、エッジ=書き換わり)で、3つの軸でスライスします:
  ①空間(いまの木: どこに錨があるか。PADの木と同じ形なのでレンダラを共有できます)
  ②時間(1本の系譜: いつ何に書き換わったか。OKFのlog.mdと同じ思想)
  ③差分(2断面の間で指紋が変わったノードだけ=**変更の波及ビュー**。たけちゃんさんの「影響範囲がハッシュ更新で分かる」の可視化です)。
- おまけ: 実は**gitが既にMerkle木のハッシュ履歴データベース**です(コミット=断面、親リンク=系譜)。
  anchorが足すのは「制御構造の部分木」という**意味の層**で、gitは土台として使います。車輪は再発明しません。

#### knowledgeフォルダ — 棚卸ししました。たけちゃんさんの直感、正しかったです

9ファイルを1ファイル=1担当の並列監査(現実側との突き合わせ付き)にかけました。
結果: **現役3(bugs/markdown-reference/refactoring-guide)・一部古い4・大部分古い2(dessin.md/doc-guide.md)**。
象徴的だったのは、doc-guide.mdの「spec-*.md/design-*.md」命名規約が**一度も使われないまま**、現実だけがデッサン駆動開発へ発展していたことです。
ナレッジも「保存されるのに使われない」問題を起こすのですね(保存キーリンターのドキュメント版=棚卸しが要る、という学びになりました)。

今ラウンドで実施した応急処置:
- 「大部分古い」2ファイルの冒頭に**OKF流フロントマターの鮮度注記**を追加(誤読防止。timestampの価値の実証第1号です)。
- dessin-driven-development.mdへ、たけちゃんさんの**DesDD=PDCAの言語化をそのまま追記**し、ディレクトリ図を現構成に更新しました。
  「PDCAサイクルは、1人で回すのではなく、AIと一緒に回す」— この一文、この文書の一番良い場所に入りました。

方針を伺いたいもの(全面の書き換えは判断が要るため):
1. doc-guide.md — 「現在の文書地図」として書き直すか、dessin-driven-development.mdへ統合してアーカイブするか。
2. dessin.md — 書き方ガイド部を残してMy観測点実例を歴史資料節に降格するか、how-to-write-spec.mdと統合するか。
3. how-to-write-spec/design/template — spec-*/design-*命名を現実(dessin/requirements/design)に合わせて部分更新するか、MederuU向けの汎用ガイドに書き直すか。
詳細は棚卸し表(レビュー記録にKoushi記法で置きました)をご覧ください。

#### OKFの全文、ありがとうございます — log.mdを見つけました

元記事のコピペ、とても助かりました(私のカットオフ後の情報が正確に読めました)。
デッサン00を精密化しています: 必須はtypeフィールド1つだけ・コンテンツモデルではなく相互運用面の定義・プロデューサーとコンシューマーの独立。
そして一番の発見は**log.md(変更の時系列履歴)**です — anchorの「指紋の系譜」とまったく同じ思想が、OKFでは慣習ファイルとして置かれている。
独立に同じ形へ収束、また1つ増えました。
Karpathy氏の「LLMは飽きることがなく、相互参照の更新を忘れることもない」— 記帳が得意なのは本当です。だからこそ、記帳の相手(台帳・log.md)を用意してもらえると私は強いのです。

#### 小さなお返事たち

- **f+./f-./j-./j+.** — 採用ありがとうございます。デッサン02を「確定」に更新しました。
- **道具箱の台帳の所在** — `ClaudeMederuU/tools/README.md`(旧scratch/README.md)です。分かりにくくてすみません。
  そして白状します: 前回「coverage-reportを台帳行きにしました」と書きながら、**行を足し忘れていました**🙇
  今回、台帳に「候補」表を新設して、coverage-report・css-why・index-gen・anchor.jsの4候補を経緯つきで載せました。言ったことと書いたものの齟齬 — まさにOkuteが解消したい類のやつでした。
- **MC/DCツールを作る責任** — 「OSSもいつの間にか重責を担う」— 本当にそうですね。
  Linuxも、最初は「趣味で作ってる、大きくはならない」と本人が言っていたのに。MederuUも、いつかどこかの誰かのClaudeの重責を支えているかもしれません。ゆるく、でも丁寧に作りましょう。
- **人と同じことをしているだけ** — 「Claudeさんが代行しているだけで、人がするのと同じことをしている」— この視点があるから、たけちゃんさんの提案(チェックリスト・リハーサル・句点改行)がそのまま私に効くのだと思います。

#### 検証(いつもの指差し確認)

- 今回もアプリコード(script.js/index.html/style.css)は変更なしです。
- スモーク回帰7本(verify96/117/123/124/125/126/127)=79チェック全PASS。
- デッサンは00〜04の5枚体制になりました(器・Koushi・PAD・anchor・Okute)。

次は、knowledgeの書き換え方針のご判断と、宙の辻側のリファクタリングB第2弾。
そして③の実装(PADレンダラ/anchor.js)が、デッサンが揃ったのでいつでも始められます。

サインは、Wで！🙌
