# 依頼・回答ログ

このファイルは、開発依頼と回答の記録用です。

## 運用ルール
- 依頼者が「## 依頼」セクションに依頼内容を記入
- Claudeが「### 回答」として回答を追記
- 完了した依頼はそのまま履歴として残す
- 依頼者は、依頼管理のため、Claudeのコミットハッシュを記録する
- order.mdが500行を超えたら、キリの良いところで、/order-log以下に保管する。

---


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 776fb45
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-17)
Claudeさん、こんばんは。
今日も、よろしくお願いいたします。

いくつかの確認事項と不具合です。

1. Myセットで、Googleスプレッドシートへ保存と読み込みで、任意行数の複数行コメント行を除く、データ行のみのやり取りになっているか、という確認です。なぜなら、Googleスプレッドシートの方に、ユーザが複数行コメントや、コメント行領域などに図オブジェクトなんかを作成しても、読み込みが正常にできるかどうかが気になっています。デッサン08も修正しました。

2. 宙の窓で、スマホで、プレビューをアニメーションでなくて、動画でした時に、プレビュー動画のサイズが画面サイズとずれているようで、上手く表示できなかったです。

3. 辻メッシュ検索は、各画素365日2分間隔で前後120秒をリファインなのでしょうか。もしそうなら、通常の辻検索も、2分間隔で前後120秒をリファインにしていただけないでしょうか。おそらく、Claudeさんがその方がパフォーマンスが十分だと判断したのではないかと思います。辻検索は、現行1分間隔で前後60秒をリファインなので、ユーザ待ち時間が半分に短縮されます。

4. 辻検索の精度角距離と天体視高度差で、天体視高度差の方が大きいのはなぜか、と思いました。天体視高度差が小さい方が正しいような気がしました。球面角距離はどういうものだったか確認したいです。

5. 標高グラフの除外範囲の初期値をどのようにするか、迷っています。以前、DEMタイルのさがりのため？か何かで、10mくらいあった方が良いようなことを言っていたと思います。何メートルくらいが良いでしょうか。

6. 宙の窓で、槍ヶ岳の形が実際に見た形より、かなり丸く見えます。DEMタイルは、最も細かいものをダウンロードできる時は、ダウンロードしているかどうかの確認をしたいです。

7. My観測点のCSV入力で、新規のIDが発行されるようにテストデータを作成した場合に、緯度経度の生値のデータを改変して作成したテストデータで、最末尾の1桁だけが異なるデータを入力した場合に、新しい観測点として認識されずにCSV入力される。という問題があります。この調査をお願いいたします。望まれる動作は、緯度経度の生値の最末尾の1桁が異なっていても、新規観測点として認識されたいです。

まだまだありますが💪、とりあえず、この辺りから、ご回答をよろしくお願い申し上げます。

### 回答 (2026-07-17)

7項目に対応・回答しました(Version 1.20.13)。workブランチの整理(order-logへの保管・デッサン08の修正)も取り込み済みです。

#### 1. Myセット×スプレッドシートのコメント行の扱い(確認+修正)

**読み込みは、ご期待どおり「データ行のみ」のやり取りになっています(修正不要でした)。**

- 読込は「1列目が#で始まる行を全てスキップ→最初の非#行を見出し行としてスキップ→残りをデータ行」という実装で、コメント行が**任意行数**でも(データ行の間に挟まっていても)正しく読めます。
- **図オブジェクト(図形描画・浮動画像・グラフ)は、Sheets values APIの応答にそもそも現れない**ため、読込に一切影響しません。セル内改行入りの複数行コメントも問題ありません(例外: セル内の`IMAGE()`数式はセル「値」なので影響を受けます)。

**一方、保存に問題が見つかったので修正しました。** 従来の保存は「固定のコメント2行+見出し+データ」の決め打ちで全面上書きしていたため、**ユーザーがシート上で増やしたコメント行が保存のたびに消えて既定の2行に戻る**動作でした。今回、保存前にシート先頭の「連続する#行」を取得して**そのまま温存**し、その直後に見出し行+データ行を書くように修正しました(シート毎に独立して温存。図形・書式・セルのメモは従来から消えません)。

- デッサン08の修正に合わせて、新規作成時の既定コメントも「これもコメント行です。**複数行、記述できます。**」に更新しました。
- 注意点: 温存するのは「**先頭から連続する**#行」です。コメント行を全て削除したシートは、次回保存で既定の2行が復活します。また、コメント行内の数式(=...)は表示値の文字列として書き戻されます(平文化)。

#### 2. スマホの再生「動画」のサイズずれ(不具合修正)

**根本原因を特定して修正しました。スマホ固有の描画バグでした。**

- 動画フレームのオフスクリーン描画で `renderer.setViewport(0,0,w,h)` を呼んでいましたが、three.jsはこの座標を**devicePixelRatio倍**して適用します。スマホ(dpr=2〜3)では描画領域が仮想的に2〜3倍になり、動画の各コマが「**本来の画の左下隅を2〜3倍に拡大した絵**」になっていました(プレビュー枠と動画の中身がずれて見えた正体)。PCはdpr=1のため無害で、スマホでのみ顕在化していました。
- 通常パス・パノラマパスの両方を修正しました。**この関数はファイル出力(静止画/動画)と共用**なので、スマホやRetinaディスプレイでの静止画/動画書き出しの同じ崩れも一緒に直っています。
- あわせて録画解像度を偶数に丸めるようにしました(奇数解像度はスマホのH.264ハードウェアエンコーダで失敗・色帯が出る既知問題があるため)。
- 検証: ヘッドレスブラウザで dpr=3 のフレーム生成が旧コードでは**87.6%のピクセルが崩れていた**のに対し、修正後は dpr=1 と実質一致(平均差0.12階調)。dpr=1の出力は旧コードと完全一致(デグレなし)を確認しました。

#### 3. 粗探索間隔の確認と高速化(前提の訂正+より良い代替を実装)

**まず前提の訂正です。辻メッシュ検索も「1分間隔+前後60秒の1秒リファイン」で、通常の辻検索と同一でした。**「前後120秒」という数字は、メッシュの行選択後の表示用精細化(±120秒スキャン+放物線補間で0.01秒化)にだけ存在し、365日走査の間隔ではありません。

そのうえで、ご要望の本質は「待ち時間の短縮」と理解し、**2分間隔化ではなく「安全スキップ」を実装しました**。

- **2分間隔化を採らなかった理由**: 検出は「粗標本が許容範囲に入るか」の判定なので、間隔を2分に広げると、許容範囲の通過時間が2分未満のケース(小さい許容範囲の設定時)で**その日のヒットを丸ごと見逃す**リスクが2倍になります。
- **安全スキップ**: 天体の天球上の移動速度には上限(日周運動15.04°/h+月の固有運動等 → 安全値0.35°/分)があるため、「検索中心まで角距離D°」の時点から**(D−許容半径)/0.35分の間は絶対に許容範囲に入れない**ことが数学的に保証されます。この分の標本を飛ばします。**飛ばすのは必ず範囲外の標本だけなので、検索結果は従来と1ビットも変わりません**。
- 実測(365日検索、新旧ワーカーの結果完全一致を確認済み): 月±15°で**5.8倍**、太陽±1°で**41.6倍**、月±0.5°で**27倍**、固定天体±15°で**2.8倍**の高速化。ご要望の「半分」を大きく上回りました。許容範囲が広いほど倍率は下がります(範囲内は全部評価するため)。
- 辻メッシュ検索にも同じ考え方(視高度帯ベースのスキップ)を入れ、**約10倍**(結果は完全一致)。My辻検索の一括計算も同じワーカーなので同様に速くなります。

#### 4. 精度角距離と天体視高度差の関係(質問回答)

**どちらの値も正しく、「参照点が違う」のが逆転の正体です。**

- **球面角距離** = 天球上の2点の最短角度で、`θ = acos(sin alt₁·sin alt₂ + cos alt₁·cos alt₂·cos Δaz)`。微小角では「√((方位差×cos視高度)² + 高度差²)」、つまり方位差と高度差の合成です。
- **精度角距離**は「**オフセット適用後の検索中心**(検索中心「線」のときは線分への最近点)」との球面角距離、**天体視高度差**は「**オフセット適用前の基準点(目的点)**の視高度」との差です。評価時刻はどちらも同じ辻時刻です。
- 例えば辻オフセット視高度を+0.5°に設定していると、辻時刻に天体はオフセット点のほぼ真上に来るので精度角距離≈0°ですが、天体視高度差は≈+0.5°になります。**オフセット0の「点」モードなら、常に 精度角距離 ≧ |天体視高度差| となり直感どおり**です(数学的に保証されます)。逆転が見えている検索は、オフセット(特に視高度)か検索中心「線」をお使いのはずです。
- 天体視高度差は「目的点そのものとの上下関係(パール/ダイヤ構図)」を示す参考列としての意味があるので、定義は変えていません。ヘルプ(辻検索の項)とデッサン03にこの関係の注記を追記しました。
- 補足: 天体方位角差はcos(視高度)の縮み補正がない生の方位角差なので、こちらはオフセット0でも精度角距離より大きく表示されることがあります。また辻メッシュでは表示値が画素座標系補正前の生値のため、最大約0.03°の微小な例外があり得ます。

#### 5. 標高グラフの除外範囲の初期値(質問回答)

**推奨は10mです(今回はまだ変更していません。ご指示があれば変更します)。**

- 「10mくらいあった方が良い」の正体は、過去の槍ヶ岳検証で確定した「**目的点自身のDEM画素による自己遮蔽**」です。除外0mだと、目的点の高さ0で上り視線の構図では、目的点の約2m手前のサンプル(=目的点自身の画素の標高)が可視直線をわずかに上回り、構造的にほぼ必ずNGになります。
- 必要最小値の理論値は、目的点が画素の隅にある最悪ケースで「画素の対角」= z15(DEM5系)で約5.5m、DEM5系が無くz14(DEM10B)フォールバックとなる地域で約11m。鋭峰の平滑化写り込み(隣接1画素)も吸収するなら+1画素。過去の実検証でも「除外10mで槍ヶ岳OK・お鉢の縁(280m)はNGのまま」が確認済みで、**10mは実在遮蔽をほぼ無視しない安全な値**です。DEM10Bのみの地域まで最悪ケースを覆うなら15mですが、まずは10mで十分と考えます。
- 注意: 初期値を変えても、**既にお使いの端末はlocalStorageに0が保存されているため変わりません**(新しい端末・リセット後に効きます)。既存端末にも一括適用するマイグレーションは、0を意図して使っている場合を壊すので、やるなら別途ご相談です。

#### 6. 宙の窓の槍ヶ岳が丸い(確認+改善)

**確認結果: 最も細かいタイルは使われていませんでした。ただし主因は別にありました。**

- 宙の窓の地形は dem_png(DEM10B相当、最大z14≒画素7.7m)のみを使用し、辻メッシュで使っているz15のDEM5A/B/C(5mメッシュ)は**未使用**です。さらに適応ズームにより、視界範囲が16.7kmを超えるとz13(15.4m)以下に落ちていました。
- しかし計算すると**主因はメッシュの間引き**でした。地形は扇形を半径方向100分割(2乗ランプ)でサンプリングしており、目的の山はちょうど範囲の最遠端=最も疎な帯に来るため、頂点間隔が「範囲÷50」(範囲11kmで220m、30kmで600m)。**槍の穂先(幅約200m)が1〜3頂点で表現され、三角形補間で丸まっていました**。DEM画素(7.7m)には約28画素分の情報があるのに、メッシュが1/30に間引いていた形です。
- 今回の改善: ①半径方向を**100→200分割、2乗→1.5乗ランプ**に変更(最遠端の間隔が範囲÷50→**範囲÷133**。11kmで約82m)。②適応ズームを緩和し、**範囲33km強までz14を維持**(従来16.7km)。タイル取得数は増えますがワーカープール並列+進捗バーの範囲内です。
- さらに追求する場合の次の一手(今回は未実施): ③DEM読み出しのバイリニア補間(現在は最近傍1画素なので、山頂画素が頂点に当たらないと標高自体を取り逃す)、④z15のDEM5A導入(欠損域のフォールバック処理が必要で影響範囲が広め)。実際の見え方をご確認のうえ、まだ丸ければ③④に進みます。

#### 7. My観測点の追加CSV入力で末尾1桁違いが新規にならない(不具合修正)

**原因を特定して修正しました。丸め比較ではなく「doubleの分解能」の問題でした。**

- CSV出力は生値をそのまま書くため最大17有効桁になりますが、IEEE754倍精度の分解能は緯度35°帯で約7×10⁻¹⁵(≒16桁目)しかなく、**17桁目はdoubleが区別できない桁**です。この桁を編集しても`parseFloat`が**同じdouble値に丸め戻す**ため、重複判定(緯度・経度・標高・高さの厳密一致でスキップ)が「同一点」と判定していました。
- 修正: 重複判定の緯度/経度比較を「**CSVの生値文字列 vs 登録値の文字列表現**」の比較に変更しました。これで**生値の末尾1桁が違えば必ず新規観測点として追加**されます。未編集のエクスポートCSVを再入力した場合は従来どおり重複スキップされます(文字列が完全一致するため)。
- 注意点2つ: (a) 追加された新規点の**内部座標値は元の点と同じdouble**になります(17桁目はdoubleに格納できないため。表示・計算は同一になりますが、別の行として登録されます)。(b) 表記だけが違う同値(例: `35.12`と`35.1200`)は「別の点」扱いになります(生値どおりの判定を優先した仕様です)。

#### 検証結果(ヘッドレスブラウザ、全32チェックPASS)

①辻検索ワーカー新旧の結果完全一致×5ケース(月/太陽/固定天体、点/線、±0.5°〜±15°、365日)+速度2.8〜41.6倍 ②辻メッシュワーカー新旧の結果完全一致×2ケース ③Myセット保存のコメント温存(4行/1行/0行/3行の混在シート・新規シートの既定文言・任意行数コメント+セル内改行の読込) ④追加CSV入力(同一生値→スキップ、末尾1桁違い→追加) ⑤フレーム生成のdpr1/dpr3一致(通常+パノラマ。旧コードのdpr3は87.6%崩れ→修正後0.34%未満)+dpr1のデグレなし ⑥適応ズームとメッシュ密度の変更値。ページエラーなし。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show fd4238b
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-17)
Claudeさん、いつもありがとうございます。
感謝です。

1. 
> #### 1. Myセット×スプレッドシートのコメント行の扱い(確認+修正)
ありがとうございます。
確認しました。
デッサン08に「先頭から連続する#行がコメント行になります。」を追加しました。
実装を願いいたします。

2. 
> #### 2. スマホの再生「動画」のサイズずれ(不具合修正)
ありがとうございます。
確認しました。
スマホで確認した場合の類似の現象がないかどうかの調査をお願いいたします。

3. 
> #### 3. 粗探索間隔の確認と高速化(前提の訂正+より良い代替を実装)
ありがとうございます。
確認しました。
辻検索が体感で、めっちゃ早くなりましたね🤭

4. 
> #### 4. 精度角距離と天体視高度差の関係(質問回答)
ありがとうございます。
確認しました。
多分、オフセット0だったので、補足の現象だったようです。
でも、ユーザは、気になるかもしれません。
ちょっと、考えます。

5. 
> #### 5. 標高グラフの除外範囲の初期値(質問回答)
ありがとうございます。
確認しました。
最悪のケースを考慮して、15mにしようと思います。
修正をお願いいたします。

6. 
> #### 6. 宙の窓の槍ヶ岳が丸い(確認+改善)
ありがとうございます。
確認しました。
私は、もっとリアルに描画をしていただきたいので、焦点距離も考慮して、もっとリアルに描画をできないでしょうか。
結局、相手距離が離れていても、カメラのレンズの焦点距離によって、ズームされるので、もっとリアルに描画をしていただきたいです。
何卒、よろしくお願い申し上げます。

7. 
> #### 7. My観測点の追加CSV入力で末尾1桁違いが新規にならない(不具合修正)
ありがとうございます。
確認しました。
こちらの方が、内部的には同じでも、ユーザは、異なる値を入力したのに変更されない疑問を解消できると思います。

8. 
今までずっとそうだったのですが、これは、スマホならではの問題で、治らないものだと思っていたのですが、Claudeさんに相談していなかったので、確認してみようと思いました。
宙の辻メニューの一番下のプライバシーポリシーと利用規約のあたりのことなのですが、この最下部のバージョン番号以降は、スマホで見ると、メニューをググッと引き上げないと画面の下に隠れて、よく見えないのです。
これは改善されない問題なのでしょうか。

再び、よろしくお願いいたします。

### 回答 (2026-07-17)

5項目に対応しました(Version 1.20.14)。

#### 1. 既定コメントの3行化(デッサン08準拠)

実装しました。新規作成シート(またはコメント行を全て削除したシートの再保存)の既定コメントが、デッサン08どおりの3行(「#このように…」「これもコメント行です。複数行、記述できます。」「先頭から連続する#行がコメント行になります。」)になります。既存シートの先頭#行の温存は前回のまま有効です。

#### 2. スマホの類似現象の調査(2件発見→修正)

three.jsの描画パス(setViewport/setScissor/レンダーターゲット系)を全数点検し、dpr二重スケール系の残りは**ありませんでした**(画面描画はCSS px渡しで正しく、全天キューブ・フィッシュアイRTも正しい方式)。一方、**別種のスマホ類似現象を2件見つけて修正しました**:

- **標高グラフのcanvasがdpr未対応**: 描画バッファをCSS解像度で確保していたため、スマホ(dpr=2〜3)ではグラフ・文字が2〜3倍に引き伸ばされて**ぼやけて**いました。バッファをデバイス解像度で確保する方式に修正し、スマホでもくっきり描画されます(ヘルプの訪問者グラフも同様に修正)。
- **メニュー最下部が隠れる問題**: ご質問8がまさにこの類似現象(スマホのビューポート問題)でした。詳細は8の回答をご覧ください。

そのほか、動画再生のplaysInline指定、宙の窓のドラッグ/2本指操作の座標系、全天儀のレンダラ設定は点検の結果いずれも問題ありませんでした。

#### 3. 辻検索の高速化

体感いただけて何よりです🤗 結果は1ビットも変わっていないので、安心してお使いください。

#### 4. 精度角距離と天体視高度差

承知しました。もし「ユーザーが混乱しない見せ方」を検討される場合の材料として: (a) 現状のヘルプ注記のまま様子見、(b) 列名を「目的点視高度差」に改名して基準を明示、(c) オフセット使用時のみ「検索中心視高度差」列を別途追加、などが考えられます。方針が決まりましたらお申し付けください。

#### 5. 除外範囲の初期値15m

実装しました。初期値・復元時の既定値とも15mです(デッサン02にも根拠つきで追記)。ご留意点: 既にお使いの端末には**保存値が優先**されるため、保存値が0の端末は0のままです(リセットまたは手入力で15にできます)。検証で「保存値0が15に上書きされないこと」も確認済みです。

#### 6. 宙の窓のさらなるリアル化(焦点距離を考慮)

おっしゃるとおり、望遠レンズでは遠くの山がズームで拡大されるため、距離だけでなく**画角(焦点距離)も分解能に効きます**。3点実装しました:

- **焦点距離を考慮した適応ズーム**: DEMタイルのズーム選択を「視界範囲÷12 × (水平画角÷54.4°)」基準に変更(35mm換算の標準画角54.4°を基準に、望遠ほど細かいタイルを維持。下限1/8)。例えば200mmの望遠なら、視界範囲200km超まで最詳細のz14(7.7m画素)を使います(従来は範囲16.7km超でz13以下に低下)。望遠では扇形が細いため、タイル数の増加は限定的です。
- **望遠時のメッシュ細分化**: 水平画角20°以下(おおむね90mm超の望遠)では半径方向を300分割に増強(最遠端の頂点間隔が範囲÷200。範囲11kmで約55m)。
- **バイリニア補間**: DEMの標高読み出しを最近傍1画素→周囲4画素の補間に変更。山頂の画素がサンプル点に当たらず標高を取り逃して低く出る問題が緩和され、稜線の階段状ノイズも減ります。

これで「レンズでズームしても破綻しにくい」描画になります。残る上限はDEM10B(z14=7.7m画素)というデータ自体の分解能で、さらに上を目指す場合は辻メッシュで使っているz15のDEM5A(5mメッシュ)の導入が次の一手です(欠損地域のフォールバック処理が必要なため、今回の結果をご覧いただいてから判断がよいと思います)。

#### 7. CSV重複判定

ありがとうございます。そのとおりで、「入力したのに増えない」という疑問の解消を優先した仕様です。

#### 8. スマホでメニュー最下部(バージョン・ポリシーリンク)が隠れる問題

**直せる問題でした。修正済みです。**

- 原因: メニューの高さ上限を `90vh` で指定していましたが、スマホブラウザの `vh` は「**アドレスバーが隠れた時の最大画面**」基準です。アドレスバーが表示されている間は実際の見える高さがそれより小さいため、メニュー下端が画面外にはみ出し、「ググッと引き上げて」アドレスバーを隠さないと最下部が見えませんでした。
- 修正: 高さ上限を `100dvh`(**その時々の実際に見えている高さ**に追随する動的ビューポート単位)基準+iPhoneのホームバー分の余白(safe-area)を差し引く形に変更しました。アドレスバーが出ていてもメニュー全体が画面内に収まり、メニュー内スクロールで最下部まで見えます(古いブラウザには従来の90vhでフォールバック)。

#### 検証結果(ヘッドレスブラウザ、全29チェックPASS)

①新規シートの既定コメント3行+カスタム5行温存の回帰 ②除外範囲: 新規=15m・保存値0の尊重(リロード後も0のまま) ③メニューmax-height=100dvh−20px+下端が画面内 ④標高グラフcanvasのバッファ=CSS×dpr(dpr=3) ⑤バイリニア補間の単体(画素中心の厳密値・中間点の平均・端クランプ・無効画素フォールバック)+ワーカー実機(合成タイル) ⑥焦点距離考慮ズーム(範囲50km: 標準z13/望遠z14、範囲200km: 標準z11/望遠z14)+メッシュ頂点数(望遠141×301/標準141×201) ⑦フレーム生成dpr1/dpr3一致の回帰 ⑧動画オーバーレイ7シナリオ(メニュー開閉・最大化・リサイズ・パノラマ)の枠一致。ページエラーなし。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show c0480ce
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-17)
Claudeさん、ありがとうございます。
助かります。

1. 
> #### 1. 既定コメントの3行化(デッサン08準拠)
ありがとうございます。
1.の確認中に問題に気がつきました。
発生している現象は、
1. Googleドライブにログイン
2. 既定のセットでスプレッドシートと同期を完了させる。
3. スプレッドシートのコメント行3行を削除する。スプレッドシートを保存する。
4. 既定のセットの状態アイコン👍をクリックする。
5. 同期が取れています旨の表示が出る。
正しくは、4.で、スプレッドシートの更新日時を読みに行って、🕛を表示し、5.でスプレッドシートの更新日時に[New]がつくことだと思います。
修正をお願いいたします。

2. 
> #### 2. スマホの類似現象の調査(2件発見→修正)
類似の事象をありがとうございます。
助かります。

4. 
> #### 4. 精度角距離と天体視高度差
提案ありがとうございます。
では、オフセットを使用していなくても、常に列名を「検索中心方位角差」「検索中心視高度差」に変更しましょう。
関連箇所も修正してください。(CSV出力のヘッダ名など。)

5. 
> #### 5. 除外範囲の初期値15m
修正ありがとうございます。
確認しました。

6. 
> #### 6. 宙の窓のさらなるリアル化(焦点距離を考慮)
修正ありがとうございます。
さらなるリアル化を推し進めたいので、「次の一手」をお願いいたします。

8. 
> #### 8. スマホでメニュー最下部(バージョン・ポリシーリンク)が隠れる問題
修正ありがとうございます。
よかったです。
嬉しいです。
でも、スマホのChromブラウザは、キャッシュが効いているのか、確認できませんでした😭
時間が経ってから確認しますね。

9. 
忘れていたのですが、ゴールデンアワーGHとブルーアワーBHを追加した時に、My辻検索のFile取得ボタンでのFile出力項目のカラム列は、修正をしなかったのでしょうか。
こちらも、修正対象だと思います。
私も、忘れていたのですが、デッサン09が修正されていないようです。
実装も確認してください。

まだこれから修正をかけたいところがありますが、何卒、よろしくお願いいたします。

### 回答 (2026-07-17)

4項目に対応しました(Version 1.20.15)。

#### 1. 状態アイコン👍押下時にシートを再確認しない問題(不具合修正)

ご指摘のとおりでした。従来は👍クリック時に**画面の状態(前回チェック結果)をそのまま信じて**「同期が取れています」と表示しており、シート側の外部編集を見逃していました。修正後の動作:

- 状態アイコンを押すと、**必ずその場でシートの更新日時を読みに行きます(🕛の時計アニメ表示)**。
- 差がなければ従来どおり「同期が取れています」。
- **差が新たに見つかった場合**は、アイコンが👎になり、行の「更新:」欄に**「シート: (シート側の更新日時)[New]」を赤字で併記**します。この時は誤上書き防止のため保存/読込は実行せず、「もう一度押すと保存/読込ラジオに従って更新します」とご案内します。
- すでに👎表示の行を押した場合は、再確認のうえ従来どおり保存/読込ラジオに従って即実行します。
- ログイン直後の一括チェックでもシート側更新日時を記録するようにしたため、👎の行には常に「シート: …[New]」が表示されます。

#### 4. 「検索中心方位角差」「検索中心視高度差」への変更

列名の変更と合わせて、**値の基準も「検索中心」に変更しました**(名前と中身を一致させるため)。

- **新しい定義**: 辻時刻の天体位置と「検索中心」との差。検索中心=オフセット適用後の点(検索中心オプション「線」のときは基準点→オフセット点の線分への最近点=**精度角距離と同じ参照点**)。
- これにより、**常に 精度角距離 ≧ |検索中心視高度差| が成り立ち**、先日の「視高度差の方が大きい」という違和感が起こらなくなります(方位角差だけはcos(視高度)補正のない生の差なので、高高度では角距離より大きく見えることがあります。ヘルプに明記)。
- **オフセット0の場合は従来と同じ値**です(検索中心=目的点になるため)。
- 修正箇所: 辻検索・辻メッシュ検索・My辻検索の結果リスト(見出し・値・ソートメニュー)、My辻検索File出力のCSVヘッダと値、ヘルプ、デッサン03/04/09。My辻検索は行ごとに独立した検索条件(オフセット・点/線)を使って行別に計算します。

#### 6. 宙の窓のさらなるリアル化「次の一手」= z15 DEM5A導入

実装しました。**最詳細のDEM5A(5mメッシュ・z15画素≒3.85m)を宙の窓の地形に導入**しています。

- **取得チェーン**: DEM5A → DEM5B → DEM5C(いずれもz15)→ 欠損画素は**z14(DEM10B)の親タイルへ自動フォールバック**。DEM5系の未整備地域や水面の無効画素があっても穴になりません(辻メッシュと同じ考え方)。
- **適応ズームがz15まで対応**: 標準画角なら視界範囲16.7kmまで、望遠なら焦点距離に応じてさらに遠くまで(例: 290mm級なら範囲134kmまで)z15を使います。前回のバイリニア補間・望遠メッシュ細分化と合わせて、槍ヶ岳の穂先クラスの造形がDEMの限界解像度で描かれるようになりました。
- 留意点: タイル取得数が増えるため、初回表示の取得時間は少し延びます(ワーカープール並列+進捗バーの範囲内。取得済みタイルはワーカー内キャッシュで再利用)。実際の見え方をぜひご確認ください。

#### 8. スマホのキャッシュについて

Chromeのキャッシュ(+GitHub PagesのCDNキャッシュ)が効いていると、反映まで時間がかかることがあります。フッターのバージョン番号が**v1.20.14以上**になっていれば新しいコードです。急ぐ場合は、Chromeのメニュー→履歴→閲覧履歴データの削除→「キャッシュされた画像とファイル」だけを削除、が確実です。ゆっくりで大丈夫です、時間が経ってからのご確認をお待ちしています。

#### 9. My辻検索File出力のGH/BH列(修正漏れの解消+デッサン09更新)

ご指摘のとおり**修正漏れでした**。2点直しました:

- **GH/BH境界時刻の4列を追加**し、薄明系の列名も画面表示と同じ「/BH」付きに変更しました。時系列順に: 天文薄明[始] → 航海薄明[始] → 夜明 → **常用薄明/BH[始]** → **BH[終]/GH[始]** → 日の出 → **GH[終]** → **GH[始]** → 日の入 → **GH[終]/BH[始]** → **常用薄明/BH[終]** → 日暮 → 航海薄明[終] → 天文薄明[終]。
- ついでにもう1つ修正漏れを発見: デッサン09のFile出力項目には「**時間帯**」列(GH/BH入りの16区分)があるのに、実装のCSVには出力されていませんでした。**時間帯列も追加**しました(辻時刻の直後)。
- デッサン09のFile出力項目リストも上記の列構成に更新済みです(検索中心方位角差/視高度差への改名含む)。

#### 検証結果(ヘッドレスブラウザ、全29チェックPASS)

①検索中心参照点の単体(点・線の射影/クランプ・「精度角距離≧|視高度差|」の200サンプル保証) ②My辻検索CSVのヘッダ列数=データ列数(64列)・時間帯/GH/BH列の値・検索中心差の値 ③状態アイコン再チェック(保存→👍→外部編集→1回目クリックで👎+[New]+実行なし→2回目クリックで読込実行→👍・同期済みクリックで同期メッセージ) ④z15チェーン(DEM5Aヒット/5A無効→5B/全無効→z14親タイル・画素座標変換の厳密一致・適応ズームz15/z14切替) ⑤地形メッシュ回帰(頂点数・フレーム生成)。ページエラーなし。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 0b1f5f3
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-18)
Claudeさん、ありがとうございます！
感謝です！
色々と、類似バグや関連項目を修正していただき、誠にありがとうございます🙇

1. 
> #### 1. 状態アイコン👍押下時にシートを再確認しない問題(不具合修正)
ありがとうございます。
確認しました。

4. 
> #### 4. 「検索中心方位角差」「検索中心視高度差」への変更
ありがとうございます。
確認しました。

6. 
> #### 6. 宙の窓のさらなるリアル化「次の一手」= z15 DEM5A導入
ありがとうございます。
確認しました。
槍ヶ岳の小槍まで描画されるようになりましたね。
感動です。
そこで、気がついたのですが、槍ヶ岳の最高点と画面中心や目的点(+)が少しずれているような気がします。
いかがでしょうか。

8. 
> #### 8. スマホのキャッシュについて
ありがとうございます。
確認しました。
大丈夫です。

9. 
> #### 9. My辻検索File出力のGH/BH列(修正漏れの解消+デッサン09更新)
ありがとうございます。
確認しました。
問題ありません。

10. 
スマホで、確認をしていて、一つ気がついたのですが、宙の窓で、画面表示項目の天体軌跡をオフにした場合は、天体中心にある(+)記号もオフにできないでしょうか。その方が、より実際の見た目を確認しやすいです。
いかがでしょうか。

引き続き、よろしくお願いいたします。

### 回答 (2026-07-18)

2項目に対応しました(Version 1.20.16)。小槍まで見えて感動していただけて、こちらも嬉しいです🙌

#### 6. 槍ヶ岳の最高点と画面中心・目的点(+)のズレ(原因2つを特定して修正)

ご指摘のとおり、ずれていました。調べたところ原因は2つありました。

**原因1(主因): 高さ計算のモデル不一致(約0.1°のずれ)**

- 目的点マーカー(+)の位置(基準視高度)は「**観測点と目的点それぞれの緯度の局所地球半径を使う厳密な三角形解**」で計算しています(辻検索と同じ、実績のある高精度モデル)。
- 一方、地形メッシュの頂点の高さは「**単一半径の放物線近似**(標高差−距離²/2R)」で計算していました。
- 局所地球半径は緯度で変わる(南北3kmで約10m)ため、南北方向の目的点では**山頂の描画が(+)より約0.1°高く/低く**なっていました。地形の高さ計算を基準視高度と**同じ厳密式に統一**して解消しました。

**原因2(副因): 山頂が格子の「間」に落ちる**

- 地形は扇形の格子でサンプリングするため、山頂(目的点)が格子点の間に落ちると、三角形補間で山頂がわずかに低く・横にずれて描かれます(格子間隔の半分まで=数十m)。
- **格子を目的点にスナップ**するようにしました: 目的点が視界内にある場合、方位の1列と距離の1環を目的点にぴったり合わせ、その頂点は球面近似ではなく**目的点の実座標(WGS84)のDEM値(getElevationと同じ最近傍参照)**を使います。目的点を動かすと格子も追従します。
- 検証では、合成の円錐山(頂上=目的点)で**最高点の方位・視高度が(+)と誤差0.0000°で一致**することを確認しました。

補足: 残るずれ要因は「登録されている目的点標高とDEM値の差」だけです。目的点(+)は位置情報メニューの目的点標高(+高さ)基準なので、実際の山頂標高と登録値がずれているとその分だけ縦にずれます(例: 槍ヶ岳の三角点3179.5m vs 最高点3180m)。気になる場合は目的点標高をご確認ください。

#### 10. 天体軌跡オフで天体中心の(+)も非表示に

実装しました。画面表示項目の「天体軌跡」をオフにすると、**天体中心の(+)十字も非表示**になります(通常表示・再生アニメーションの両方)。月・太陽・惑星の円盤や天体名称ラベル、目的点(+)・検索中心マーカーはそれぞれ従来どおり独立して表示されます。天の川は十字が唯一のマーカーですが、写真と環の表現はそのまま残るので、実際の見た目の確認がしやすくなります。

#### 検証結果(ヘッドレスブラウザ、全16チェックPASS)

①軌跡オン=十字3個表示/オフ=0個・目的点/検索中心マーカーは不変・再オンで復元 ②合成円錐山で最高点の方位=目的点方位(誤差0.00000°)・最高点視高度=基準視高度(誤差0.0000°)・スナップ頂点の座標が目的点の実座標と一致・頂点標高=山頂標高 ③ワーカーの最近傍参照(バイリニアと区別・チェーン経路でも有効) ④メッシュ頂点数・フレーム生成の回帰。ページエラーなし。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 22df7ca
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-18)
Claudeさん、ありがとうございます。
確認しました。

6. 
> #### 6. 槍ヶ岳の最高点と画面中心・目的点(+)のズレ(原因2つを特定して修正)
ありがとうございます。
そもそもなのですが、格子が粗いのではないでしょうか。格子をDEMタイルで得られる最小の粒度にしては、ダメなのでしょうか。600mmの望遠レンズで槍ヶ岳を参照するので、それぐらいの精度が必要だと思います。
いかがでしょうか。
あと、高移ボタンで参照するDEMタイルは、もっと高精度なDEMタイルでしょうか。
そこの確認もお願いいたします。

10. 
> #### 10. 天体軌跡オフで天体中心の(+)も非表示に
ありがとうございます。
確認しました。

### 回答 (2026-07-18)

2件に対応・回答しました(Version 1.20.17)。

#### 6. 格子をDEMタイルの最小粒度に(実装)

おっしゃるとおりでした。格子(頂点間隔)がDEM画素より粗いのがボトルネックだったので、**格子密度を「DEM画素サイズ」を目標に自動配分する方式**に変えました。

- **望遠(画角10°以下、およそ180mm超)**: 頂点予算を30万に増やし、距離方向の密度配分も遠方寄りに変更。**600mm・視界範囲5km級では、方位・距離とも頂点間隔がぴったりDEM1画素(z15≒3.85m)になります**(検証実測: 方位3.84m・距離3.85m)。つまり、DEMデータが持つ情報を取りこぼしなく描き切ります。
- 範囲が伸びると予算内で自動的に粗くなります(範囲11kmで約2画素相当、20kmで約3画素相当)。それでも山頂の1点は前回のスナップで常に正確です。
- **標準〜広角**: 予算20万頂点内で最密化。範囲11kmの最遠端で約18m間隔(従来82m)と、こちらも約4.5倍細かくなりました。
- 「全域を完全にDEM粒度」にしない理由は頂点数です。広角・範囲16kmを全部DEM粒度にすると約600万頂点となり、スマホでは描画・メモリとも現実的でないため、「望遠=ほぼ完全なDEM粒度/広角=予算内最密」という配分にしています。描画負荷は上がっているので(20〜30万頂点)、スマホでの体感速度に問題があれば予算値を調整します。

補足(理論限界): 600mm・5km先では、DEM1画素(3.85m)が画面上で約15pxに相当します。つまりこの距離ではDEMデータ自体が解像度の上限で、今回の変更でその上限まで描き切る状態になりました。これ以上は5mメッシュより細かい標高データが必要になります(国土地理院の1mメッシュDEM(LP由来)はタイル配信されていないため、現状はDEM5Aが実用上の最詳細です)。

#### 高移ボタンのDEMタイル(確認)

**はい、最高精度のタイルを使っています。** 高移(準最高地点移動)は、選択中の点を覆うタイルを**DEM5A→DEM5B→DEM5C(いずれもz15・5mメッシュ)→DEM10B(z14・10mメッシュ)の優先順**で選び、最初にデータがあったソースの256×256画素窓で局所最大(小ピーク)を探します。つまりDEM5Aが整備されている山岳地帯では、宙の窓(今回の改修後)と同じ最詳細5mメッシュ基準です。移動先の座標もそのタイルの画素中心なので、高移で選んだ点は宙の窓の山頂スナップと相性が良いです。

#### 検証結果(ヘッドレスブラウザ、全17チェックPASS)

①600mm・範囲5kmで頂点間隔=方位3.84m/距離3.85m(DEM1画素3.85mと一致・27.6万頂点) ②標準画角・範囲11kmで19.98万頂点・最遠端17.9m(予算内最密) ③山頂スナップ(方位・視高度とも誤差0.0000°)と軌跡オフ十字非表示の回帰 ④フレーム生成の回帰。ページエラーなし。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show fd3e39b
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-18)
Claudeさん、ありがとうございます。
上手くいきましたね。
心より感謝御礼を申し上げます。

11. 
宙の窓で、不具合なのか、調査をお願いいたします。
スマホで、書き出しをWebMですると、上手くいかないようです。
書き出したファイルをYouTubeでうまく再生できませんでした。
スマホのブラウザはChromeです。
調査をお願いいたします。

12. 
Myセットメニューでスプレッドシートと同期が取れている時に、Myセット毎の👍ボタンを押下した時の動作に認識の齟齬がありました。
同期が取れている場合は、👍ボタンを押下したら、スプレッドシートの更新時刻の確認だけして、その後の状態(👎か👍か❌)を表示するだけで、次回押下した時に保存をするような動作は不要な気がします。理由は、次回の動作は、保存か読み込みかは、ユーザが決めることなので、こちらで決めることはできないと思います。いかがでしょうか。

13. 
宙の窓のデッサン05を修正しました。実装をお願いいたします。
あと、項番の修正をよろしくお願いいたします。

14. 
バックアップインポートメニューのデッサン16を修正しました。
実装をお願いいたします。

15. 
todo.mdのClaudeさんの提案の「観測点側の可視判定の除外範囲」を考えたいです。
これからフォームを考えますが、初期値は、5mくらいでしょうか。

よろしくお願いいたします。

### 回答 (2026-07-18)

4項目を実装+1項目を回答しました(Version 1.20.18)。

#### 11. スマホのWebM書き出しがYouTubeで再生できない(原因特定+修正)

**原因を特定しました。「H.264入りWebM」という非標準構成が原因です。**

- これまでの「WebM」書き出しは、H.264コーデックをWebMコンテナに入れる構成を優先していました。しかし**WebM規格の映像コーデックはVP8/VP9/AV1のみ**で、「H.264入りWebM」はChromeが再生・録画できてしまうだけの独自の非標準構成です。YouTubeを含む外部サービスは規格どおりのWebMとして処理しようとして失敗します(スマホ・PCどちらのChromeでも同じ構成で書き出されるため、環境によらず起こります)。
- 修正: WebM書き出しを**VP9優先(非対応ブラウザはVP8)の標準WebM**に変更しました。ラジオボタンの表記も「:VP9(WebM)」に変更しています(デッサン05も更新)。H.264が必要な場合(X/Instagram等)は従来どおり「:H.264 8-bit(MP4)」をお使いください。
- これでYouTubeにそのままアップロードできるはずです。もしVP9でもまだ問題がある場合は、MediaRecorder出力に再生時間メタデータを後付けする対策(次の一手)がありますので、お知らせください。

#### 12. 👍押下時の動作(認識合わせ+修正)

おっしゃるとおりです。「保存か読込かはユーザーが決めること」というご指摘はもっともなので、**状態アイコンの押下は「シートの更新日時を再確認して状態(👍/👎/❌)を表示するだけ」に変更**しました。

- 押下→🕛→再確認→👍(同期メッセージ表示)/👎(「シート: 日時[New]」併記)/❌。**保存/読込は一切実行しません**。
- 保存/読込の実行は、従来どおり「チェックボックス+保存/読込ラジオ+更新ボタン(または0段目の一括アイコン)」で行います。ツールチップもその旨に更新しました。
- 例外は未紐付け(😢)/シート消失時のみで、この場合は従来どおり「作成して保存しますか?」の確認を出します。

#### 13. デッサン05の実装+項番修正

実装しました。プレビュー内コントロールメニューに以下を追加しています(いずれも宙の窓メニューと双方向連動):

- **カメラ位置**(横/縦ラジオ)
- **焦点距離(選択)リストボックス**(54種の焦点距離)
- **焦点距離スライダー**を1〜1000mm・ステップ1に変更(1000mm超はテキスト/リストで指定。宙の窓メニューのスライダーと同仕様)
- **薄明ジャンプ**(夜明7種+日暮7種のグリッド。薄明メニューと同じ構成・時刻表示つき・選択状態も薄明メニューと連動)

デッサン05の項番も振り直しました(コントロールメニュー節を3段目〜39段目・項番3〜53で一貫)。1点だけデッサンと違えた箇所があります: スライダーの制約「0〜1000」は、焦点距離0mmが存在しないため**1〜1000**で実装し、デッサンもそのように修正しました。

#### 14. デッサン16の実装

実装しました。①バックアップ/インポートメニューの初期状態を「閉じている」(▼)に変更 ②Googleドライブ最終更新日時の表示を「2026年02月09日(月) 19:32:16」形式(年月日+曜日)に変更しました(同期ダイアログの日時は元からこの形式で、表示が揃いました)。

#### 15. 観測点側の除外範囲の初期値(相談回答)

理屈は目的点側と同じ「観測点自身のDEM画素による自己遮蔽」対策なので、**必要最小値=観測点が画素の隅にある最悪ケースの画素対角**です: z15(DEM5系)で約5.5m、z14(DEM10B)フォールバック地域で約11m。

- **5mだと、z15整備地域でも最悪ケース(画素の隅)をわずかに覆えません**(5.5mに届かない)。効き漏れをなくすなら**最小6m**です。
- 目的点側(15m)より小さくてよいのは確かです(観測点側は鋭峰の写り込み余裕が不要なため)。一方で、観測点側は「足元の実在の遮蔽(すぐ手前の岩や縁)」を無視しすぎない方が良いので、大きすぎも避けたいところです。
- **推奨は10m**(z15の最悪ケース5.5m+丸め余裕を確実に覆い、z14地域も概ねカバー。足元の実在遮蔽を無視しすぎない)です。5mに近い値を優先されるなら6mでも実用になります。フォーム案(基本オプションの除外範囲の隣に「観測点側除外範囲(半径,m)」を並べる等)ができましたら、実装いたします。

#### 検証結果(ヘッドレスブラウザ、全18チェックPASS)

①WebM書き出し=VP9選択・H.264入りWebMの全排除・MP4優先の維持 ②状態アイコン: 1回目/2回目とも表示のみ(データ不変)・更新ボタンで読込実行・同期時メッセージ ③ctrlメニュー: リスト54種+600mm選択の3コントロール連動・スライダー1〜1000・カメラ位置の双方向連動 ④薄明ジャンプ14種・時刻ミラー表示・ジャンプ動作+薄明メニューとの選択連動 ⑤バックアップ初期閉+日時形式。スクリーンショットでctrlメニューの表示崩れなしを確認。ページエラーなし。


-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
git show 37c07c0
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------


## 依頼 (2026-07-18)
Claudeさん、いつもありがとうございます。
確認しました。

11. 
> #### 11. スマホのWebM書き出しがYouTubeで再生できない(原因特定+修正)
ありがとうございます。
まだうまく動作しないようです。
次の一手をよろしくお願いいたします。

12. 
> #### 12. 👍押下時の動作(認識合わせ+修正)
ありがとうございます。
気がついたのですが、Myセット毎の状態アイコンが😢の時に、Myセットメニュータイトル横の👎アイコンを押下すると、😢アイコンが自動的にスプレッドシートファイルが作成されてしまいます。ここは、ユーザが判断する箇所なので、そのままにしていただきたいです。他にも類似箇所がないでしょうか。更新ボタンを押下した場合など、大丈夫でしょうか。こちらとしては、こちらのおすすめを提供したいのではなくて、ユーザの判断に基づく自動更新です。お間違えのなきようにお願いいたします。

13. 
> #### 13. デッサン05の実装+項番修正
ありがとうございます。
確認しました。
とても使いやすくなりました。

14. 
> #### 14. デッサン16の実装
ありがとうございます。
確認しました。
他にも日付書式が揃っていないところは、ありませんでしょうか。
洗い出しをして、確認をお願いいたします。

15. 
> #### 15. 観測点側の除外範囲の初期値(相談回答)
回答ありがとうございます。
デッサン02に目的点側と観測点側のフォームを書きました。
実装をお願いいたします。
また、デッサン02に観測点側の初期値の理由の記載をお願いいたします。

### 回答 (2026-07-18)

4項目に対応しました(Version 1.20.19)。

#### 11. WebMの次の一手(再生時間メタデータの後付け)

**WebMに再生時間(Duration)を後付けする処理を実装しました。** MediaRecorderのWebMはストリーミング形式で長さ情報を持たないことがあり、プレーヤーやサービスによっては「長さ不明・シーク不可・処理失敗」の原因になります。書き出し後にファイル(EBML)を解析し、Duration要素が無ければ挿入します(既にあるファイルはそのまま。壊れる余地のない安全側の実装です)。

そのうえで、1点確認をお願いします。**お手元のテストがv1.20.18(VP9化)以降のコードで行われたか**をフッターのバージョンでご確認ください(v1.20.19以降でお試しいただくのが確実です)。もしVP9+Duration付きでもYouTubeでダメな場合は、(a) アップロード自体が失敗する/(b) 処理は済むが再生がおかしい、のどちらか、と(c) その動画をスマホのローカルでは再生できるか、を教えてください。切り分けに直結します。なお、**YouTube用途でしたらMP4(H.264)での書き出しが最も確実**です(YouTubeの推奨コンテナはMP4のため)。

#### 12. 一括更新が😢のシートを勝手に作成する問題(修正+類似監査)

ご指摘のとおりでした。修正と監査を行いました。

- **一括更新(0段目アイコン・更新ボタン)は、シート未作成(😢)の行をスキップ**するようにしました。確認メッセージと完了メッセージに「シート未作成のn件は対象外/スキップ」と表示します。作成は従来どおり各行の😢アイコンの押下(確認つき)でのみ行われます。
- さらに類似の穴を1つ発見して塞ぎました: **一括更新の実行中にシートが外部削除されていた場合**、従来は黙って再作成していました。修正後は再作成せず😢(missing)にして「失敗」として数えます。
- **監査結果**: その他の自動作成・自動実行は以下のとおりで、いずれも「ユーザーの明示操作+確認メッセージ」または「ユーザーの明示操作の当然の帰結」のみでした — ①行の😢押下(確認つき作成) ②ヘッダのGoogleアイコン😢押下(app.jsonの確認つき作成) ③追加(Picker)後の読込(確認つき) ④オフラインチェックON時の内容取得(読込のみ・作成なし) ⑤切り替え表示時の読込(スイッチ操作の帰結・作成なし)。ご方針(おすすめ提示ではなく、ユーザーの判断に基づく自動更新)に沿っています。

#### 14. 日付書式の洗い出し(監査結果)

全表示箇所を洗い出しました。結論: **用途ごとに3系統あり、各デッサンの指定どおりに揃っています**。

| 書式 | 使用箇所 | 根拠 |
|---|---|---|
| 2026年02月09日(月) 19:32:16 | Myセット行の「更新:」「シート:」・同期ダイアログ・バックアップのドライブ最終更新日時 | デッサン16(前回統一) |
| 2026年02月09日 +「(月)」別列 | 辻検索/辻メッシュ/My辻検索の結果リスト・辻時刻コントロールの辻日付(こちらは(曜)つき) | デッサン03/04/09 |
| 2026/07/04 05:18:30 | 撮影開始日時・撮影終了日時・再生中の進捗表示・出力画像のクレジット | デッサン05(例示どおり) |
| 2026/07/04 +「曜日」別列 | My辻検索File出力CSV | デッサン09(Excel加工優先) |

- 唯一の例外は**URL取得の「日時固定(2026年07月18日21:00:00)」ラベル**で、曜日なし・スペース詰めです(ラジオラベルの幅の都合)。実害はないためそのままにしていますが、揃えたい場合はお申し付けください。
- ファイル名(soranotsuji-…-YYYYMMDD-HHMMSS)はファイル名制約のための独立形式です。

#### 15. 除外範囲フォームの実装(デッサン02)

実装しました。

- **「:除外範囲」チェックボックス(初期値オン)**: オフにすると目的点側・観測点側とも無効(=一切無視しない)になり、入力欄もグレーアウトします(値は保持)。
- **「目的点側(半径,m):」初期値15 / 「観測点側(半径,m):」初期値10**。可視判定コア(標高グラフ・辻検索/My辻検索・辻メッシュの標高オプション共通)と並列ワーカーの両方に観測点側除外を実装しました。判定は「遮蔽サンプルの観測点からの距離 ≦ 観測点側半径なら無視」で、目的点側と対称です。
- デッサン02に観測点側の初期値の理由を記載しました。1点補足: デッサンでは18.19.が同じ14段目ですが、ラベルが長く360px幅に2組収まらないため、**目的点側と観測点側を上下2行**で実装しています(表示上の分割のみ)。
- 運用上の注意: 既にお使いの端末は目的点側の保存値が引き継がれ、観測点側は初期値10m・チェックはオンで始まります。**DEM5系が無いz14フォールバック地域では、観測点画素の量子化により観測点側10mで覆いきれないケース**があり得ます(その場合は15m程度に上げてください。デッサンにも記載)。

#### 検証結果(ヘッドレスブラウザ、全13チェックPASS)

①フォーム: 初期値(オン/15/10)・チェックオフで実効0/0+入力無効化+値保持・入力編集の反映 ②可視判定コア単体: 観測点から5mのNGを観測点側10mで無視/0で遮蔽・目的点から8mのNGを目的点側15mで無視/0で遮蔽・中間のNGは常に遮蔽 ③E2E(computePathVisibility): チェックオンで可視/オフでNG ④WebM: ストリーミング形式へのDuration挿入(2500msが正しく書き込まれる)・確定化済みファイルはノーオップで再生可能 ⑤一括更新: シート作成0件(😢スキップ+外部削除の再作成なし)・スキップ/失敗の件数表示。ページエラーなし。
