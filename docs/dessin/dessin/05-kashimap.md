# デッサン（dessin）
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------
## 可視マップメニュー
-------------------------------------------------------------------------------
-------------------------------------------------------------------------------

(第7版: 2026-09-21 第147ラウンド[第148: 動的モードの入口を「My目的点で計算」に=依頼者修正。段1の実装開始]。
名山対応表の検品完了(依頼者)を受けて「実装の段取り」の節を追加し、実装に入る版。
第6版[2026-09-21 第145。第146で「地図の縞=重複数がいちばん多い縞島」を依頼者が承認]=依頼者のQ5修正を反映: 縞島は「画面の範囲」ではなく「選んだ山の島全体」から作り、
各山の島(重複数1)と縞島(重複数2以上・所属山は複数)を**一つの島リスト**に「重複数」つきで並べる。行を選ぶと地図がその島へ。
第5版[2026-09-20 第144]=第143の検品(Q1〜Q19・P1〜P14)への依頼者回答を反映: ①静的+動的 ②対象は地理院1003山の全体
③島の最小サイズは1画素 ④山頂マーカー/全展望マーカー ⑤選べる山の数に上限なし。
やり取りの原文は末尾の「検品の記録」に残す。第1〜5版はgit履歴に残る)

### まず一言で:
- 可視マップは、**「この山(推し山)は、どこから見えるか」を地図に塗る機能**です。
- よく見られる山(地理院の1003山のうち計算済みのもの)は、**計算済みの地図(静的資産)をすぐ表示**します。
  それ以外の山(地元の山・今の目的点)は、**アプリがその場で計算**します(動的)。計算した結果は、静的な地図として配ることもできます。
- 複数の山を選ぶと、それぞれの山の島と、**選んだ山が同時に見える場所(可視辻)のかたまり(縞島)**が、「重複数」(そこで見える
  山の数)つきで一つの島リストに並びます(重複数の多い順に並べ替えられる)。行を選ぶと地図がその島を中央に映します。
  塗りは見える山の数が多いほど濃い金色、重複数がいちばん多い縞島は金と赤の縞で示されます。

### 用語(このデッサンで使う言葉):
- **推し山**: 見たい山(目的点)。可視マップの山リストは地理院「日本の主な山岳標高(1003山)」の全体で、
  百名山・二百名山・三百名山・百高山・その他のチェックで絞る(4つのリストは重なる。合わせて359の山頂。残り704が「その他」)。
- **名山対応表**: 地理院1003山の索引番号に、百/二百/三百/百高の印と表示名・別名を結び付けた表(`tools/kashimap/meizan-map.csv`)。
- **タイル**: 地図を格子に切った1枚。地理院の標高タイル(z15)に揃える。北緯35〜36°でほぼ**1km四方**。
  緯度で伸び縮みする(北海道で約0.89km・沖縄で約1.09km)。1タイルは256×256画素、画素は約3.9m間隔。
- **見える/見えない**: 各画素に「地上1.5mに立った人」を置いた時、推し山の山頂が見えるかどうか。
- **島**: 「見える」画素が地図上でひとかたまり(8近傍)になった区域。最小は1画素。可視マップはこの島を一覧にする。
- **島の代表点**: 島の画素のうち、島の重心にいちばん近い画素の中心。必ず島の中にある。地図の移動先・項番の並びに使う。
- **静的/動的**: 静的=一度計算して配った資産を読むだけ。動的=アプリがその場で計算する。どちらも結果の形は同じ。
- **静的資産**: 山ごとに配る「島の輪郭(ベクトル)+島の索引+計算条件」。ユーザーの入力で変わらない。
- **可視辻**: 選んだ複数の山が同時に見える場所(「辻」=交差点を可視判定に当てた言葉。第144・依頼者)。
- **可視辻マーカー**: 可視辻の塗り。見える山の数が多いほど金色に近づく(辻メッシュ検索のメッシュマーカーに相当)。
- **重複数**: その場所(島)で同時に見える、選んだ山の数。ふつうの島は1、縞島は2以上。
- **縞島**: 選んだ山のうち2つ以上が同時に見える場所のかたまり(可視辻の島)。画面の範囲には関係なく、選んだ山の島全体から作る
  (第145・依頼者のQ5修正)。同じ組み合わせの山が見える画素のつながりを1つの縞島とする(例: 3山を選ぶと、A・B・Cの島と、
  A∩B・A∩C・B∩C・A∩B∩Cの縞島ができる)。重複数がいちばん多い縞島を金と赤の縞で塗る。
- **マーカー**: 可視マップが表示用に出す目印。**山頂マーカー**(選んだ山の山頂)と**全展望マーカー**(重複数がいちばん多い
  縞島の代表点。選んだ山が全部見える場所があれば、そこ)。ユーザーが投稿する「ピン」(推し山サイト)とは別物。
- **山色**: 山ごとに割り当てる島の輪郭の色。表示天体のスタイル設定の16色を、選んだ順に使う(17山目は1山目の色に戻る)。
- **生値**: 緯度経度を度の小数のまま(度分秒に直さず)表示すること。
- **金色**: 塗りの基準色(辻メッシュと同じ #FFD700)。

### 目的:
- 推し山が「どこから見えるか」の全体像(島)を、地図上で一望できるようにする。
- 複数の推し山が「同時に見える場所」(可視辻)を、地図の中で探すのではなく**リストから選べる**ようにする
  (本質は「見える位置を知る」こと。第144〜145・依頼者)。
- 辻メッシュ検索(重なる日時を探す)の前段として、「そもそも見える場所」を教える。
- 推し山サイト(投稿型)の土台データになる(島の形と場所を渡す)。

### 概要:
- 機能概要(依頼者の構想・第136〜144)
  - 対象と範囲
    - 山リストは**地理院1003山の全体**(名山対応表)。「:百名山」「:二百名山」「:三百名山」「:百高山」「:その他」の
      チェックで絞る(Q1・第144。二百名山=百名山を含む200座、三百名山=百名山を含む300座。荒沢岳だけが二百にあって三百に無い)。
    - 山リストに無い山(地元の山)は、**My目的点**を対象に動的に計算できる(Q19・第144。第148: 依頼者修正=「今の目的点」ではなく
      My目的点。Myセットで切り替えられるので、地元の山を複数まとめて対象にできる)。計算したMy目的点は山リストに
      「My」の所属で並ぶ。
    - 範囲は推し山を中心に**60km四方 / 100km四方 / 300km四方 / 700km四方**(700km四方=中心から±350km。
      富士山が紀伊半島の潮岬[約320km]から見えた実績に合わせた)。範囲は入れ子なので、資産は山ごとに1組(P2)。
  - 見える/見えないの判断(第139推奨案・依頼者承認)
    - 「見える」は、地形の丸み(地球の曲率)と大気差を入れた既存の可視判定で決める。大気差の係数kは固定値を計算条件に記録。
    - 遮るものは三段で見る: **地形**(地理院DEM)+**建物**(PLATEAUの実測高)+**樹冠**(木の高さ)。
    - 樹冠のデータは**Meta/WRIの1m樹冠高マップ**(CC-BY 4.0・認証不要・公開)を第一候補にする
      (第140に富士山域のタイルの実在を確認済み)。控え: JAXA AW3D30(登録が要る)・ETH全球樹冠高10m。
    - 観測者は**地上1.5m**を標準にする(アプリの観測点高さの既定値1.5m化と揃える)。
    - 自分の立っている画素の木や建物は「遮るもの」に数えない(足元の木で自分が塞がれる誤判定を防ぐ)。
    - 樹冠の考慮は**オン/オフできる**(資産は「樹冠あり/なし」の2種)。
    - 可視判定の目的点は**地理院の最高地点**の座標。三角点の位置は使わない(Q9)。
  - 計算の進め方(静的=MCPで先に作っておく分)
    - 推し山を中心に**10km四方(=z15タイルの10×10のかたまり)に区切り、順番に計算**する。
    - 範囲は**小→大(60→100→300→700km)の段階**で外へ広げ、水平線の前段フィルタで「これ以上外には見える画素が無い」
      と分かった範囲で止める(P1・P2の回答。槍ヶ岳なら300kmで済むかもしれない)。
    - 判定は「山頂から放射状に、最大見上げ角を更新しながら外へ歩く」視域計算(P10。1画素を1回だけ触る)。
      既存の辻メッシュの可視判定ワーカーは「答え合わせ」に残す。
    - この計算は**MCPで宙の辻を動かして**行う(MCPは計算の指揮者=順番出し・進み具合・中断と再開)。
  - 計算の進め方(動的=アプリがその場で計算する分・第144)
    - 対象は「山リストで選んだが未計算の山」または「My目的点」(第148: 今のMyセットのMy目的点。複数可)。同じ判定・同じ結果の形。
    - 範囲は小さめから(端末の力とタイルの読み込み量で上限を決める。目安: 60km四方をz15で読むと標高タイル3,600枚≈360MB
      になるので、動的は10〜30km四方から、または粗いDEMで。実装デッサンで確定)。進捗は結果パネルの進捗バーに出す。
    - 結果は端末に保存し、静的資産と同じ形で**File出力**できる(→静的資産へ昇格できる。「静的マップも作れる」)。
  - 結果の形(静的資産・P1確定)
    - 計算はタイルごとの画像(見える=1/見えない=0)で行い、**配るのは島の輪郭(ベクトル・PMTiles)+島の索引(JSON)+
      計算条件(JSON)**。タイル画像は計算の中間物として手元に残す。範囲は入れ子なので**山×樹冠の1組**だけ。
    - **島の最小サイズは1画素**(P3・依頼者決定: 気象条件で見える日もあるので、小さくても「見えない」とは言わない)。
    - **ユーザーの入力は反映しない**(第138確定)。実地の見え方の答え合わせは推し山サイトの投稿が担う。
  - 島の一覧
    - **項番**=代表点を北から南(同じなら東から西)に並べた固定の通し番号(並べ替えても変わらない)。
      行のタップで地図がその島を中央に映す、チェックで輪郭が点滅(Q2)。
    - 島リストは、選んだ各山の島(重複数1)と縞島(重複数2以上)を**一つに並べる**。初期の並びは重複数の多い順→面積の大きい順
      (第145・Q5修正)。「:縞島のみ」で縞島だけに絞れる。
    - 面積は**km²・小数2桁+画素数**(Q15)。都道府県は**島がまたがる県を全部**(Q13)。
- デザイン概要(第144: Q10で検索の一式を先頭に。範囲・可視タイル・樹冠・マーカーの切替はコントロールメニューへ)
  - 入口: 位置情報メニューの「可視マップ」トグルボタン(デッサン`01-location.md`の23.)。
    押すと**検索を実行して結果パネルを表示**する(辻メッシュ検索と同じ・Q11)。
  - 0段目:
    - 1. 「可視マップ」ラベル(太字・左寄せ)
    - 2. 「▼」(右寄せ)。タップで開閉・初期は閉じている。
  - 1段目:
    - 3. 「推し山:」テキストボックス。スペース区切りで複数指定できる。
      プレースホルダー「山名、読み、都道府県名」。各語は山名・読み・別名・都道府県名のどれかに部分一致(Q7)。
  - 2段目:
    - 4. 「:AND」ラジオ(初期値オン)=全部の語に当たる山。
    - 5. 「:OR」ラジオ=どれかの語に当たる山(例:「長野 山梨」で両県の山)。
  - 3段目:
    - 6. 「:百名山」チェック(初期値オン)/7. 「:二百名山」チェック/8. 「:三百名山」チェック。
  - 4段目:
    - 9. 「:百高山」チェック/10. 「:その他」チェック(地理院1003山のうち、どのリストにも無い山)。
  - 5段目:
    - 11. 「検索」ボタン。テキストが空なら、チェックしたリストの山を全部出す(Q7)。
  - 6段目:
    - 12. 「My目的点で計算」ボタン(動的モードの入口。今のMy目的点を対象に、範囲を選んで計算する。山リストに無い地元の山向け)。
  - 7段目:
    - 13. 注記「標高・建物・木の高さからの概算です。現地では見えないこともあります」。
- デザイン詳細
  - 結果パネル(辻メッシュ検索と同じ骨組み・P7): ヘッダ=「⛶」最大化・タイトル「可視マップ」・件数「山N件/島M件」・「✕」閉じる。
    進捗バー(動的計算の進み具合。静的は「読み込み中…」)。山リスト→島リスト(2段)→コントロールメニュー。
    高さの割り振り(最大化/非最大化×コントロール開閉)は辻メッシュ検索と同じ。
  - 検索ボタンを押すと山リストが出る。山リストの行のチェックで山を選ぶと、その山の可視タイルが地図に塗られ、島リストに
    その山の島が並ぶ。選べる山の数に**上限は無い**(Q12。北アルプスでは16山以上が見える)。
  - 山リストの上に「全て選択」「全て解除」ボタン(見出し行のチェックはソートと両立しないため・P7)。
  - 山リストで選んだ山の山頂には**山頂マーカー**が立つ(色は山色・P6。コントロールの「:山頂マーカー」で表示/非表示)。
  - 山ごとに島の輪郭を色分けする(**山色**)。色は選んだ順に、表示天体のスタイル設定の16色(赤・桃・橙・黄・黄緑・緑・水・青・藍・
    紫・薄紫・茶・こげ茶・白・灰・黒)を使い、17山目は1山目に戻って循環する。輪郭には暗い縁取り(黄・白・桃でも読める・P6)。
    色は「空いている最小番号の席」方式で割り当てる(解除しても他の山の色は動かない。解除→再選択は同じ色に戻る)。
  - **可視辻マーカーと縞島**(第145・Q5の依頼者修正): 複数の山を選ぶと、各画素を「見える山の数(重複数)」で塗る。
    1山=基準色(薄い金)→重複数が多いほど濃い金(辻メッシュ検索の件数グラデーションと同じ流儀)。
    **縞島**は画面の範囲によらず、選んだ山の島全体から作る: 同じ組み合わせの山が見える画素のつながりを1つの縞島とし、
    島リストに「重複数」と「所属山(複数)」つきで並べる。例: 10山を選んで、いちばん重なる場所が5山なら、重複数5の縞島が
    先頭に並び、その下に重複数4・3・2の縞島と、各山の島(重複数1)が続く。
    地図では**重複数がいちばん多い縞島を金と赤の縞**で塗る(選んだ山が全部見える場所があればそこ。1山だけ選んだ時は縞なし)。
    行を選ぶと地図がその縞島を中央に映す。選び直すたびに作り直す(画面を動かしても作り直さない)。
  - **全展望マーカー**: 重複数がいちばん多い縞島ごとに代表点へ1本(Q4。タイルごとには立てない)。
    コントロールの「:全展望マーカー」で表示/非表示(初期値オフ)。
  - ホバーとクリック(Q17): 辻メッシュ検索と同じ。PCはホバーで島リストの該当行を更新(ハイライト)し、クリックで固定
    (ポップアップを閉じると解除)。ポップアップは「可視辻(重複数N): 山名…」。スマホはタップだけ(iOSのズームボタンの不具合回避)。
  - 島リストの行のチェックで、その島(縞島も)の輪郭が**点滅**する(複数可)。0.5秒交互(1Hz)。選んだ島が0・パネルを閉じた・
    画面が裏に回った、の時は止める(Q18・P5)。
  - 山リストの表示項目(各見出しでソートできる。見出しの下に注記「表示中: 60km・樹冠あり」・Q6):
    - 連番・索引番号(地理院1003山の番号。並べ替えても変わらない・Q16)
    - 山名称(名山対応表の表示山名)/読み(ひらがな)/所属(百/二百/三百/百高/他の印)
    - 都道府県(複数は全部。JIS都道府県コード順=概ね北から南。列の並べ替えは先頭の県のコード、同点は山頂の緯度・Q8)
    - チェックボックス(その行の山を選択)
    - 山頂の緯度/山頂の経度(地理院の最高地点。生値)/標高(m)/種別(三角点・標高点・測定点)(Q9)
    - 島の数(今の設定[範囲・樹冠]での数)/静的・動的(計算済みの資産があるか、その場で計算したか)
  - 島リストの表示項目(各見出しでソートできる。各山の島と縞島を一つに並べる・第145):
    - 項番(北から南・東から西の固定番号)/縞印(縞島なら「縞」)/**重複数**(1=ふつうの島、2以上=縞島。初期の並びはこの降順→面積の降順)
    - 所属山名称(縞島は見える山を全部「/」区切りで)/読み
    - 島がまたがる都道府県(全部・JIS順)/チェックボックス(その行の島を選択=点滅)
    - 代表点の緯度/経度(生値)/面積(km²・小数2桁)/画素数/山頂からの距離(km。縞島は各山への距離を「/」区切りで)
    - リストの上に「:縞島のみ」チェック(縞島だけに絞る)と「全て選択」「全て解除」ボタン。
    - 行のタップで地図がその島を中央に映す。
  - コントロールメニュー(辻メッシュ検索と同じ位置。項番は独自・P8。メニュー側には置かない=表示切替はここだけ・Q10):
    - 0. 「⛶」最大化(結果パネルのヘッダ)
    - 1. 範囲ラジオ「:60km / :100km / :300km / :700km」(初期値: 60km)。表示の切り抜き。未計算の範囲は選べない(灰色)。
    - 2. 「:可視タイル」チェック(初期値オン)=塗り(可視辻マーカー)の表示/非表示。
    - 3. 「:樹冠あり」チェック(初期値オン)=樹冠込みの資産と地形だけの資産の切替。
    - 4. 「:山頂マーカー」チェック(初期値オン)/5. 「:全展望マーカー」チェック(初期値オフ)。
    - 6. 
      - 注記「山リストは百名山、二百名山、三百名山、日本百高山、国土地理院の「日本の主な山岳標高（１００３山）」を元にしています。それ以外の山は、My目的点に登録してご利用ください。」
      - 注記「標高・建物・木の高さからの概算です。現地では見えないこともあります」。
    - 7. 「File出力」ボタン=山リスト・島リスト(縞島を含む)をCSVで(推し山サイトの下地・静的資産への昇格にも使う)。
    - 範囲・樹冠の切替で山リスト・島リストを作り直す(山のチェックは保つ)。
  - 描き方(P4・P13): 島の多角形をベクタのタイル(PMTiles)で読み、透明の塗り+山色の線の2レイヤに載せる。
    可視辻マーカー(重複数の濃淡)は、画面に映る範囲だけをオフスクリーンで数えて描き直す(地図を動かすたび)。
    縞島は、山を選び直した時に一度だけ作る: 選んだ山の島を、島全体を覆う作業用の格子(範囲に応じて粗さを変える)に描いて、
    画素ごとに「見える山の組み合わせ」を求め、同じ組み合わせのつながりを縞島にする(面積・代表点・都道府県はこの格子で概算)。
    多角形同士の交差を厳密に取る方式は代案(実装デッサンで比較)。
    タイル単位で合成する方式(P13)は代案として実装デッサンで比較する。1枚の画像に全体を描く辻メッシュの方式は使わない
    (60km四方で1辺15,360pxになり上限2048pxに入らない)。

- 推し山サイトとの分担(第142の静的/動的の線を第144で更新)
  - **可視マップ**: 島の形と場所・見える/見えないの塗り・可視辻。よく見られる山は計算済み(静的)、それ以外はその場で計算(動的)。
  - **推し山サイト**: 1〜2週間以内と2年先の推し辻(◎○)の日時、観測点のピン(ユーザー投稿:
    写真の位置情報+写真+レビュー+評価+ありがとう)、検索、ユーザー履歴。可視マップの結果(島)を土台の地図に使う。
    詳細は`07-oshiyamasite.md`。

### 進め方(第144・依頼者決定。第147で①完了・②〜④を「実装の段取り」に具体化):
- ①名山対応表(第147で検品完了: 359山頂・依頼者確認済10)→②〜④は下の「実装の段取り」の4段で進める。
- 可視マップの次に推し山サイトの実装。その後に宙断面・宙検索・宙統計・宙の会合。

### 実装の段取り(第147・AI起草。依頼者「実装に進んでも良い」を受けて):
- **段1: 山データと山リスト(アプリ側の前半)** — **第148で実装(v1.88.0・verify174=16チェック)**
  - `data/mountains.json`(第147で生成済み。名山対応表→アプリ用。1063山頂: 地理院1059+地理院に無い4)。
    形: `{id(索引番号), seq(連番), name, yomi, peak(山頂名), alias[], elev, kind(三角点/標高点/測定点), pref[](JISコード順),
    prefCode[], lat, lon, lists[](100/200/300/high), state, src}`。生成は `tools/kashimap/make-mountains-json.py`。
  - 位置情報メニューの「可視マップ」節(デザイン概要の0〜7段目)と、結果パネルの山リスト(検索・AND/OR・5つのチェック・
    ソート・全て選択/解除)。この段では地図に何も描かない(選んだ山の山頂マーカーだけ)。
  - 第148の実装メモ: 「可視マップ」ボタンは辻検索・辻メッシュと同じ段(宙の窓は次の段へ。01-locationの10/11段目)。
    結果パネルは辻メッシュと同じ配置/最大化/積み上げ規則で、辻検索・辻メッシュ・宙検索とは排他。
    山リストの列=選択・連番・索引番号・山名・読み・所属(百/二百/三百/高/他)・都道府県・山頂の緯度/経度・標高・種別
    (島の数・静的/動的の列は段3で)。件数は「山N件 / 選択M」。カタカナ入力はひらがなに直して読みに当てる。
    選択は閉じても保つ(開き直すとマーカーが戻る)。保存・URLキーは無し(段3で要否を決める)。
    「My目的点で計算」ボタンは置いてあるが無効(段4)。
- **段2: 計算の道具(静的資産を作る側)** — 次(第149〜)
  - `tools/kashimap/viewshed.js`(Node)。入力: 山id・範囲・樹冠あり/なし。出力: 1bitタイル(中間物)→島の輪郭GeoJSON+
    島索引JSON+meta.json。判定は放射状の視域計算(P10)+水平線フィルタ。DEMは地理院z15(DEM5A→5B→5C→10B)。
    大気差の係数kはアプリの標準値0.132で固定(計算条件に記録)。
  - 試作は**富士山・60km四方・樹冠なし(地形のみ)**から。既存の辻メッシュの可視判定ワーカーで数百画素を答え合わせ。
  - 樹冠(Meta/WRI 1m→3.9m画素へ最大値で落とす)と建物(PLATEAU)は段2の後半。
- **段3: 地図表示(アプリ側の後半)**
  - 島の輪郭をベクタで載せる(第1弾は山ごとのGeoJSON。300/700kmを入れる時にPMTilesへ)。山色の輪郭+暗い縁取り、
    可視辻マーカー(重複数の濃淡。画面範囲をオフスクリーンで数える)、縞島(作業用の格子=最大2048×2048で組み合わせを数え、
    つながりを取る。60kmで約30m/画素、700kmで約340m/画素)、島リスト(縞島を含む・重複数)、コントロールメニュー、
    ホバー/クリック/点滅、File出力。
- **段4: 動的モードと配布とMCP**
  - 動的モード: 段2の判定をブラウザのワーカーに移植し、「My目的点で計算」ボタンから(第148: 今のMyセットのMy目的点を
    まとめて対象に。Myセットの切替で地元の山を複数)。範囲は小さめ(下の未決Q20)。
    結果は端末に保存(IndexedDB)し、File出力で静的資産と同じ形に。
  - 静的資産の配布: 別リポジトリのGitHub Pages(下の未決Q21)。パスは `kashimap/v1/{id}/{canopy}/…`。
  - MCP: 段2の道具のコマンド(山id・範囲・進み具合・中断/再開)をそのままMCPの言葉にする(MCP対応デッサンと一体)。
- 検証: 各段でverifyを1本ずつ(段1=山リストの検索と絞り込み、段2=富士山60kmの島数と答え合わせの一致率、
  段3=描画とリスト、段4=動的計算の結果が段2と同じ)。Version Historyは段ごとに1行。

### 決定台帳(第143の検品→第144の依頼者回答で確定。原文は末尾の「検品の記録」):
- Q1 対象: 山リストは地理院1003山の全体。百/二百/三百/百高は**チェックボックス**(重なりあり)+「:その他」を追加。
- Q2 島リスト: 項番=北→南の固定番号。初期の並びは面積順。行タップで地図移動・チェックで点滅。
- Q3 名前は**山頂マーカー・全展望マーカー**(2段に置く。「ピン」は使わない)。
- Q4 全展望マーカーは**島ごとに代表点へ1本**。
- Q5(第145修正) 縞島は**画面の範囲によらず、選んだ山の島全体から作る**。各山の島(重複数1)と縞島(重複数2以上・所属山は複数)を
  一つの島リストに並べ、重複数の多い順に並べ替えられる。行を選ぶと地図がその島を中央に。塗り=重複数の濃淡。
  地図の縞=**重複数がいちばん多い縞島**(第146・依頼者承認。「選んだすべての山」が見える必要はない。最大がその範囲内の最大)。
  縞島は大きめの作業用の格子で計算する(第146・依頼者了承)。
- Q6 「範囲」「樹冠」は列に持たず、見出しの下の注記「表示中: 60km・樹冠あり」。
- Q7 :AND初期オン・対象チェックの初期値は百名山だけオン・テキスト空なら全部・各語は山名/読み/別名/都道府県名に部分一致。
- Q8 都道府県はJISコード順。複数県はコード順に並べ替えて格納。
- Q9 列は「山頂の緯度/経度(地理院の最高地点)+標高+種別」。可視判定の目的点も最高地点。
- Q10 検索の一式を先頭に。範囲・可視タイル・樹冠・マーカーの切替は**コントロールメニューだけ**。
- Q11 トグル=検索実行+結果パネル表示(辻メッシュと同じ)。
- Q12 選べる山の数に**上限なし**(色は16色を循環)。
- Q13 島の都道府県は**またがる県を全部**。
- Q14 既定の目的点「富士山」は**地理院の最高地点「剣ヶ峯」(35.360738, 138.727373)に揃える**(出典が明確な方を正とする。
  ダイヤモンド富士の中心のずれはオフセット検索で追える。実装時に回帰の期待値の更新とセット)。
- Q15 面積はkm²・小数2桁+画素数。
- Q16 山リストの項番=地理院の**連番と索引番号**。
- Q17 ホバー=詳細リスト更新・クリック=固定(辻メッシュと同じ)。
- Q18 輪郭は**点滅**(P5の止め方で)。
- Q19 可視マップは**静的+動的**。よく検索される山は静的、地元の山も動的に計算できる。入口は「My目的点で計算」
  (第148・依頼者修正: 今のMyセットのMy目的点を対象に。Myセットで切り替えて地元の山を複数)。
- P1 配る形は**島の輪郭ベクトル**。P2 範囲は入れ子で資産は山×樹冠の1組。計算は**小→大の段階**で、要る範囲で止める。
- P3 **島の最小サイズは1画素**(依頼者決定。AI案の距離帯の閾値は採らない)。
- P4 描き方は**タイル化**(1枚の画像方式は使わない)。P5 スマホ・点滅の規則。P6 縁取り・山頂マーカーは山色。
- P7 結果パネルは辻メッシュと同じ骨組み+File出力。全て選択/解除はボタン。P8 コントロールは独自の項番・表示切替はここだけ。
- P9 名山対応表: 表記違いは**別名に両方**を持つ(利尻岳/利尻山)。峰が複数ある山はどの峰かを精査。同名異山は索引番号で突き合わせ
  (第144で下書き: `tools/kashimap/meizan-map.csv`)。
- P10 計算側(放射状の視域計算・水平線フィルタ・既存ワーカーで答え合わせ)。P11 計算条件JSONと版・出典表記。
- P12 地理院サイトのTLS: 手当ては`tools/kashimap/fetch-gsi-1003zan.sh`(証明書検証はそのまま)。依頼者のMacやブラウザでは不要。
- P13 タイル単位の合成(addProtocol)は代案。実装デッサンで比較。P14 百高山の内訳(北・中央・南アルプスが揃うので問題なし)。

### 未決事項(実装しながら決める。依頼者への質問はQ20〜Q23):
- Q20 動的モードの範囲: 初期値10km四方、スマホは最大20km、PCは最大30km(いずれもz15)。60km以上は静的資産だけ、でよいか
  (60km四方をz15で読むと標高タイル3,600枚≈360MBになるため)。

  できたら、辻メッシュ検索で、6×6があるので、最低それ以上にしたいです。

- Q21 静的資産の置き場所: 公開の別リポジトリ(例: `soranotsuji-kashimap`)をたけちゃんが作り、GitHub Pagesで配る、でよいか
  (本体リポジトリに数百MBを入れない)。

  はい、承知しました。

- Q22 段2の試作は「富士山・60km四方・樹冠なし」から、でよいか。

  はい、承知しました。

- Q23 大気差の係数kは静的資産ではアプリの標準値0.132で固定、でよいか(アプリの標高グラフは気象値から出すので、資産のmeta.jsonに記録し、注記に「標準大気」と書く)。

  はい、承知しました。

- 動的モードの計算時間の目安・端末保存(IndexedDB)の形・「目的点で計算」の段組み(段4で)。
- 縞島の作り方(作業用の格子の粗さ=範囲に応じて。粗いと小さな縞島が消える→重なりの近くだけ細かく取り直すか、
  多角形の交差で厳密に取るか)。縞島の面積・代表点の精度の書き方(概算の印)。
- 山リストの「静的・動的」列の取り方(静的資産の有無の索引)。
- 名山対応表の検品(要確認6件=谷川岳・九重山・飯豊山・那須岳・安達太良山・丹沢山の峰の選択、地理院に無い4座の座標)。
  百高山の定義(標高順上位100)と山と溪谷社の選定との差の確認。
- 静的資産の形式の細部(PMTilesの属性・島索引の項目・meta.jsonの項目)と置き場所(別リポジトリのGitHub Pagesから)。
- 樹冠マップの品質確認・PLATEAU建物の取り込み方・kの固定値・樹冠1m→3.9mの落とし方(最大/平均)・既存ワーカーとの一致率の目標。
- MCPの言葉の決めごと(操作・進み具合・結果の受け取り)=MCP対応デッサンと一体で。
- 既定目的点の富士山を剣ヶ峯へ揃える作業(回帰の期待値更新とセット・実装時)。

### 検品の記録(第143のAIの質問Q1〜Q19・提案P1〜P14と、第144の依頼者回答。原文):

(第143のAIの検品と、それへの第144の依頼者回答[→で始まる段落]をそのまま残す。決定は上の「決定台帳」に一行ずつ写した。
観点: 画面の一貫性・山データの入手性・静的資産の規模・文書の整合・実装の作り方)

#### 直したもの(誤記・表記。依頼者の文への手入れはこれだけ):
- 「山頂三角点の経度」「島の中心の経度」の説明文が「緯度」のままだった→「経度」に。行頭の全角スペースも半角に。
- 「番号づけしたのもの」→「番号づけしたもの」。「山の数表示し」→「山の数を表示し」。「焦茶色」→「こげ茶色」(アプリの色名)。
- 用語の節に「百高山(4リストは重なる)」「マーカー(ピンとの区別)」「山色」「全展望」「島の代表点」「生値」「金色」を追加。
  「対象と範囲」に百高山を追加。「結果の形」を「タイルで計算し、島の輪郭で配る」案に(提案P1・採否待ち)。

#### 質問(依頼者の判断が要るもの):
- Q1 「:二百名山」「:三百名山」のチェックの意味: 200座・300座の全部か、百名山に加わる分だけか。
  (二百名山は百名山を全て含む。三百名山も百名山を全て含み、二百名山のうち荒沢岳1座だけが三百名山に無い。
  百・二百・三百の和集合は301座。百高山は約半分がどのリストにも無い脇峰で、4つの和集合は約358山頂)
  →AI案: 「200座全部・300座全部」。重なりは山リストに「所属」列(百/二百/三百/百高の印)を足せば一目で分かる。
  
  →ありがとうございます。二百名山や三百名山の中に百名山が含まれているとは、知りませんでした。
  お恥ずかしいです。
  その上で、4つの和集合は約358なら、全体の山リストを、ご提案のあった「日本の主な山岳標高（１００３山）」リストにして、その中から、全百名山、全二百名山、全三百名山、全百高山を選びたいのですが、百高山だけ集合が包含関係が少しずれているので、やはり、4つのオプションは、ラジオボタンではなくて、チェックボックスで、選択したいです。
  さらに、全体のリストを「日本の主な山岳標高（１００３山）」にしたので、「:その他」のチェックボックスも増やしたいです。

- Q2 島リストの並び: 「面積の大きい順」(概要)と「北から南・東から西の項番」(詳細)は両立させてよいか。
  →AI案: 項番=代表点を北から南(同じなら東から西)に並べた固定の通し番号(並べ替えても変わらない名札)。
  初期の並びは面積の大きい順。行のタップで地図がその島へ移動、チェックで輪郭が点滅、と役割を分ける。
  
  →ありがとうございます。
  その案を採用でお願いいたします。

- Q3 山頂の目印: 「:山頂ピン」チェック(初期値オン)で黄色のマーカー、に一本化でよいか(今は105行目が無条件に読める)。
  色は黄色のままか、山色にするか(山色の4番目=黄と、塗りの金[#FFD700]と、黄色のマーカーが地図上で紛れる)。
  名前は「:山頂マーカー」「:全展望マーカー」と、投稿型の「ピン」と区別してよいか。

  →すみません。山頂ピンと山頂マーカーは、同じものを指していて、全展望ピンと全展望マーカーは同じものを指しています。
  マーカーと書くと、横幅が入らないだろうかと思ったのですが、そもそも、2段にすれば良いことに気がつきました。
  ピンという言葉は使わず、優辻マーカーという言葉を使っているので、山頂マーカーと全展望マーカーの言葉を使用しましょう。

- Q4 全展望の目印: タイルごとではなく「全部見える島(縞の島)ごとに、代表点へ1本」でよいか
  (タイルごとに立てると数千〜数万本になる。縞のタイルと二重の表現にもなる)。初期値はオフでよいか。

  →そうですね。確かに、富士山とかだと、数千とかなりますよね。
  そこは考えていませんでした。
  提案内容を採用します。

- Q5 塗りの階調: 辻メッシュは「1件=白(基準色スライダー)→件数が多いほど金→最多だけ金赤縞」で、1件は金ではない。
  可視マップは「1山=金」と書かれているので、2山目以降の濃さの行き先が無い。
  →AI案: 「1山=薄い金(半透明)→N山=濃い金(不透明)→選んだ山が全部見える場所=金赤縞(2山以上を選んだ時だけ。
  1か所も無ければ縞は出ない)」。基準色のスライダーは持ち込まない(塗りの下に地図が透ける方が場所が分かる)。
  縞のタイルをクリックすると、島リストが「そのタイルを含む島(選んだ山ごとに1つずつ)」に絞られる、でよいか。
  終点も決めたい: 辻メッシュは「実際の最大の重なり数=いちばん濃い金・縞」(相対)だが、05の縞は「選んだ山が全部
  見える場所」(絶対N)。縞が絶対Nなら、いちばん濃い金もNに揃える(見た目は辻メッシュと同じ・条件は全展望)。
  1山だけ選んだ時はグラデーションも縞も出ない、と読んでよいか。輪郭は塗りの上に描く(塗りの不透明度は辻メッシュの約90%より薄く)。

  気がつきたけれど、絶対Nを縞にするのはやめました。
  何故なら、縞を探すのが手間になってしまうからです。
  よくよく考えたのですが、島リストが欲しいのが目的ではなくて、可視マップは、見える位置がどこかを知りたいが本質ですよね。
  そうしたら、複数の山リストの中の山を選択していったら、その選択していった山々が同時に見える位置のリストが表示された方が、目的に合っていると思います。
  けして、どの範囲で見れるという島が見たいかではないと思うのです。
  どうせ知るなら、どの範囲で見れる島より、複数選んだ時に、同時に見える位置の範囲のリストがある方が、より本質的な解決だと思います。
  そういう観点で言うと、こう言うことができるかどうか分かりませんが、画面内の同時に複数山見える位置(メッシュマーカーならぬ、可視辻マーカーとして、)を、見える山の数によって、辻メッシュ検索のように、金色のグラデーションで表現するのは如何でしょうか。
  また、選択している山が10山あった場合に、画面内で重なって見える位置の山の数の最大が5山だった場合は、5山の位置を縞にする。
  そして、画面で表示範囲が変更されれは、その山の最大の位置もずれていくと思うので、縞も変更されて表示される、と言うのは如何でしょうか。
  できたら、縞一ヶ所ではなくて、縞の集合の縞島リストにしたいです。

  修正です。
  よくよく考えたのですが、画面内で縞島を探すより、リストアップされた中で探した方が早いと思いました。
  ですから、山リストで、1山目を選択すると、その島リスト表示され、2山目を選択すると、1山目の島リストと、2山目の島リストと、1山目と2山目の縞島リストが表示される、と言うのはどうでしょうか。
  もちろん、列に、重複数という列があって、重複数の多い順に降順にソートできるとか、1山目の島リストの島の集合Aと、2山目の島リストの島の集合Bと、1山目と2山目の島リストの縞島の集合(A and B)と、があるようにはできないでしょうか。
  その縞島を含む島リストから行を選択すると、その島が画面中央に表示されるという方が、使いやすいように思えました。
  ですから、画面内に限らず、山リストで選択した山の数だけ島リストの島が表示され、尚且つ、選択した山の数だけ重なる縞島も表示され、選択した山の数より少なく重なる縞島も表示されるようになります。
  こちらの方が、網羅的であり、選択しやすいように思えます。
  また、島リストでは、所属山が記載されますが、縞島は、複数の山に所属しているので、所属山も複数記載します。
  如何でしょうか。

- Q6 山リストの1行の単位: 「山」1行でよいか。「可視マップの範囲」「樹冠のあり、なし」は全体の設定なので、
  列に持つと全行同じ値になる。→AI案: 列から外し、リストの見出しの下に注記「表示中: 60km・樹冠あり」。
  「島の数」は今の設定(範囲・樹冠)での数を出す。範囲や樹冠を変えるとリストを作り直す。山のチェックは保つ。
  
  →そうですね。全て同じになってしまうので、リストの見出しの下に注記「表示中: 60km・樹冠あり」でお願いいたします。

- Q7 検索の細部: 「:AND」の初期値オン、対象チェックの初期値は百名山だけオン、でよいか。
  「空白で」は「テキストが空なら、チェックしたリストの山を全部出す」の意味でよいか。
  検索語は「山名・読み・別名・都道府県名のどれかに部分一致」でよいか(「富士 静岡」でANDなら両語とも当たる山)。
  ORが要る場面(例:「長野 山梨」で両県の山)を一言添えたい。

  ありがとうございます。
  その方向で、お願いいたします。

- Q8 都道府県の並び: JIS都道府県コード順(01北海道〜47沖縄。概ね北から南)で代替してよいか。
  「東から西」は厳密には経度でしか並ばない(関東は茨城→栃木→群馬→埼玉→千葉→東京→神奈川の慣用順)。
  複数県の山は取り込み時にコード順へ並べ替え、列の並べ替えは先頭の県のコード、同点は山頂の緯度(北が先)。

  ありがとうございます。
  その方向で、お願いいたします。

- Q9 三角点の列: 「山頂の緯度/経度(地理院の最高地点)」+「標高」+「種別(三角点/標高点/測定点)」に改めてよいか。
  地理院の座標は三角点ではなく最高地点のもので、全1059行のうち約3割(330行)・百名山では4〜5割が三角点でない
  (富士山・剱岳・北岳・槍ヶ岳・奥穂高岳など。名前照合できた84山のうち38山、別名も当てて99座まで見ると約半数)。
  「三角点があれば」のままだと主要な山で空欄になる。
  可視判定の目的点は「地理院の最高地点」で、三角点の位置は使わない、と明記したい(立山は一等三角点が雄山2991.6m・
  最高点は大汝山3015mで約300m離れる。谷川岳はトマノ耳/オキノ耳、鳥海山は七高山/新山。三角点を目的点にすると誤差になる)。
  三角点の等級(一等〜四等)は地理院の一覧に無く、欲しい山だけ手で引く別列(任意)。

　ありがとうございます。
  地理院の1003山を利用したいので、その方向でお願いいたします。

- Q10 段組み: 検索前は地図に何も出ないので、検索の一式(7〜14)を先頭に上げ、範囲・可視タイル・樹冠・
  表示オプションは「検索後に効く切替」としてコントロールメニュー側に一か所へまとめてよいか
  (辻メッシュは1段目が実行ボタン、その下に条件、が流儀)。注記は検索ボタンの直下か結果パネルの先頭へ。
  その前に一つ決めたい: 「範囲」「樹冠あり」は、資産の版を選ぶ**検索条件**(検索ボタンの上)か、検索後に効く
  **表示切替**(コントロール側)か。同じ4項目(範囲・可視タイル・樹冠あり・注記)が今はメニュー先頭とコントロールの
  2か所にあるので、答えによって片方を削る。「:可視タイル」と山頂/全展望の目印は「選んだ山」に効くので表示切替で確定。

  > 検索の一式(7〜14)を先頭に上げ、範囲・可視タイル・樹冠・表示オプションは「検索後に効く切替」としてコントロールメニュー側に一か所へまとめてよいか
  はい、上記でお願いいたします。

- Q11 「可視マップ」トグル(位置情報メニューの23.)の役割: メニューの開閉だけか、結果パネルの表示か、
  前回の検索結果の復元までか(辻メッシュではトグル=検索実行+結果パネル表示)。

  辻メッシュと同様で、検索実行+結果パネル表示でお願いいたします。

- Q12 選べる山の上限: 山色が16色なので「同時に選べるのは16山まで」(17山目は選べない・注記を出す)でよいか。
  「全て選択」は結果が16を超える時は無効に。色の割り当ては「空いている最小番号の席」方式
  (解除しても他の山の色は動かない。解除→再選択は同じ色に戻る)でよいか。

  北アルプスに行くと、16山以上見れるので、これは、ユーザーが選択した数だけ許容したいです。

- Q13 島の都道府県: 「代表点の県(1つ)」か「島がまたがる県(全部)」か(今の文は「中心」なのに「複数」で矛盾)。

  すみません。
  「島がまたがる県(全部)」で、お願いいたします。

- Q14 既定の目的点「富士山」(35.3627986, 138.7307814。火口の中央付近)は、地理院の最高地点「剣ヶ峯」
  (35.360738, 138.727373)と385m離れている。山データを地理院に揃えると既定値と食い違う。
  揃えるか据え置くか(揃えるなら回帰の期待値の更新とセット。実装時でよい)。

  これは、迷いますね。
  私は最初は、剣ヶ峯を富士山の座標にしていたのですが、龍ヶ岳からダイヤモンド富士を検索したら、ずれてしまうのですよね。
  ただ、どちらが、中心でも、オフセット検索があるので、ジャストなダイヤモンド富士は、検索できます。
  一手間かかるかどうかの違いです。
  ここは、出典が明確な方が正な情報と捉えていただきやすいので、地理院の最高地点の「剣ヶ峯」に合わせましょう。

- Q15 面積の単位: 「例:100m2」は約7画素(1画素≈15m²@北緯35°)。「km²・小数2桁(例:1.25km²)+画素数」でよいか。

  はい、それで、お願いいたします。

- Q16 山リストの項番の並び: 島リストは「北から南、東から西」の固定番号(Q2)だが、山リストは「検索結果を番号づけ」
  だけで規則が無い。「山名の読みの五十音順(または標高順)で振る固定の通し番号。列で並べ替えても番号は変わらない」でよいか。

  地理院の「日本の主な山岳標高(1003山)」があるので、これらの「連番」と「索引番号」を採用しましょう。

- Q17 ホバーとクリックの役割: 05は「ホバー=山の数を表示・クリック=島をリストアップ」、辻メッシュは「ホバー=詳細リスト
  更新・クリック=固定(ポップアップを閉じると解除)」で違う。意図した違いか。→AI案: 辻メッシュに揃え、ポップアップは
  「可視タイル(N山)」の書式。スマホはタップだけ(iOSのズームボタンの不具合回避。実装済みの規則)。

  すみません。
  私の認識違いがありました。
  05も辻メッシュと同じように、「ホバー=詳細リスト更新・クリック=固定(ポップアップを閉じると解除)」でお願いいたします。

- Q18 島の輪郭の「点滅」: アプリ内の点滅の前例は全天儀の星座名称(黄と赤を0.35秒交互・タップ時は約4秒で自動解除)だけで、
  地図上の線の点滅は前例が無い。「選択中は太線+白縁」の静的な強調でもよいか、点滅を残すか(残すならP5の止め方で)。

  ありがとうございます。
  やはり点滅が良いです。
  ですので、P5の止め方で大丈夫です。

- Q19 進捗表示: 可視マップはアプリ側で計算しないので、辻メッシュの進捗バー(計算の進み具合)はそのままでは当てはまらない。
  60kmは「読み込み中…」の1行だけ、700kmの大きい山だけ読み込みの進捗を出す、でよいか。

  大変すみません。
  大事なことを忘れていました。よく検索される山は、静的にして、すぐに表示されるようにしたいのですが、小さな地元の山まで網羅をしたいので、どんな山も、アプリで計算できるようにしたいです。
  今まで言っている事と、二転三転してしまいますが、推し山サイトは、観測点は、投稿型で、動的かもしれませんが、その地図のベースとなるマップは、静的も利用し、ユーザー個別の地元の山もサポートするので、動的だと思います。
  ですから、可視マップは、アプリで計算をして、静的マップも作れる、動的な生成を行います。
  ですから、個人的には、目的点の検索ボックスがそのまま使えないかとも思いました。
  目的点ボックスでは、ありきたりな、「中山」と検索すると、住所がわかるので、地元の「中山」が検索できるようになっています。
  でも、山名の読みがあるかは分かりません。

#### 提案(AIの案。数字は根拠つき。採否はお任せ):
- P1 **配る形は「島の輪郭ベクトル」、タイル画像は計算の中間物にとどめる**。
  数字: タイル数=(一辺km)²で 60km=3,600枚/700km=490,000枚。1タイルの1bit生サイズ=256×256÷8=8KB。
  山1つ・700kmで 490,000×8KB≈3.9GB(生)。陸だけ(地理院DEMは海に無い)・見える所だけに絞っても、
  全山×樹冠2種で数GB・数百万ファイルになり、GitHub Pages(目安1GB)にもgitにも載らない。
  島の輪郭(3.9m画素を1画素の許容で単純化した多角形)なら、60kmで約0.5MB/山、700kmの富士山級で5〜8MB/山。
  約358山頂×樹冠2種=約716組×0.5MB≈360MB=GitHub Pagesに収まる。形式はPMTiles(1ファイルを部分取得する
  形式。読み手はProtomaps製のpmtilesライブラリで、今の4.7.1でaddProtocolから使える。GitHub Pagesが部分取得に
  応じることは実測済み[Range要求→206])。置き場所の上限: GitHub Pages=サイト1GB・1ファイル100MB・帯域100GB/月、
  Firebase Hosting無料枠=保存10GB・転送360MB/日(料金ページの値。未実測)。転送に日次上限があるので
  「1山の閲覧で数MB以内」を目標値に。
  1組(山×樹冠)=islands.pmtiles(島の多角形。属性: 島id・山id・面積・画素数・代表点・都道府県コード)
  +islands.json(島リスト用の索引。タイルを読まずにリストが出せる)+meta.json(計算条件)。

  ありがとうございます。
  島の輪郭ベルトルの集合で構いません。
  しかも、百名山、二百名山、三百名山、百高山を一旦700km→300→100→60kmの順で計算して、富士山なら700km以内に実際に観測点がありそうですが、槍ヶ岳だったら、300kmで済むかもしれませんし、段階的に計算をして、最小で済む範囲の島の輪郭ベクトルの集合で良いと思います。

- P2 **範囲は入れ子なので、資産は「山×樹冠」の1組だけ**。60kmの結果は700kmの結果の中心±30kmそのもの。
  範囲ラジオは表示の切り抜きにし、未計算の範囲は選べなくする。まず全山を60kmで揃え、700kmは富士山など数山から
  (700km・1組の判定は約10¹⁰画素。全山を700kmで揃えるのは第1弾では非現実的)。

  なるほど。
  範囲を入れ子にするのですね。
  それなら、上記で、700→300→100→60kmとせずに、小→大の範囲で計算をしていくのが良いですね。

- P3 **島の最小サイズは、一律ではなく山頂からの距離で段階を付ける**。大気差の揺れ(係数kの±0.05は目安。アプリの標準値は
  0.132、気象値から出す式では概ね0.13〜0.19)による見かけの上下は
  30kmで±3.5m・100kmで±39m・300kmで±353m・350kmで±481m。300km超では境目が日によって数百m動くので、
  数画素の島は見かけの精密さでしかない。案: 〜30km=9画素(≈12m四方)以上・30〜100km=64画素・
  100〜300km=1,024画素・300km超=4,096画素。地図は全島(拡大率で自動的に間引かれる)、リストは閾値以上だけ。

  確かに、見かけの精密さでしかないかもしれないけれど、見えないと言っているわけではないと思います。
  何故なら、気象条件が絡んでくるから、私としては、島の最小サイズは、1画素固定にしたいです。

- P4 **描き方は辻メッシュの「1枚の画像」方式では入らない**。辻メッシュは検索範囲を1枚の画像(最大辺2048px。
  iPhoneで描画に失敗した実績から決めた上限)に描いて地図に貼る。可視マップは60km四方で1辺15,360px、
  700kmで179,000pxになり、画素のまま(3.9m)は不可能。→島の多角形をベクタレイヤ(透明の塗り+山色の線)に載せ、
  「重なりの濃淡」「全展望の縞」は画面に映る範囲だけをオフスクリーンで数えて描き直す(地図を動かすたび)。
  第1版は半透明の重ね塗り(重なるほど自然に濃くなる)+ホバーの山数で始め、厳密な階調は第2版に。
  ホバーの山数・クリックの島・輪郭の色・点滅は、ベクタなら地物の問い合わせ1つで全部足りる。
  なお、memoの「DEM1画素=カンバス1画素」は、範囲全体を1枚に持つ限り成り立たないが、memoが同じ項目で挙げる
  「カンバスの切り替え」(=タイル化)なら成り立つ(z15のタイル1枚=256×256画素=DEM画素そのまま。地図ライブラリの
  タイルソースがその仕組みで、今見えている範囲のタイルだけを読む。700kmをスマホで見る拡大率では十数〜数十枚)。

  ありがとうございます。
  もちろん、「カンバスの切り替え」(=タイル化)でお願いいたします。

- P5 **スマホ(ホバー無し)と点滅**: 辻メッシュの規則(PCはホバー=島リスト更新・クリック=固定、スマホはタップだけ。
  iOSのズームボタンの不具合回避)をそのまま写す。点滅は0.5秒交互(1Hz。毎秒3回未満)で、選んだ島が0・
  パネルを閉じた・画面が裏に回った、の時は止める(常時描画にすると電池を食う)。

  ありがとうございます。
  その方向でお願いいたします。

- P6 **輪郭には暗い縁取り**(黄・白・桃の山色が淡色地図でも読めるように)。山頂マーカーは山色にすると対応が分かる。

  そうですね。
  その方向でお願いいたします。

- P7 **結果パネルは辻メッシュと同じ骨組み**(⛶最大化・件数=山N件/島M件・進捗・詳細リストの高さ規則)と明記し、
  **File出力(山リスト・島リストのCSV)**を付ける(座標・面積つきの島リストは推し山サイトの下地にもなる)。
  04の「検索結果表示領域」には✕閉じると件数表示が書かれていない(実装にはある)ので、05には「ヘッダ=⛶最大化・
  タイトル・件数「山N件/島M件」・✕閉じる」と直接書く。
  見出し行のチェック(全て選択/解除)は、ソートが並べ替えのたびに見出しの文字を書き直すので、クリックが競合する
  だけでなくチェック要素そのものが消える。リストの上の「全て選択」「全て解除」ボタンにする(Q12の16山上限もボタンなら出しやすい)。

  ありがとうございます。
  その方向でお願いいたします。

- P8 コントロールメニューは項番を独自に振り(辻メッシュの流儀)、メニュー側と「同じ状態の別の見た目(連動)」か
  「検索実行時の独立コピー」かを一言で明記。辻メッシュには両方の前例がある(検索エリアのラジオは両側に置いて双方向連動、
  再フィルタ群は独立コピー)。Q10の答えが「表示切替」なら、範囲・可視タイル・樹冠あり・山頂/全展望の目印を
  コントロール側に集めてメニュー側から外す案(前例より一歩踏み込む)。

  Q10は、表示切り替えなので、そのようにお願いいたします。

- P9 **山データは地理院「日本の主な山岳標高(1003山)」で賄える(実測済み)**: CSV(79KB・Shift_JIS・2026-03-31版)・
  9列=連番/索引番号/山名<山頂名>/山名よみ/標高/種別(三角点・標高点・測定点)/都道府県(複数は空白区切り)/緯度/経度。
  利用条件は公共データ利用規約(PDL1.0=CC BY 4.0互換)。出典表記「『日本の主な山岳標高一覧(1003山)』
  (国土地理院)を加工して作成」。ページは https://www.gsi.go.jp/kihonjohochousa/kihonjohochousa41139.html 。
  これに自前の**名山対応表**(索引番号キー・表示山名・よみ・別名・百/二百/三百/百高フラグ・備考=どの峰を山頂にしたか)
  を1枚足す。名前の自動照合は百名山でも1割外れる(利尻岳/利尻山・甲武信岳/甲武信ヶ岳・八ヶ岳=赤岳…)ので人手。
  峰が複数ある山(谷川岳3行・丹沢山3行・阿蘇山5行)はどの行を山頂にするか人が決める。同名異山(大山3・駒ヶ岳10)
  があるので主キーは索引番号。memoの「百名山の目的点CSV作成」はこの対応表作りに読み替え、実装の最初の作業に。

  ここは、利尻岳/利尻山の違いなどは、リストを作成するときに、両方を含めることにする。
  また、丹沢山3行は、百名山や○名山のリストから、どれを対象にしているかを精査する。
  同名異山も、きちんと突き合わせをClaudeさんにしていただく。

- P10 **計算側**: 既存の可視判定ワーカー(辻メッシュ)の判定式は流用できる(観測者高1.5mを固定・遮蔽面=地形+
  max(建物,樹冠)を1枚の格子にして渡す・足元は地形だけ)。ただし画素ごとに山頂まで直線を歩く方式は規模に合わない
  (60kmで約2.4億画素×1万歩)。→山頂から放射状に「これまでの最大見上げ角」を更新しながら外へ歩く視域計算
  (1画素を1回だけ触る。正距方位図法で計算すれば光線が直線で、地図上の直線と大圏[地球上の最短経路]のずれ
  [L²×tanφ÷(8R)。北緯35°で東西300kmなら1.24km・495kmなら3.37km]も消える)。規模は60km四方≈2.4億画素で
  辻メッシュの最大(6×6タイル=236万画素)の100倍、700kmではさらに約1,600倍。
  前段の「水平線フィルタ」(式: 画素の標高+1.5 ≥ (d − √(2×Reff×H))²÷(2×Reff)。Reff=R÷(1−k)≈7,585km[k=0.16]):
  富士山(H=3776m)の水平線は√(2×7,585×3.776)≈239km。350km先で見えるには標高約810m以上、495km先では
  約4,300m以上が必要(存在しない)ので、700km四方の大半はタイルの最高標高だけで捨てられる。
  既存ワーカーは「答え合わせ」に残す(資産完成後に数千画素を抜き取り、アプリの標高グラフと同じ判定である証明)。
  大気差の係数kは固定値(アプリの標準値0.132か、標準気象値の約0.17か)を決めて計算条件に記録(アプリは気象値から
  出すので、値を揃える)。
  樹冠1mを3.9m画素へ落とす時は最大値(安全側)か平均かを決める。

  ありがとうございます。
  その方向で、お願いいたします。

- P11 **計算条件(meta.json)と版**: DEMの種類と取得日・PLATEAUの都市と年度・樹冠データの版・観測者高・k・
  計算済みの広さ・島の閾値・生成日時・道具の版。パスに版を入れる(kashimap/v1/{id}/…)。
  GitHub Pagesは10分キャッシュ(max-age=600)なので、同じパスの上書きは混在し得る。注記か凡例に出典
  「国土地理院(標高タイル)・PLATEAU・Meta/WRI樹冠高(CC-BY 4.0)・日本の主な山岳標高(国土地理院)を加工」。

  ありがとうございます。
  その方向で、お願いいたします。

- P12 地理院サイト(www.gsi.go.jp)はTLSが古く、この環境のcurlは既定では失敗する(証明書検証はそのままに、
  古い再交渉の許可だけで取得できた)。MCPで自動取得する時も同じ手当てが要る。

  そうなのですね。
  どのようにしたら良いでしょうか。

- P13 **重なりの塗りの代案: addProtocolでタイル単位に合成する**(P4の代案。実装デッサンで試して軽い方を採る)。
  地図ライブラリには「白黒のタイルに色の階調を当てる」機能(raster-color)が無い(4.7.1だけでなくv5・v6にも無い。
  Mapbox GL JS v3の機能)ので、色は端末でタイル画像に焼き込む。同じ経路は本番で既に動いている(地理院DEMタイルを
  canvasで加工してPNGで返す addProtocol('gsidem'))。選択中N山分の島多角形をそのタイルに描いて1ビット化し、画素ごとに
  件数を出し、辻メッシュと同じ規則(件数で金を濃く・件数==Nは金赤縞)で描いて返す。選択が変わったらタイルのURLを
  切り替える。山ごとに「タイルが存在する範囲」の粗い索引(z10の有無ビット)を持ち、海や見えない領域は取りに行かない。

  これは、あまりよく分かりませんでした。
  もう少し説明をお願いいたします。

- P14 百高山の内訳(名山対応表の備考に): 標高順上位100=百名山の主峰29+百名山の別峰4(北穂高岳・前穂高岳・鳳凰山の
  薬師岳/地蔵岳)+二百名山8+三百名山6+どのリストにも無い53(涸沢岳・大喰岳・中岳・南岳・西穂高岳・双六岳…)。
  全て中部山岳。百高山だけを選んだ時は「半数は登山地図に名の薄い脇峰」の注記を出す。

  双六岳は、百高山に含まれるのですね。
  そもそも、百高山を入れたかったのは、中部山岳地帯の北中南アルプルの山岳が網羅されたいからですので、問題ないと思われます。

