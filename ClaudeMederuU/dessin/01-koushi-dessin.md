# Koushi(格子) — 記法のデッサン(第42ラウンド起草)

Markdownの表では書けない「結合セル・入れ子・入力要素つきの表」を、テキストのまま
リポジトリで管理できるようにする表記法。命名はたけちゃんさん(格子)。
記法の原案は第40ラウンドの依頼者サンプル、区切りの改良(`:`区切り・`.`終端)は第42ラウンドで確定。

-------------------------------------------------------------------------------
## 目的
-------------------------------------------------------------------------------

- デッサン(仕様書)のマトリックス表・チェックリストを**テキストで書き、diffで追え、
  HTMLの表現力(colspan/rowspan・入れ子・入力要素)で表示する**。
- チェックボックスの状態もテキスト=リポジトリで管理できる(進捗表・移行チェックリストに使える。
  MederuUの昇格判断チェックリストもこれで書く)。
- Markdown手書きの表(`|`区切り)は列がずれると読めなくなる。Koushiは1セル1行なので、
  行の追加・セルの編集がdiffに素直に出る。

-------------------------------------------------------------------------------
## 記法(依頼者原案+第42ラウンドの区切り確定)
-------------------------------------------------------------------------------

### 基本の3層

- 箇条書きの3層で表を書く: `t:`(table) -> `r:`(row) -> `c:`(cell)。
- **トークンは全て`.`で終端する**(第42ラウンド確定)。`.`の後がセルの内容。

```koushi
- t:1.
  - r:1.
    - c:1. A1
    - c:2. B1
    - c:3. C1
  - r:2.
    - c:1. A2
    - c:2. B2
    - c:3. C2
```

### スパン(結合セル)と寄せ — `:`で繋ぐ属性

- トークンは`:`で区切った属性の列。**x接頭=スパン、1文字=寄せ**。
  - `c:1:xc2.` ... 1列目からcolspan=2(横に2セル結合)
  - `c:3:xr2.` ... 3列目からrowspan=2(縦に2セル結合)
  - `c:2:c.` ... 水平寄せ(l=左 / c=中央 / r=右)
  - `c:2:m.` ... 垂直寄せ(t=上 / m=中央 / b=下)
- 【提案・要レビュー】属性は**順不同で連結可**とする: `c:1:xc2:c:m.`(結合+中央寄せ)。
  読み手は「xで始まる=スパン、1文字=寄せ」で迷わない。

### セルの内容

- 同じ行の`.`の後のテキスト(Markdown可)。
- 複数行・リスト・**入れ子のテーブル**は、セルの子の箇条書きとして書く:

```koushi
- t:3.
  - r:1.
    - c:1.
      - 1行目の内容
      - 2行目の内容
        - 要素にMarkdownが書ける
    - c:2. B1
  - r:2.
    - c:1.
      - t:4.
        - r:1.
          - c:1. 入れ子のテーブル
    - c:2. B2
```

- **input要素**も書ける(デッサンのUIモック・チェックリストに使う):
  **チェック状態がテキスト=リポジトリで管理される**(マトリックスチェックリストの核)。
  - `btn.`
    - `btn:"クリックしてね".` ... <input type="button" value="クリックしてね" />
  - `chb.`
    - `chb:on.` ... <input type="checkbox" checked />
  - `clr.`
    - `clr:"#ff0000".`... <input type="color" value="#ff0000" />
  - `dat.`
    - `dat:"2017-06-01".` ... <input type="date" value="2017-06-01" />
  - `dtl.`
    - `dtl:"2017-06-01T08:30".` ... <input type="datetime-local" value="2017-06-01T08:30" />
  - `eml.`
    - `eml:"sophie@example.com".` ... <input type="email" placeholder="sophie@example.com" />
  - `fil.`
    - `fil:multiple.` ... <input type="file" multiple />
  - `hdn.`
    - `hdn:"34657".` ... <input type="hidden" value="34657" />
  - `img.`
    - `img:"https://www.example.com/login.png".` ... <input type="image" src="https://www.example.com/login.png" />
  - `mnt.`
    - `mnt:"2001-06".` ... <input type="month" value="2001-06" />
  - `nmb.`
    - `nmb:"42".` ... <input type="number" value="42" />
  - `pwd.` ... <input type="password" />
  - `rad.`
    - `rad:on.` ... <input type="radio" checked />
  - `rng.`
    - `rng:"90".` ... <input type="range" value="90" />
  - `rst.`
    - `rst:"フォームをリセット".` ... <input type="reset" value="フォームをリセット" />
  - `sch.` ... <input type="search" />
  - `smt.`
    - `smt:"リクエストを送信".` ... <input type="submit" value="リクエストを送信" />
  - `tel.`
    - `tel:"123-4567-8901".` ... <input type="tel" placeholder="123-4567-8901" />
  - `txt.`
    - `txt:"小文字で一語で".` ... <input type="text" placeholder="小文字で一語で" />
  - `tim.`
    - `tim:"13:30".` ... <input type="time" value="13:30" />
  - `url.`
    - `url:"http://www.example.com".` ... <input type="url" placeholder="http://www.example.com" />
  - `wek.`
    - `wek:"2017-W01".` ... <input type="week" value="2017-W01" />

### 依頼者サンプル(第40ラウンド原案を確定記法で書き直したもの)

```koushi
- t:2.
  - r:1.
    - c:1:xc2. A1-B1
    - c:3:xr2. C1-C2
  - r:2.
    - c:1:xr2. A2-A3
    - c:2. B2
  - r:3.
    - c:2:xc2. B3-C3
```

-------------------------------------------------------------------------------
## レンダラ計画(Koushi->HTML 片方向)
-------------------------------------------------------------------------------

- **スモールスタート**: 単一関数 `koushiToHtml(text) -> html`(依存なしの素のJS)。
  Markdown中の````koushi`フェンスを見つけてHTMLの`<table>`へ置き換える。
- 実装順:
  1. パーサ: 箇条書きのインデント木 -> {table->rows->cells(attrs, 内容)}
  2. レンダラ: colspan/rowspan/寄せ(styleでなくclass出力=構造とスタイルの分離)
  3. `chb:on.`等の短縮形展開
  4. セル内Markdown(最小: 強調・コード・リンク。入れ子tは再帰)
- 検証は**ゴールデン方式**(入力->期待HTMLの標本を凍結。宙の辻の辞書ゴールデンと同じ作法)。
- 置き場所: MederuU本体の`tools/koushi.js`。それまでは宙の辻の`scratch/`で開発・実証する。
- 将来: VSCode拡張(プレビュー)。まず片方向レンダラが安定してから。

-------------------------------------------------------------------------------
## 第43ラウンドで決まったこと
-------------------------------------------------------------------------------

- **ヘッダの表現を採用**: 行ヘッダ`r:1:h.`・セル単位の行見出し`c:1:h.`(同じ規則)。
- **属性連結は順不同で連結可**(`c:1:xc2:c:m.`)。
- **`t:`/`r:`/`c:`の番号は、プログラムが表を描く時には無視する**(依頼者決定):
  行追加で番号がずれたままでも表示が崩れない=編集のしやすさ優先。番号は人が
  おおよその位置を把握するための目印で、振り直しは後々コマンドで行う。
- **表全体の属性(枠線・幅)は当面CSS任せ**(コアロジックが固まるまで)。
- **チェック状態の更新フローは当面「表示専用+テキスト手編集」**(書き戻しはOkuteの領分)。
- **input要素は3文字トークン22種**(依頼者起草・上の一覧): `chb:on.`のように
  `トークン[:値].`で書く。値は`"`括りで自由文字列(placeholder/value)。

### input 3文字トークンへのAIの意見(第43ラウンド)

- 「input typeを3文字にした既存の標準リスト」は**存在しない**(HTMLのtype名22種に略記の
  公式文化は無い)。一番近い既存文化は**Emmet**(エディタの省略記法)だが、Emmetは
  `input:t`(text)・`input:c`(checkbox)など**長さがバラバラの略記**で、体系としては不揃い。
  **全て3文字で揃える方が「1つの規則で読める」**ので、Koushi独自の22種で良いと思う
  (方言を無くす手段は「既存に合わせる」だけでなく「規則を1つにする」でも達成できる)。
  対応表(上の一覧)がデッサンにあるので、迷いは索引で解ける。
- 1つだけ提案: **`src.`(search)は再考の余地** — HTMLの`src`属性(画像の取得元。`img:`の値にも
  登場する)と同じ綴りで、読み手が一瞬迷う。`sch.`(search)への変更を提案したい
  (searchの慣用略はsrchで、3文字ならsch)。ほかの21種は衝突・紛れなしと確認した。
  - `sch.`に修正済み。

-------------------------------------------------------------------------------
## 第45ラウンド: レンダラ第1版を実装した(依頼者GO「進めてください」)
-------------------------------------------------------------------------------

- 置き場所: `ClaudeMederuU/tools/koushi.js`(依存なし・Node/ブラウザ両用)。
  検証: `node ClaudeMederuU/tools/koushi.test.js`(ゴールデン方式・13チェック)。
  標本: `koushi-golden.json`(このデッサンの実例10件を凍結。更新は`--update`+目視確認とセット)。
- 実装で確定した描画仕様(ゴールデンが正):
  - 寄せはclass出力のみ: `k-l/k-c/k-r`(水平)・`k-t/k-m/k-b`(垂直)。**style属性は出さない**
    (構造とスタイルの分離。「無いことのテスト」で保証)。
  - 見出し: `r:1:h.`=行内全セルth、`c:1:h.`=そのセルだけth。
  - セルの子: 素の行=`<div class="k-line">`、行の子=`<ul><li>`(畳み)、`t:`=入れ子table(再帰)、
    inputトークン=`<input …/>`(デッサンの対応表どおり。`"`値のエスケープは`\"`)。
  - 番号無視は実装済み(番号を振り直しても出力が同一、をテストで保証)。
  - **未知の属性は読み飛ばす**(前方互換: 将来PADと同様に`#指紋`属性が来ても古いレンダラが壊れない)。
- 残り(急がない): セル内Markdown(強調・コード・リンク)は実装計画の4番のまま未実装。
  VSCode拡張(プレビュー)はレンダラ安定後。

-------------------------------------------------------------------------------
## 第66ラウンド: VSCodeプレビュー拡張を実装した(依頼者GO「Koushiプレビュー拡張、GO」)
-------------------------------------------------------------------------------

- 動機(依頼者): PADより先にKoushiのプレビューを。宙の辻の品質確保のため、マトリックスCLや
  状態遷移表をMarkdown内の```koushiフェンスで書き、VSCodeで漏れなく・ダブりなく目視したい。
- 置き場所: `ClaudeMederuU/tools/vscode-koushi/`(拡張フォルダ一式+ビルド済みvsix)。
- 仕組み(回答その63の設計通り):
  - package.jsonの`markdown.markdownItPlugins`で標準Markdownプレビューのmarkdown-itへ介入し、
    fenceルールを差し替える。infoの先頭語が`koushi`(大文字小文字不問)のフェンスだけ
    `koushiToHtml`で描き、他のフェンスは元の描画へ委譲(ハイライトを壊さない)。
  - レンダラは`koushi.js`の**同梱コピー**(正は`tools/koushi.js`)。バイト一致のドリフト検査を
    `extension.test.js`に常設(ズレたらcp一発で同期)。
  - 見た目は`markdown.previewStyles`のCSS1枚(罫線・寄せ・入れ子。テーマ非依存の半透明グレー)。
  - 描画エラーは`<pre class="koushi-error">`へ閉じ込め、プレビュー全体を壊さない。
- 検証: `extension.test.js`=Node単体9チェック(擬似markdown-itでフェンス差し込み・委譲・
  エラー閉じ込め・ドリフト・package構造)。回帰の道具テスト3本目として常設(kaiki更新済み)。
  実機(VSCode本体での表示)の最終確認だけは依頼者のMacの目視(sample.md=CL/状態遷移表/
  チェックリストの3例+js素通しの1例)。
- 将来: PADレンダラも同じ拡張に相乗り予定(```padフェンス。指紋書式の確定後)。

-------------------------------------------------------------------------------
## 第67ラウンド: 罫線属性の追加+編集画面ハイライト(依頼者要望)
-------------------------------------------------------------------------------

- 動機(依頼者): 複雑な状態遷移表では、罫線の種類(二重線・実線・破線・点線)で
  グループの区切りが見分けられると分かりやすい。あわせて、編集画面でも```koushiフェンス内の
  `-`がMarkdownのリストと同じようにハイライトされてほしい。
- **罫線属性(記法の追加)**: 2文字属性 `bd`=二重・`bs`=太実線・`ba`=破線・`bo`=点線。
  - 置き場所で辺が決まる: **行(r:)に付けるとその行の上辺**・**セル(c:)に付けるとそのセルの左辺**。
    (例: `r:3:bs.`=3行目の上に太実線 / `c:2:c:bd.`=2列目の左に二重線+中央寄せ)
  - 既定の罫線は従来どおり細実線。出力はclassのみ(`k-bd`等。行はtrへ・セルはtd/thへ)で、
    見た目はプレビューCSS側の規則(styleを出さない原則は不変)。
  - 既存の1文字属性・xc/xrとの併記可。未知属性の読み飛ばし(前方互換)も不変。
  - ゴールデンに borders-row-cell を追加(既存ケースの出力は不変=互換を差分ゼロで確認済み)。
- **編集画面ハイライト**: 拡張(v0.2.0)にTextMate文法2本を追加。
  - `syntaxes/koushi-markdown.tmLanguage.json`: Markdownの```koushiフェンスへ注入(定跡の型)。
  - `syntaxes/koushi.tmLanguage.json`: `-`はMarkdownのリストと**同じスコープ名**(=テーマの同じ色)・
    t:/r:/c:はタグ色・属性は属性色・input22種は関数色・"値"は文字列色。
  - VSCode本体はこちらで動かせないため、JSON妥当性+正規表現の挙動はNodeで検査し(extension.test.js⑥)、
    実際の色は依頼者の実機確認に委ねる。
- 実戦投入第1号: `docs/operation/myset-sync-states.md`(Myセットの状態遷移表3枚。依頼者の叩き台の
  再構成。罫線・結合セルを使用)。
