# anchor(錨) — 指紋のデッサン(第44ラウンド起草)

コード・文書の「この場所」を、行番号が腐っても・ファイルが引っ越しても指し続けられるようにする仕組み。
命名の心は「周りが変化しても、位置が変わらないもの」(依頼者)。
原案は第39〜41ラウンドの対話、パーマネントの本質の考察は第44ラウンドの依頼者メモから。

-------------------------------------------------------------------------------
## 原点(どんな問題があったから、これが欲しいか)
-------------------------------------------------------------------------------

- レビュー記録や学びから「コードのこの場所」を指したいが、**行番号は次の編集で腐る**。
  ファイルパスも移設(第43ラウンドのClaudeMederuU/移設のように)で変わる。
- リンク切れは「参照先が動いたのに、参照元が知らない」ことで起きる。
  欲しいのは、動いても追える錨。

-------------------------------------------------------------------------------
## 2つの指紋(第41ラウンドの整理)
-------------------------------------------------------------------------------

- **構造指紋 = 本人**(同一性)。
  プログラムを多分木(制御構造の木)で表し、ノード/ブランチ以下の部分木を正規化(空白・コメント除去)してハッシュを取る(Merkle木の要領)。
- **位置指紋 = 現住所**。
  ファイル+行内容+発行時刻から作る、人間とツールがジャンプするための住所。引っ越したら更新される方が正しい情報。
- 「引っ越しても本人は本人」。錨の本体は構造指紋に置く。

### 構造指紋が「変更に強い」理由(第44ラウンドの依頼者考察)

- 部分木のハッシュは、**内包しない部分木が変更されても変わらない**。
  位置指紋(ファイル全体が変わると変わる)より変更に強い。
- さらに、**ハッシュの更新の伝播が「変更の影響範囲」を教えてくれる**:
  子のハッシュが変わると親のハッシュも変わる(Merkleの性質)ので、根から変更ノードへの経路=変更の波及線。
  実行順序が木の辿り順と対応する言語なら、変更ノードまでの経路が「ここから先が影響を受けうる」の目安になる。

-------------------------------------------------------------------------------
## パーマネントの本質(第44ラウンドの依頼者考察を設計原則に)
-------------------------------------------------------------------------------

- 指紋そのものは、内容が変われば書き換わる。**指紋は永続ではない**。
- パーマネントにするのは指紋ではなく、**「このハッシュは、どのハッシュに書き変わりましたか」に答えられる仕組み**の方。
  問い合わせに答えが返るなら、指紋が変わっても追える=**リンクは実質パーマネントになる**。
- つまり anchor の本体は「指紋の発行」と「**指紋の系譜(書き換わりの履歴)**」の2つ。

### 履歴の担い手(3案の比較と採用案)

1. 参照元・参照先の両方を毎回更新する
   * リンク切れは消えるが、参照元が散らばるほど更新が漏れる(人は忘れる)。
2. ドキュメント自身が自分のハッシュ履歴を持つ
   * 他のドキュメントの負担は減るが、履歴が各所に分散する。
3. リポジトリが履歴を担う(問い合わせるとリポジトリが答える)
   * 正が1つ(MederuUのハブ思想と同じ)。
- **採用案 = 3を基本に、2を台帳で実装する折衷**:
  リポジトリに**追記型の台帳 `anchors.jsonl`**(1行=1つの書き換わりイベント: 旧指紋→新指紋・日時・コミット・理由)を置く。
  台帳は「1ファイル=履歴の正」(ドキュメントが持つ)でありつつ、リポジトリにあるので「リポジトリに問い合わせる」も満たす。
  追記型なので過去は書き換わらない(発行済みURL辞書の凍結・学びは重ねる、と同じ原則)。

### gitとの関係(車輪を再発明しない)

- 実は**gitが既にMerkle木のハッシュ履歴データベース**(コミット=断面のスナップショット、親リンク=系譜)。
  `git log -S`(内容検索)や`git log -L`(行範囲の追跡)で「内容の引っ越し」もある程度追える。
- anchorが足すのは**意味の層**: gitのハッシュは「ファイル・ツリー単位」だが、anchorは「制御構造の部分木単位」。
  「この関数のこのループ」に錨を打てるのはanchorだけ。gitは土台として使い、台帳のイベントにコミットIDを添えて両者を結ぶ。

-------------------------------------------------------------------------------
## ビューワー(第44ラウンドの質問「どんなスライスがあれば良いか」への設計)
-------------------------------------------------------------------------------

データ構造は**指紋の系譜DAG**(ノード=指紋、エッジ=書き換わりイベント)。これを3つの軸でスライスする:

1. **空間スライス(いまの木)**: あるコミット断面での、制御構造の木+各ノードの指紋。
   「どこに錨があるか」のツリービュー(=PADの木と同じ形! レンダラを共有できる)。
2. **時間スライス(1本の系譜)**: 1つの錨の「いつ、何に書き換わったか」の年表。
   各イベントに日時・コミット・(あれば)理由。**OKFのlog.md(変更の時系列履歴)と同じ思想**。
3. **差分スライス(変更の波及)**: 2つの断面の間で指紋が変わったノードだけを、根からの経路で表示。
   =「この変更の影響範囲」ビュー(依頼者考察の可視化)。

- 「リポジトリは断面しか持たないので、一つ前の指紋の履歴を持てば追跡できるか」への答え:
  **はい。各イベントが「旧指紋→新指紋」の1ホップを持てば、断面の列が鎖に変わります**(単方向リンクリストと同じ。gitのコミット親リンクと同型)。
  台帳が1ホップずつ記録し、ビューワーが鎖を辿って系譜を復元する、が役割分担。

-------------------------------------------------------------------------------
## PADとの接続(`d:#指紋.` の2本立ての意味の提案)
-------------------------------------------------------------------------------

- PADデッサン02の `pad:#a1b2c3d4-#A1B2C3D4.` の2本は、**構造指紋-位置指紋**の対と解釈するのを提案
  (前=本人・後=現住所。次のお茶で擦り合わせ)。
- 呼び出し側 `- d:#構造指紋. 名前` は本人だけを指す(引っ越しに強い参照)。
  ビューワーはそこから台帳を引いて現住所へジャンプする。

-------------------------------------------------------------------------------
## スモールスタート(実装順)
-------------------------------------------------------------------------------

1. [x] `anchor.js`(tools/): 波括弧の入れ子木+正規化テキストのハッシュ(第41ラウンド合意の最小版)。
   - 発行(print)・検証(verify: 不変/書き換わり)・検出(dup: 同一指紋の列挙=重複コード検出)
   - **第45ラウンドで実装した**(`ClaudeMederuU/tools/anchor.js`+性質テスト14チェック)。
     このコアは指紋書式(02の未決)と独立なので先行できた。PADテキストへの指紋の
     読み書き(`ref:`等)は書式確定後に足す。
   - **正規化v1の規則(凍結)**: コメント除去+空白の畳み込み(単語文字同士の間だけ1個残す)。
     変数名は吸収しない。規則を変える時は版を上げる(発行済み指紋の意味は変えない=辞書凍結と同じ)。
   - ハッシュはsha256・台帳にはフル・表示は先頭8桁(Gitの短縮表示に倣う)。
   - 初収穫: 宙の辻script.js=3754ノード、60文字以上の同一指紋グループ**92件**
     (最大は1184文字の`elevAtPix15`が2箇所)。リファクタリングB第2弾の実測データが先に手に入った。
2. [ ] 台帳 `anchors.jsonl` の追記(書き換わり検出時に旧→新のイベントを書く)
3. [ ] ビューワー(空間→時間→差分の順。PADレンダラと骨格共有)

-------------------------------------------------------------------------------
## 第45ラウンド: 決定事項と語彙の整理
-------------------------------------------------------------------------------

### 決定(依頼者回答)

- **指紋の桁数はGitに倣う**: 台帳にはフル、表示は先頭8桁の短縮(理由: Gitの上に構築するから。
  キャパシティ設計もGitの実績に乗る)。
- **台帳の「理由」フィールドは必須**(依頼者一票+AI賛成):
  gitがコミットメッセージを必須とするのと同じで、台帳の変更履歴をサマリーする時の目印になる。
- **正規化は空白・コメント除去のみ。変数名の違いは吸収しない**(依頼者判断: ASTは大変かも
  しれないので。簡単ならやる、だが方針は吸収しない)。
  - この決定の帰結: **名前はハッシュに含まれるので、変数名の変更は構造指紋が検出する**。
    「検出する/しない」の迷い(依頼者)はこれで解ける — 現方針では自然に「検出する」になる。
    純粋な構造だけ(名前を無視)を見たくなったら、将来「骨格指紋」(α正規化)を**種類として追加**
    すれば良い(指紋の種類は増やせる。既存の指紋の意味は変えない=辞書凍結と同じ作法)。

### 語彙の整理: 指紋は2種+住所1種(第45・AI提案)

第44までの「位置指紋」に2つの役割が同居していたので、名前を分けたい:

- **構造指紋(=本人)**: 正規化(空白・コメント除去)した部分木のハッシュ。参照(`ref:`)の対象。
- **生指紋(=姿)**: 正規化**しない**生テキストのハッシュ。
  依頼者の言う「空白・コメントの変更があった時に検出する役割」はこちら
  (住所では変更は検出できないので、役割に名前を付けた)。
- **住所(=現住所)**: ファイルパス+木の経路。**ハッシュではなく平文**で良い
  (人とツールがジャンプするための情報で、隠す理由が無い)。台帳のイベントに記録する。

どの変更に、どれが反応するか(Koushi記法・実戦投入第1号):

```koushi
- t:1.
  - r:1:h.
    - c:1. 変更の種類
    - c:2. 構造指紋
    - c:3. 生指紋
    - c:4. 住所
  - r:2.
    - c:1. 空白・コメントのみ
    - c:2:c. 不変
    - c:3:c. 変わる
    - c:4:c. 不変
  - r:3.
    - c:1. 変数名の変更
    - c:2:c. 変わる
    - c:3:c. 変わる
    - c:4:c. 不変
  - r:4.
    - c:1. 構造の変更
    - c:2:c. 変わる
    - c:3:c. 変わる
    - c:4:c. 不変
  - r:5.
    - c:1. 引っ越し(中身そのまま)
    - c:2:c. 不変
    - c:3:c. 不変
    - c:4:c. 変わる
```

- 3つが直交しているので、組み合わせで「何が起きたか」を言い当てられる
  (生指紋だけ変わった=見た目の整形だけ・構造も変わった=意味の変更・住所だけ変わった=引っ越し)。
- **生指紋(旧・位置指紋)の必要性への回答: 必要(依頼者+AI一致)**。
  gitのblobハッシュはファイル単位で、Markdown内のPADブロック単位の生ハッシュはgitに存在しない。
  VSCodeの更新マークはエディタの一時表示(永続せず、ブロック単位でもない)なので代替にならない。
- `pad:#a1b2c3d4-#A1B2C3D4.` の2本立ての解釈もこれで更新提案: **構造指紋-生指紋**の対
  (前=本人・後=姿)。住所は台帳に記録(第44の「構造指紋-位置指紋」提案を置き換える)。
  ノード(要素)に書くのは構造指紋だけ(02の第45節・提案cと同じ)。

### 台帳anchors.jsonlの競合問題(依頼者指摘)への設計案

- 指摘: 単一ファイルの台帳はデータベースになってしまい、読み書きの競合が起きる。
  多層化(キャッシュ・メインメモリ・ストレージ)のような仕組みを考えよう。
- AIの提案: **これもGitに倣う** — gitのloose objects→packfilesの2層と同じ形にする。
  - 書き込み層: 1イベント=1ファイル(`anchors/loose/<新指紋>.json`)。内容アドレスなので
    並行ブランチでも別ファイル=**マージ競合が構造的に起きない**(そもそも間違えない型)。
  - パック層: 定期的に`anchors/pack-YYYY-MM.jsonl`へまとめて追記(読み込みはパック+looseの2段)。
  - これは依頼者の言う多層化そのもの(書き込みは速く小さく、読み込みは大きくまとめて)。
- スモールスタートの判断: 単一ユーザー・単一エージェントの現状では、v1はjsonl1本で始めて良い。
  **追記型なので、多層化への移行も「追記先を変える」だけで済む**(間違っても大丈夫にする型)。
  移行のしきい値=マージ競合が実際に起きた時。

-------------------------------------------------------------------------------
## 未決事項(次のお茶の時間に)
-------------------------------------------------------------------------------

- 語彙の整理(構造指紋・生指紋・住所)と`#構造-#生`のヘッダ解釈で良いか(依頼者判断待ち)。
- `f-:`/`j+:`の`ref:`の意味(02の第45節の確認事項)。
- 台帳の形(v1=jsonl1本、多層化はloose/pack方式へ移行、で良いか)。
- ※anchor.jsのコア(発行・検証・検出)は第45で実装済み。PADテキストへの指紋の読み書きは
  上記の確定後に足す(コアは書式と独立)。
