# レビュー記録 2026-08-14 — 第89ラウンド差分(v16第2弾のデッサン叩き台)

依頼: 自走継続(第88の続き)。宣言済みの次=v16第2弾の叩き台起草(実装は承認後)。
デッサンのみのラウンド(アプリコード変更なし・版数据え置き)。

## 実施内容

デッサン00のURLパラメータ節に「組込天体の色/線種(第89ラウンド叩き台)」を起草。
- キーの形: bodyColor<ID>/bodyDash<ID>(天体毎キー=依頼者の承認済み方向)。
- 既定値のキーは省略(全既定なら44キーが0個=URL長不変)。
- My天体は対象外(starColor/starIsDashedで既に完全再現)。
- 非表示天体も既定から変えていれば発行(表示切替後の再現のため)。
- 省略キーの復元=復元時点の既定(既定変更に追従する設計を明記)。
- v17辞書は名前シード44個のみ(既定値ペア不要の理由も書いた)。
- 実装時の作法(リンターの配列リテラル発行・発行順固定)まで先に落とした。

## 学び(次の私へ)

- **「省略=既定」の設計では、第3規則(既定値ペア)の辞書シードが不要になる** — 省略される値は
  そもそもURLに現れないから。省略設計と辞書設計はセットで考えると、シード数が半分で済む。
- **叩き台には「実装時の作法」まで書く** — リンター対応(動的キー合成の禁止→配列リテラル)や
  発行順の固定は、承認後の実装ラウンドで自分が踏む地雷。設計時に思いついた注意は設計書に
  置いておくのが一番安い引き継ぎ(次の実装者=数ラウンド先の自分は忘れている)。
- **デッサンのみのラウンドは版数を上げない・回帰を再走しない** — アプリのコードが第88のまま
  なら直前の全PASSが有効。その旨を回答に明記して、検証の空白ではなく「変更なしの証明」に変える。
- **承認待ちの項目はtodoを2行に割る** — 「叩き台済み(承認待ち)」と「実装(承認後)」を別項目に
  すると、次のセッションが「どこで止まっているか」を一目で拾える。

## 検証結果

- アプリコード変更なし(第88のv1.72.0のまま)。直前ラウンドの全PASS(スモーク7本+
  verify140/143/145〜155+道具3本)が有効。
