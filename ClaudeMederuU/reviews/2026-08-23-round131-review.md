# レビュー記録 2026-08-23 — 第131ラウンド差分(「:○」常時オンへ確定 v1.87.0追補3)

依頼: 第130の連動方式への訂正 — 「認識がズレています。**○を常時オン**で大丈夫です」。
設計モデルの提示: 精度フィルタ=**精度の下限の範囲**(この機能は○精度までを扱う固定の枠)・
精度フィルタオプション=**精度の上限の範囲**(保持する精度を「選択精度以上」で決める。
○なら○◎◎×2◎×4等・◎なら◎◎×2◎×4等)。「この考え方で、いかがでしょうか」。

## 実施内容

1. **撤去** — _tmSyncSymODisplay関数と2箇所の呼び出し(復元時・ラジオ変更時)を削除。
   JSは「:○」表示に一切触らず、HTMLのchecked属性(◎・○オン)が常時の表示。
   状態キーtsujiMeshSymOは常時false固定のまま(URL/保存互換不変)。
2. **文言** — ツールチップを「読み取り専用・常時オン。この機能が扱う精度の下限は○(保持する
   範囲は精度フィルタオプションで選択)」へ(メニュー+結果パネル)。
3. **明文化** — デッサン04へ設計モデルを記載(73.精度フィルタ=下限の枠・78.オプション=上限の
   範囲・75.○=常時オン+分岐を持たない理由)。Version History 1.87.0行へ追補3。
4. **検証** — verify172 M5書き直し(オプション○/◎/◎×4切替でも両面オンのまま+連動関数の
   撤去確認+状態キー不変)。verify148 K4は意図コメントのみ更新(検証条件は両立)。

## 学び(次の私へ)

- **第130の学び「固定オンより導出」は過剰一般化だった** — [New]の導出原則(表示=状態からの導出)を、
  意味の違う表示に当てはめた。この「:○」は**現在の状態を映す表示ではなく、機能の仕様を示す
  固定の枠**(能書き・レンジ表示)。「導出で定義すべき表示」と「固定の枠として示す表示」は別物で、
  どちらなのかはコードからは決まらない。**依頼者の頭の中のモデルが仕様**。
- **決め手はデッサン全体との整合** — 常時オンの根拠は「見た目の好み」ではなく、コントロールの
  精度フィルタオプションで○を選べる設計との整合(検索は全精度=○で持ち、絞るのは表示側)。
  一つのUI片の意味を決める時は、その値を使う下流(今回はコントロールの表示切替)まで含めて
  矛盾しないかを見る。
- **同じ箇所で2回ズレたら、実装の前に相談** — 第130で「静的オフが意図?」→「連動?」と、
  2回とも実装で答えを出そうとしてズレた。「実装前に、なんでも相談していただいても大丈夫ですよ」を
  いただいた。解釈が2通り以上あると気づいた時点で、モデルを文章にして先にお見せする。
  相談は遠回りではなく、このラウンドのような手戻り(実装3回)より速い。
