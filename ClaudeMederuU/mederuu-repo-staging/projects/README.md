# projects/ — プロジェクト毎の文脈つき記録(写し)

各プロジェクトの`ClaudeMederuU/`(+`.claude/skills/`)から、sync.js(手順5・未実装)が
一方向で写す場所。**ここは写しであり、正は各プロジェクト側**。手で編集しない。

## 約束

- 吸い上げ対象は各プロジェクトの`ClaudeMederuU/`フォルダ一式と`.claude/skills/`だけ
  (パス表の管理を不要にするための場所規約)。
- sync.jsには秘密検査(鍵・トークン・個人情報のパターンフィルタ)を仕込む。
- 最初の住人は`soranotsuji/`(宙の辻)になる予定。

まだ空。sync.jsの実装(手順5)とセットで最初の吸い上げを行う。
