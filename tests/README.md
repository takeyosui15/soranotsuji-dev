# tests/ — ヘッドレスブラウザ検証スイート(ナレッジ)

Claude(AI)が各開発ラウンドで実行している検証スクリプトの保管場所です。
`docs/order.md` の「検証結果」に対応する実体で、ラウンド毎に `verifyNN.js` を追加します。
(旧来のPlaywright E2E一式は `test-initial/` に移動しました)

## テスト方針(2026-07-19 決定)

- **テストは原則ローカル環境内で完結させる**(CDNはローカルvendor・APIはモック)。
  理由: テストスクリプトの実行で相手側サーバーに負荷をかけないため。
- **実ネットワーク検証(verify102系)は常用しない。**アプリに問題が出た時に
  「ローカル環境との環境差かどうか」を切り分ける目的でのみ、単発で実行する。

## 一覧

| ファイル | ラウンド | 内容 |
|---|---|---|
| `verify96.js` | 第8 | 焦点距離初期値24・File取得ボタン・月輝面比列・辻検索File取得E2E(65列CSV)・Myセット4シート存在確認(タブ削除→読込/保存で自動作成) |
| `verify97.js` | 第8 | 天体検索/登録メニュー改修・全天儀ctrl(開閉1/2分割・基本オプション/日時連動・速度ボタン委譲)・フレーム同期録画(媒体時間=コマ数/fps・再生可) |
| `verify98.js` | 第9〜10 | 全天儀ctrl2〜4段目(前景/後景クリップ面・透過=不透明両面描画の視覚検証・回転モードの軸不変量・北奥/目的点前リセット)・最大化サイズ規則5パターン・辻メッシュFile取得の全画素CSV(観測点ID=画素ID・行内日時順)・🕛アニメ・CSV回帰 |
| `verify100.js` | 第14 | 宙検索フェーズ1(メニューUI・扇形標本の格子丸め/重み・スコア関数の性質[快晴/曇天/月の避狙反転/対象高度]・Open-MeteoモックE2E[バッチ1コール/キャッシュ0コール/best_match延長/信頼度列]) |
| `verify101.js` | 第19 | 宙検索の光害組み込み(実アセット読込960×660・東京/山間/範囲外の参照・方向光害・光害因子[暗い空=高得点/都市=減点/lp省略の後方互換/重み0は不影響]・列表示) |
| `verify99.js` | 第11〜12 | 花火モード(UI30要素・号数仕様表示・メニュー/ctrl双方向連動・固定でばらつき0・打ち上げ点設定と地図マーカー・アニメーション起動と実寸ENU・ばらつき分布±100/0・URL記憶+短縮URL v9往復・OFF後始末) |
| `verify102.js` | 第20 | 実ネットワークE2E(ネットワーク許可後の初回。素のindex.html=CDN書き換え無しで、実CDN[unpkg/jsDelivr/sourceforge]・実地理院タイル/標高/地名検索・実Open-Meteoの宙検索[富士山麓14件・実SQM]・キャッシュ0コール・カウンター遮断の確認)。**常用しない**(テスト方針参照) |
| `verify103.js` | 第21 | 宙検索フェーズ2(メニュー拡張[重み8+ラジオ2組+時間帯4+File取得]・プリセット連動と自動カスタム・専用パネル18列・ソート▲▼・行クリック[日時移動+詳細リスト+地図扇形/標本点の雲量着色]・排他[辻検索⇄宙検索]・CSV44列BOM・URL v10以降往復。ローカル完結=route abortで外部遮断) |
| `verify104.js` | 第22 | 宙検索フェーズ3残り(雲海度の推定[雲頂570m×観測点1500m=90/雲中=0/データなし=null]・AOD霞み因子と雲海の狙う/避ける・E2Eで雲海度90%/透明度0%列・詳細の気圧面プロファイル+上空風+AOD・ERA5統計行[晴天率50%・10年10コール→キャッシュ0・クリック不動作]) |
| `verify105.js` | 第22 | 宙検索フェーズ4=My宙検索(メニュー14要素・宙検索取得でMy観測点/目的点自動作成・行編集[カスタム化で重み取り込み]・一括計算[20列・複数エントリ・行クリックで行のスナップショット詳細/地図]・一括CSV51列・リストCSV22列往復・一括選択/全て登録の参照検証) |
| `verify106.js` | 第23 | MyセットにMy宙検索を追加=5シート構成(シート行22列・旧4シートへの保存でタブ自動追加・読込往復[カスタム重み/時間帯/範囲・行末空セル落ち耐性]・セット切り替え[旧スナップショット安全適用]。Drive/Sheetsはインメモリエミュレータ) |
| `verify107.js` | 第23 | 宙断面ビュー骨格=MapLibre初導入(読込・開閉・排他[全天儀/宙の窓⇄宙断面]・terrain[gsidem変換]設定・雲スラブ81格子×3層=243面[base/top=雲量比例×誇張1.2。第24で細分化に保守]・日時変更の時単位追従・✕でマップ破棄) |
| `verify108.js` | 第24 | 宙断面ビュー肉付け(⛶最大化+結果パネル併用の積み上げ・細メッシュ0.1°×9×9・時間スライダー[+12h/10分刻み/線形補間の値検証0→30→60%]・▶アニメ再生/停止・気圧面輪切り[16面1コール・気象庁配色・実高度±120m・凡例]) |
| `verify109.js` | 第25 | 本体地図MapLibre移行R1(フラグOFF=Leaflet不変[エンジン/アダプタ値/setView]・フラグON=?maplibre=1[本体+シャドウ+バッジ・ベース4種切替・ズーム換算±1・ズーム/パン/⌖/recenterのoffset向き・ダブルクリック地点移動・シャドウ視野追従]。B8はR2でマーカー移行済みの表明に保守) |
| `verify110.js` | 第26 | 本体地図MapLibre移行R2=マーカー/ポップアップ(観測点/目的点マーカー+位置ポップアップ・2本線[大圏101点]・My観測点/目的点のポップアップとクリック適用・🎆+範囲円65点リングのON/OFF・地点移動の追従・フラグOFFはLeafletのまま) |
| `verify111.js` | 第27 | 本体地図MapLibre移行R3=辻ライン/方位線(方位線=表示天体数/始点=観測点/不透明度0.3|1.0・辻ライン実計算でdash5種+時刻点/ラベル141個同数・OFFで全消去・辻ライン365のセグメント分割/表示切替/全消去・フラグOFFはlinesLayerのまま) |
| `verify112.js` | 第28 | 本体地図MapLibre移行R4=宙検索オーバーレイ(視界扇形=閉ポリゴン・標本点数一致/全点白・行選択で雲20%→rgb(215,215,215)着色+層別雲量ツールチップ・実マウスホバーでPopup表示/離脱で消去・パネル✕で全消去。LeafletオーバーレイはVerify103が担当) |
| `verify113.js` | 第29 | 本体地図MapLibre移行R5=辻メッシュ(合成4画素でメッシュ画像の四隅=bounds[updateImage+setCoordinates]・表示切替・金ドット3画素の実クリックで観測点設定+一般クリック抑止・辻マーカー画像+優辻ピンのポップアップ→内容クリック移動・画素クリックのメッシュ件数ポップアップ+詳細固定/解除・ホバー精細化ツールチップ・消去・フラグOFFのLeaflet同機能スモーク) |
| `verify114.js` | 第30 | 本体地図MapLibre移行R6=既定切替(フラグ無し=MapLibre[マーカー/コントロール/バッジ無し]・?maplibre=0=旧Leaflet・?maplibre=1=明示MapLibre・エンジンを跨いだlocalStorage状態の互換[両方向]・共有URLにフラグが乗らない) |
| `verify115.js` | 第31 | 辻ライン365の描画クラッシュ修正(Macのエラーコード5)=実測規模51万点(827+581点/日×365日)の合成流し込みで全再構築setData0回・updateData増分1〜3回のみ・730feature/513,920点の反映・表示切替は切替毎に再構築1回・全消去・ページエラーなし |
| `verify116.js` | 第32 | 地図全面移行計画 完了(手順4=Leaflet撤去)=ソースにleafletタグ/L.*なし・実行環境にLグローバル/シャドウ/leaflet-DOMなし・_geoDistM凍結値(東京→大阪403.06km)・.gl-barスタイル移植・主要操作一連(地点移動/recenter/辻ライン/宙断面開閉)・旧フラグ無視 |
| `verify117.js` | 第33 | 予測系機能の一時封鎖+アイコン修正(既定=封鎖UIがDOMに存在しない[第34でHTML分離に保守]・実行ガード・Myセット4シート・Myセット説明文の修正・リリース対象の入り口は表示・⌖/レイヤアイコンの中央±1.5px実測・?forecast=1で3機能表示+5シート+宙断面が開く) |
| `verify118.js` | 第34 | 封鎖UIのindex.html物理削除+ボタンサイズ統一(index.htmlに封鎖要素なし+スロット5個・forecast-features.htmlに5ブロック退避・既定DOMに要素なし/空スロット・左上コントロール8ボタン全て29px正方・?forecast=1でfetch注入復元+配線+宙断面が開く+Myセット説明復帰) |
| `verify123.js` | 第39 | 保存キー整合性リンター(静的: 保存157⇄復元156キーの相互検証+正規化基準線。対象ファイル引数で他プロジェクト再利用可)+Drive保存のsoranotsuji_プレフィックス限定・バックアップ生ダンプ形式一本化・書き戻しフィルタ(混入キーの掃除)のブラウザ検査 |
| `verify124.js` | 第40 | URL短縮辞書v12の2種規則リント(「&キー名=」+「キー値」の形状検査・ビルダー出力148キーとの相互網羅・壊した辞書での自己テスト)+発行済みv11短縮URLの凍結標本復号(`tests/data/qp-v11-sample.json`)+スマホ幅でレイヤーリスト縦4行・短縮URLの5ケース往復(位置情報の日時あり/なし・宙の窓・辻検索・辻メッシュ)・短縮URLからの状態復元(第41ラウンドで版数非依存へ一般化) |
| `verify125.js` | 第41 | URL短縮辞書v13(第3規則「&キー名=既定値」)の静的検査: ペア127個の形状・既知キー由来・シード合成+ゴールデン標本のバイト一致(`tests/data/qp-v13-golden.json`=辞書凍結とエンコーダ決定性)+v12発行標本の復号(`qp-v12-sample.json`)+効果の下限保証(v12比60%未満)。壊した版での自己テスト2種付き。ブラウザ不要 |
| `verify128.js` | 第50 | PLATEAU建物レイヤ「都市モード」(v1.45.0): 合成Dracoフィクスチャ(`tests/data/plateau-fixture/`=既知座標・既知高さの箱ビル2棟。実データと同じb3dm→glb→Draco+CESIUM_RTC構造。gen-fixture.jsで生成・routeモックで配信)による変換連鎖の数値表明 — 建物頂点がアプリ基準`calculateApparentAltitude`の位置と±0.15m(実測±1mm)で一致・ジオイド補正・高さスパン/ENU配置。ほかに初期値オン(依頼者決定)・メニュー/ctrl双方向連動・テクスチャON/OFF再構築・扇の向き追従・整備都市外表示・localStorage保存。Dracoデコーダはvendor/draco/(sync-apptestが書き換え)。第51で対応表検査をverify129へ移設 |
| `verify129.js` | 第51 | 都市モードの全国化(v1.46.0): 全国対応表`data/plateau-bldg-cities.json`(tools/plateau/make-bldg-cities.jsで生成・448都市ファミリ)の静的リント(base=公式配信・code5桁・bbox日本域・変種≧1・URL形状・重複なし・都庁∈新宿区/皇居∈千代田区)+LODフォールバック配線(tex: lod2Tex→lod2NoTex→lod1/notex: lod2NoTex→lod1→lod2Tex)+ブラウザで_smBldgLoadCitiesの絶対URL化と都庁10kmのbboxフィルタ(8区市ヒット)を実測 |
| `verify130.js` | 第52 | 都市モードの遠景対応+夜馴染み(v1.47.0): 観測点初期値=東京タワーの建物の外(35.658595, 139.745335。URL短縮辞書v13は凍結のまま=座標ペア不在の表明つき)・奥行き上限200km・太陽高度減光の純関数(昼1.0/夜0.26床+青寄り/薄明中間)・タイル選択スコア=見かけの高さ順(高層@65km>低い街@5km・50mクランプ)・都市の扇足切り(中=真/真後ろ=偽/扇内遠方=真)・フィクスチャで正午→真夜中の材質色追従(幾何は同一参照のまま)。初期値変更で導出6キー(soraBaseAz等)の凍結標本を更新(verify126のN2が変更を検知→意図的更新の手順どおり) |
| `verify126.js` | 第41 | リファクタリングA(APP_DEFAULTS単一情報源化)の挙動不変の表明: 初期状態のappStateスカラー161キーが凍結標本(`tests/data/appstate-defaults.json`)と一致+normalizeAppStateの挙動が凍結標本(`normalize-probes.json`=161キー×8種の崩れた値)と一致+表の内部整合リント+旧h265→h264移行+HTML入力欄の起動時上書き実測。既定値・正規化規則を意図的に変える時だけ標本を更新する |
| `verify127.js` | 第42 | リファクタリングB第1弾(ファイル入出力の型)の無いことのテスト: ファイル選択の殻=pickTextFileの1箇所のみ・FileReader=1箇所・createObjectURL=4箇所(テキストDL/宙の窓DL/動画src/画像src)・CSV行分割正規表現=splitCsvLinesの1箇所のみ+CSV出力/入力の端到端(ファイル名/BOMバイト/コメント行スキップ) |
| `verify122.js` | 第38 | 検索ボックスの正規化規則(英字入力は無変換のままOSMでヒット・日本語入力は従来どおり全角化[+0xFEE0写像で期待値生成]・数値のみはOSMへ問い合わせず検索スキップ=郵便番号ジャンプ防止の維持。GSI/OSMはrouteモック) |
| `verify121.js` | 第37 | 海外対応+URLセッション限定化+懸案対応(URLで開いたセッションは変わったキーだけ凍結し再訪で自分の保存値へ・表示中セット編集で行アイコン即👎・Drive指紋=アップロード内容・花火ラベル金字・モックTerrariumで海外のスポット標高/標高グラフ/可視判定+域外GSIリクエスト0件+国内海は従来どおり・タイル取得タイムアウト+失敗ネガティブキャッシュ) |
| `verify120.js` | 第36 | 後日課題全実施+整合性修正(時刻ラベルsymbolレイヤ[DOM 0個+ローカルglyphs取得]・標準ピン+観測点/目的点マーカー永続化+ポップアップ維持・花火設定の復元・指紋の簿記非依存・[New]=前回同期からの変更・空データ上書きガード・封鎖中のMy宙検索温存・辻検索URLの基準値保護・期間clamp・閉じるボタン5種・モバイルワーカー上限) |
| `verify119.js` | 第35 | スマホ辻メッシュ不具合修正(全検索エリアでキャンバス最大辺2048px以下[旧3×3=6144/5×5=5120]・再検索3回とも毎回visible+旧objectURLのrevoke・実チェックボックスOFF→ONの再描画なし復帰・attributionのⓘ[compact]・setDataが天体数に依らずリセット+一括の2回)+監査第2弾v1.37.1(タイルキャッシュLRU上限の追い出し実測・優辻ピンmouseenterのタッチガード+ツールチップoffset毎回設定・検索完了後のワーカープール解放[ソース検査])+第3弾v1.37.2(画像ロード失敗の安全網=フラグ解除+ステータス表示+再描画復帰・クレジット重複解消・表示天体OFF→ONの辻マーカー画像復帰) |

## 道具のテスト(ClaudeMederuU/tools/。回帰と一緒に回す)

MederuUの道具のテストは道具と同じ場所に置きます(将来MederuU本体へ道具・テスト・標本の三点セットで引っ越すため。第45ラウンド〜)。
サーバ・Playwright不要(node単体)なので、回帰のついでに次の2本を実行します:

- `node ClaudeMederuU/tools/koushi.test.js` — Koushiレンダラのゴールデン13チェック(標本=koushi-golden.json。更新は`--update`+目視確認)
- `node ClaudeMederuU/tools/anchor.test.js` — 構造指紋の性質テスト14チェック(整形/コメント不変・改名検出・文字列/テンプレート/正規表現の波括弧・実物script.jsスモーク)

APP_VERSIONピンの規約(最新verifyのみ)はアプリのverify群だけの話で、道具のテストは対象外です。

## 実行方法(ローカルハーネス)

ネットワーク遮断環境でも動くよう、CDNのライブラリをローカルに置いた複製ディレクトリをHTTPで配信して実行します。

1. 作業ディレクトリ(例 `apptest/`)にリポジトリのアプリ一式(`index.html` `script.js` `style.css` 各ワーカー)をコピー
2. `vendor/` に以下を配置し、`index.html`とワーカーのCDN参照をvendorパスへ書き換える
   - astronomy-engine 2.1.19 (`astronomy.browser.min.js`) / three 0.160.0 (`three.min.js`)
   - geographiclib-geodesic / geographiclib-dms
   - maplibre-gl 4.7.1 (`maplibre-gl.js` `maplibre-gl.css`) (いずれもnpmから取得可)
   - ※leaflet 1.9.4は第32ラウンド(手順4)の撤去に伴い不要になった(verify102の実ネットワーク検証を旧版で行う場合のみ歴史的に必要)
3. `python3 -m http.server 8099 --bind 127.0.0.1` で配信
4. `node tests/verify99.js` のように実行(要 `playwright-core` とChromium。CIやサンドボックスでは
   `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader` でWebGLをソフトウェア実行)

- Google Drive/Sheets を使うテスト(verify96のT3等)は `window.fetch` をテスト内でモックしています(実アカウント不要)
- 全verifyはPlaywrightのroute abortでローカル(127.0.0.1)以外への実アクセスを遮断しています(第22ラウンドから。テスト方針の仕組み化)
- APP_VERSIONの版数ピンは**最新のverifyのみ**に置きます(第24ラウンド〜。旧verifyのピンを毎回更新する保守を廃止。現在のピンはverify127)
- 第36ラウンドで辻メッシュ画像はcanvasソース化(同期反映)・時刻ラベルはsymbolレイヤ化(ローカルglyphs: `fonts/SoraSans/0-255.pbf`。ハーネスへfonts/の同期が必要)・地点ピンは標準maplibregl.Marker化(識別クラスは要素に残る)。verify119の一部(旧objectURL表明)とverify111/113は実挙動へ保守済み
- 第33ラウンドの機能封鎖により、封鎖対象を検証するverify(100/101/103/104/105/106/107/108/112/116)は`?forecast=1`を付けて実行します(封鎖の検証はverify117/118)。第34ラウンドで封鎖UIはindex.htmlからforecast-features.htmlへ分離され、?forecast=1時にfetch注入で復元されます(ハーネスへ同ファイルの同期が必要)
- 第32ラウンド(手順4=Leaflet撤去)で、全verifyがMapLibre上で走ります。Leaflet内部を検査していた3本(99/103/105)はGLソース/DOMの等価チェックへ移植済み。`?maplibre=0/1`は無視されます
- 視覚検証・実時間アニメ・実マウス操作系のverify(97/98/99/113など)は、実行負荷でまれに1〜2チェック落ちることがあります(単独再実行でPASSすればフレーク。頻発時は待ち時間を延長する。verify113のドットクリックは描画完了のポーリング待ちで対策済み)
- 判定は `PASS/FAIL` を標準出力に出し、FAILが1つでもあれば終了コード1

## 実行方法(実ネットワークハーネス。verify102以降)

ネットワーク許可済みの環境では、リポジトリ直下をそのまま配信して素のindex.html(CDN書き換え無し)で検証できます。
Claude Codeクラウド環境ではエージェントプロキシの都合で、ChromiumにプロキシCAのNSS登録と
`--proxy-server=$HTTPS_PROXY` `--proxy-bypass-list=<local>` `--ssl-version-max=tls1.2` が必要です
(詳細は `verify102.js` 冒頭のコメントを参照。TLS証明書検証は有効のままです)。
