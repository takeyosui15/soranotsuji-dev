# tests/ — ヘッドレスブラウザ検証スイート(ナレッジ)

Claude(AI)が各開発ラウンドで実行している検証スクリプトの保管場所です。
`docs/order.md` の「検証結果」に対応する実体で、ラウンド毎に `verifyNN.js` を追加します。
(旧来のPlaywright E2E一式は `test-initial/` に移動しました)

## テスト方針(2026-07-19 決定)

- **テストは原則ローカル環境内で完結させる**(CDNはローカルvendor・APIはモック)。
  理由: テストスクリプトの実行で相手側サーバーに負荷をかけないため。
- **実ネットワーク検証(verify102系)は常用しない。**アプリに問題が出た時に
  「ローカル環境との環境差かどうか」を切り分ける目的でのみ、単発で実行する。

## 一覧

| ファイル | ラウンド | 内容 |
|---|---|---|
| `verify96.js` | 第8 | 焦点距離初期値24・File取得ボタン・月輝面比列・辻検索File取得E2E(65列CSV)・Myセット4シート存在確認(タブ削除→読込/保存で自動作成) |
| `verify97.js` | 第8 | 天体検索/登録メニュー改修・全天儀ctrl(開閉1/2分割・基本オプション/日時連動・速度ボタン委譲)・フレーム同期録画(媒体時間=コマ数/fps・再生可) |
| `verify98.js` | 第9〜10 | 全天儀ctrl2〜4段目(前景/後景クリップ面・透過=不透明両面描画の視覚検証・回転モードの軸不変量・北奥/目的点前リセット)・最大化サイズ規則5パターン・辻メッシュFile取得の全画素CSV(観測点ID=画素ID・行内日時順)・🕛アニメ・CSV回帰 |
| `verify100.js` | 第14 | 宙検索フェーズ1(メニューUI・扇形標本の格子丸め/重み・スコア関数の性質[快晴/曇天/月の避狙反転/対象高度]・Open-MeteoモックE2E[バッチ1コール/キャッシュ0コール/best_match延長/信頼度列]) |
| `verify101.js` | 第19 | 宙検索の光害組み込み(実アセット読込960×660・東京/山間/範囲外の参照・方向光害・光害因子[暗い空=高得点/都市=減点/lp省略の後方互換/重み0は不影響]・列表示) |
| `verify99.js` | 第11〜12 | 花火モード(UI30要素・号数仕様表示・メニュー/ctrl双方向連動・固定でばらつき0・打ち上げ点設定と地図マーカー・アニメーション起動と実寸ENU・ばらつき分布±100/0・URL記憶+短縮URL v9往復・OFF後始末) |
| `verify102.js` | 第20 | 実ネットワークE2E(ネットワーク許可後の初回。素のindex.html=CDN書き換え無しで、実CDN[unpkg/jsDelivr/sourceforge]・実地理院タイル/標高/地名検索・実Open-Meteoの宙検索[富士山麓14件・実SQM]・キャッシュ0コール・カウンター遮断の確認)。**常用しない**(テスト方針参照) |
| `verify103.js` | 第21 | 宙検索フェーズ2(メニュー拡張[重み8+ラジオ2組+時間帯4+File取得]・プリセット連動と自動カスタム・専用パネル18列・ソート▲▼・行クリック[日時移動+詳細リスト+地図扇形/標本点の雲量着色]・排他[辻検索⇄宙検索]・CSV44列BOM・URL v10以降往復。ローカル完結=route abortで外部遮断) |
| `verify104.js` | 第22 | 宙検索フェーズ3残り(雲海度の推定[雲頂570m×観測点1500m=90/雲中=0/データなし=null]・AOD霞み因子と雲海の狙う/避ける・E2Eで雲海度90%/透明度0%列・詳細の気圧面プロファイル+上空風+AOD・ERA5統計行[晴天率50%・10年10コール→キャッシュ0・クリック不動作]) |
| `verify105.js` | 第22 | 宙検索フェーズ4=My宙検索(メニュー14要素・宙検索取得でMy観測点/目的点自動作成・行編集[カスタム化で重み取り込み]・一括計算[20列・複数エントリ・行クリックで行のスナップショット詳細/地図]・一括CSV51列・リストCSV22列往復・一括選択/全て登録の参照検証) |
| `verify106.js` | 第23 | MyセットにMy宙検索を追加=5シート構成(シート行22列・旧4シートへの保存でタブ自動追加・読込往復[カスタム重み/時間帯/範囲・行末空セル落ち耐性]・セット切り替え[旧スナップショット安全適用]。Drive/Sheetsはインメモリエミュレータ) |
| `verify107.js` | 第23 | 宙断面ビュー骨格=MapLibre初導入(読込・開閉・排他[全天儀/宙の窓⇄宙断面]・terrain[gsidem変換]設定・雲スラブ81格子×3層=243面[base/top=雲量比例×誇張1.2。第24で細分化に保守]・日時変更の時単位追従・✕でマップ破棄) |
| `verify108.js` | 第24 | 宙断面ビュー肉付け(⛶最大化+結果パネル併用の積み上げ・細メッシュ0.1°×9×9・時間スライダー[+12h/10分刻み/線形補間の値検証0→30→60%]・▶アニメ再生/停止・気圧面輪切り[16面1コール・気象庁配色・実高度±120m・凡例]) |
| `verify109.js` | 第25 | 本体地図MapLibre移行R1(フラグOFF=Leaflet不変[エンジン/アダプタ値/setView]・フラグON=?maplibre=1[本体+シャドウ+バッジ・ベース4種切替・ズーム換算±1・ズーム/パン/⌖/recenterのoffset向き・ダブルクリック地点移動・シャドウ視野追従]。B8はR2でマーカー移行済みの表明に保守) |
| `verify110.js` | 第26 | 本体地図MapLibre移行R2=マーカー/ポップアップ(観測点/目的点マーカー+位置ポップアップ・2本線[大圏101点]・My観測点/目的点のポップアップとクリック適用・🎆+範囲円65点リングのON/OFF・地点移動の追従・フラグOFFはLeafletのまま) |
| `verify111.js` | 第27 | 本体地図MapLibre移行R3=辻ライン/方位線(方位線=表示天体数/始点=観測点/不透明度0.3|1.0・辻ライン実計算でdash5種+時刻点/ラベル141個同数・OFFで全消去・辻ライン365のセグメント分割/表示切替/全消去・フラグOFFはlinesLayerのまま) |
| `verify112.js` | 第28 | 本体地図MapLibre移行R4=宙検索オーバーレイ(視界扇形=閉ポリゴン・標本点数一致/全点白・行選択で雲20%→rgb(215,215,215)着色+層別雲量ツールチップ・実マウスホバーでPopup表示/離脱で消去・パネル✕で全消去。LeafletオーバーレイはVerify103が担当) |
| `verify113.js` | 第29 | 本体地図MapLibre移行R5=辻メッシュ(合成4画素でメッシュ画像の四隅=bounds[updateImage+setCoordinates]・表示切替・金ドット3画素の実クリックで観測点設定+一般クリック抑止・辻マーカー画像+優辻ピンのポップアップ→内容クリック移動・画素クリックのメッシュ件数ポップアップ+詳細固定/解除・ホバー精細化ツールチップ・消去・フラグOFFのLeaflet同機能スモーク) |
| `verify114.js` | 第30 | 本体地図MapLibre移行R6=既定切替(フラグ無し=MapLibre[マーカー/コントロール/バッジ無し]・?maplibre=0=旧Leaflet・?maplibre=1=明示MapLibre・エンジンを跨いだlocalStorage状態の互換[両方向]・共有URLにフラグが乗らない) |
| `verify115.js` | 第31 | 辻ライン365の描画クラッシュ修正(Macのエラーコード5)=実測規模51万点(827+581点/日×365日)の合成流し込みで全再構築setData0回・updateData増分1〜3回のみ・730feature/513,920点の反映・表示切替は切替毎に再構築1回・全消去・ページエラーなし |
| `verify116.js` | 第32 | 地図全面移行計画 完了(手順4=Leaflet撤去)=ソースにleafletタグ/L.*なし・実行環境にLグローバル/シャドウ/leaflet-DOMなし・_geoDistM凍結値(東京→大阪403.06km)・.gl-barスタイル移植・主要操作一連(地点移動/recenter/辻ライン/宙断面開閉)・旧フラグ無視 |
| `verify117.js` | 第33 | 予測系機能の一時封鎖+アイコン修正(既定=封鎖UIがDOMに存在しない[第34でHTML分離に保守]・実行ガード・Myセット4シート・Myセット説明文の修正・リリース対象の入り口は表示・⌖/レイヤアイコンの中央±1.5px実測・?forecast=1で3機能表示+5シート+宙断面が開く) |
| `verify118.js` | 第34 | 封鎖UIのindex.html物理削除+ボタンサイズ統一(index.htmlに封鎖要素なし+スロット5個・forecast-features.htmlに5ブロック退避・既定DOMに要素なし/空スロット・左上コントロール8ボタン全て29px正方・?forecast=1でfetch注入復元+配線+宙断面が開く+Myセット説明復帰) |
| `verify123.js` | 第39 | 保存キー整合性リンター(静的: 保存157⇄復元156キーの相互検証+正規化基準線。対象ファイル引数で他プロジェクト再利用可)+Drive保存のsoranotsuji_プレフィックス限定・バックアップ生ダンプ形式一本化・書き戻しフィルタ(混入キーの掃除)のブラウザ検査 |
| `verify124.js` | 第40 | URL短縮辞書v12の2種規則リント(「&キー名=」+「キー値」の形状検査・ビルダー出力148キーとの相互網羅・壊した辞書での自己テスト)+発行済みv11短縮URLの凍結標本復号(`tests/data/qp-v11-sample.json`)+スマホ幅でレイヤーリスト縦4行・短縮URLの5ケース往復(位置情報の日時あり/なし・宙の窓・辻検索・辻メッシュ)・短縮URLからの状態復元(第41ラウンドで版数非依存へ一般化) |
| `verify125.js` | 第41 | URL短縮辞書v13(第3規則「&キー名=既定値」)の静的検査: ペア127個の形状・既知キー由来・シード合成+ゴールデン標本のバイト一致(`tests/data/qp-v13-golden.json`=辞書凍結とエンコーダ決定性)+v12発行標本の復号(`qp-v12-sample.json`)+効果の下限保証(v12比60%未満)。壊した版での自己テスト2種付き。ブラウザ不要 |
| `verify128.js` | 第50 | PLATEAU建物レイヤ「都市モード」(v1.45.0): 合成Dracoフィクスチャ(`tests/data/plateau-fixture/`=既知座標・既知高さの箱ビル2棟。実データと同じb3dm→glb→Draco+CESIUM_RTC構造。gen-fixture.jsで生成・routeモックで配信)による変換連鎖の数値表明 — 建物頂点がアプリ基準`calculateApparentAltitude`の位置と±0.15m(実測±1mm)で一致・ジオイド補正・高さスパン/ENU配置。ほかに初期値オン(依頼者決定)・メニュー/ctrl双方向連動・テクスチャON/OFF再構築・扇の向き追従・整備都市外表示・localStorage保存。Dracoデコーダはvendor/draco/(sync-apptestが書き換え)。第51で対応表検査をverify129へ移設 |
| `verify129.js` | 第51 | 都市モードの全国化(v1.46.0): 全国対応表`data/plateau-bldg-cities.json`(tools/plateau/make-bldg-cities.jsで生成・448都市ファミリ)の静的リント(base=公式配信・code5桁・bbox日本域・変種≧1・URL形状・重複なし・都庁∈新宿区/皇居∈千代田区)+LODフォールバック配線(tex: lod2Tex→lod2NoTex→lod1/notex: lod2NoTex→lod1→lod2Tex)+ブラウザで_smBldgLoadCitiesの絶対URL化と都庁10kmのbboxフィルタ(8区市ヒット)を実測 |
| `verify158.js` | 第93 | まとめ確認への対応 第1陣(v1.75.0): V1=静的な形(表示変換ヘルパー廃止・銀経へ渡す時の反転2箇所・CSV取込行オフ)・S1=符号の意味(新符号30=旧符号-30と同じ空の点。基本オプション+My辻行の両関数)・S2=スキーマ2→3の読み替え(旧保存-30/行15→新符号30/行-15。非表示中セットのスナップショット内の行も対象。ロード時に一度だけ=normalizeは純粋な丸めのまま)・S2b=スキーマ3は再反転しない(冪等)・S5/S6=プレースホルダー4種+My観測点の緯度経度欄でプラスコード(復号セル中心+標高取得)・S8=移動ボタンの段構成(読み専用段+リセット/位置反映段)。意図更新: verify149(W3/W6/W7と静的表明=新符号へ)・verify153 M1(既定セット常時⭐️)・verify155 U3(表示は収録符号)・verify157 V0(版数ピン移譲) |
| `verify159.js` | 第94 | まとめ確認への対応 第2陣(v1.76.0): 全天儀ctrlの完全複製+ショートカット幅修正。C1=複製部品15個+薄明14項目(グループ計18ラジオ)・C2=並び順(TZ→日付→時刻→アニメ→スライダー→ショートカット→月齢→薄明→表示天体)・C3=TZ表示一致+Now・C4=ショートカット表示一致+ジャンプ+3面相互連動(本家/宙の窓ctrl/全天儀ctrl)・C5=薄明ジャンプ(表示複製+ジャンプ+2面ミラー)・C6=月齢行(表示一致+▶︎で約29.5日先+入力14.8でジャンプ)・C7=ショートカット幅が両ctrlとも360px中央寄せ(はみ出し修正の実測)。意図更新: verify158 V0(版数ピン移譲) |
| `verify160.js` | 第95 | まとめ確認への対応 第3陣(v1.77.0): URL取得ボタン毎の絞り込み+「:QRコード」。U1=プロファイル毎の発行キー(full=97キー全部盛り・tsuji=22キー・soramado=67キー。含む/含まないの両側を検査)・U2=実ボタンの流れ(長いURLをクリップボードモックで捕獲: 辻検索URLにsora系/ss系/パネル無し・宙の窓URLにss系/除外範囲無し+soramado=true)・U3=My辻URLに大気差/気象+行の天の川基準点(オン=offset/オフ=center)・U4=絞ったURLの復元E2E(辻検索は復元・宙の窓は開いた側の既定のまま=漏れなし)・Q1=QRの形(短いURLと同段・初期値オフ)+エンコード→jsQR(本物の復号器)で復号→短いURLが完全一致で読める往復・Q2=容量超過時の「短いURL」案内。**要jsqr**(ハーネスnode_modules)+vendor/qrcode.min.js |
| `verify161.js` | 第96 | まとめ確認への対応 最終陣(v1.78.0): 辻ライン365のオフセット反映+花火頻度スライダー。V1=静的な形(altOffset伝搬・-offAzの回転・v18シード)・D1=365の実測(経路計算を差し替えて365回×altOffset3°の伝搬・方位角10°の回転で線が90°の位置へ・チェックオフでキャッシュを捨てて基準点線へ引き直し・同設定の再呼び出しは再計算なし)・F1=頻度の段の位置(表示モードとばらつきの間・両面)+初期値50・F2=スケジューラの実効(50=500〜1700ms・100=1/3・0=停止)+ctrl連動+normalize丸め・F3=URL発行(full/soramadoに乗り辻検索に乗らない)・F4=URL復元。意図更新: verify125 M0(18版)/M4(v18ゴールデン新設・v17降格)・verify124 L2(v18キー)・verify157 C2(版数非依存へ緩和)・verify160 V0(版数ピン移譲) |
| `verify162.js` | 第97 | ご確認フィードバックへの対応(v1.79.0): C1=ctrlのNowボタン46px固定でスペーサーと一致し日付/時刻ピッカーの左端が揃う(両ctrlの実測+本家参照)・C2=移動量の読みの文字サイズ=タイムゾーン表示と同じ継承サイズ・C3=公開ビルドのfullにss系キー(封鎖中の宙検索)が乗らない・C5=?forecast=1では従来どおり発行・C4=ショートカットの時刻/視高度がパネルを開いた状態で入っている実測(ご報告の再現確認=現行ビルドは正常)。意図更新: verify160 U1(fullのssPresetをhasNoneへ)・verify161 V0(版数ピン移譲) |
| `verify163.js` | 第98 | ご確認フィードバック第2弾(v1.80.0): R1=移動量の読みが移動ボタンと同順(上→前→右)・M1=Myセット切り替え確認メッセージに公開ビルドでは「My宙検索」が出ない(confirmを捕獲して実測)・M2=?forecast=1では入る(従来動作)・H1=ヘルプに無テクスチャビルの説明(調査結果の記載)。意図更新: verify150 B2(読みの並び)・verify162 V0(版数ピン移譲) |
| `verify164.js` | 第99 | リリース前の最終修正(v1.81.0): 観測点の回転ボタン。V1=2描画経路のカメラ角に回転が入る静的検査・B1=カメラ段が6ボタン(左回転・📷4・右回転)の並び・R1=回転の幾何(左回転1°で西へ回り込み・目的点距離の保持誤差<1mm・読みに「回転+1°」・累計角・リセットで全て戻る)・R2=位置反映(回転分が緯度経度へ・回転角クリア・基準方位角の再計算が約+1°を引き継ぐ=二重回転なし)。意図更新: verify150 V1(カメラ位置=移動+回転の形へ)・verify163 V0(版数ピン移譲) |
| `verify165.js` | 第108→110 | 共有URL再訪問題の修正(v1.85.1。第110で版数付け直し=不具合修正のパッチ版。第106調査/第107議論の案A改): 位置情報URL(full)に辻検索条件51+辻メッシュ条件50+発行漏れ4キー(soraGrayscale/soraLabelScale/smBldg/smBldgTex)を発行・復元のmode毎の適用ゲート廃止(有るキーは常に適用=「発行は絞る、復元は絞らない」)・天体色/線種の常時発行+辞書v19(既定値ペア44個)。S1=発行ヘルパーの共用(静的)・U1=発行(条件/4キー/44キー・soramadoには条件を乗せない)・U2=**既定色の短縮URL<変更色**(依頼者の採用条件の常設化・v19)・R1=2回訪問のE2E(1回目から作者の条件が届く・訪問中の変更は凍結で保存されない・2回目も長短URLともURL値で開く=第106の症状の解消)・R2=パターン5(パネル開URL→開いた側で自動実行が作者の条件で走る)・R3=天体色の再現(訪問者の変更色が既定色URLで毎回戻る+保存色は無傷)・R4=旧URL互換(条件キーの無いURLは条件に触らない)。意図更新: verify157 C1(常時発行へ)・verify125 M0(19版)/M4(v19ゴールデン新設・v18降格)・verify124 L2(SEEDS19)・verify164 V0(版数ピン移譲) |
| `verify164.js` | 第99→103 | 読みの「-0.0」表示の解消(v1.85.0): 前/右の分解の丸め残差(±1e-16m)が負に振れても表示精度0.1mで0なら「+0.0」と出す。F1=残差注入(-1e-13m)で「前+0.0m」の実測+基準方位角54方位の掃引(負残差になる60°/100°込み)×右▶︎1回で「-0.0」が一度も出ない。実マイナス(右-1.0m等)の表示は不変(H2bが担保)。版数ピン1.85.0。第102の15チェック+F1の2本=17チェック |
| `verify164.js` | 第99→102 | 観測点の回転ボタン(第102で最終確定・v1.84.0): 体の向きとカメラの向きは同じ(どちらも基準方位角+カメラオフセット方位角)。回転ボタンはカメラ向きボタンの方位側と同じ動きでオフセット方位角を±1°回す(値も入力欄も画面も一緒に回る)。B1=段構成(6段)・H1=右回転でオフセットが+1°(値と入力欄1.0000)+画面のカメラ方向も実測+1°+基準方位角/移動は不変+読みは空のまま+カメラ側「リセット」で回転が戻る・H2=オフセット90°の「前▲」が東へ1m+読みは体の向き基準(回転の項は廃止=オフセット欄が持ち場)・H2b=回転ボタンで読みの前/右を即再分解(北1mの移動が右-1.0mへ)・H3=移動側リセットは位置のみ(オフセットは残る)・位置反映は位置だけ確定しオフセット不変。実装は_smMoveHeading全廃+回転ボタンを_smCamNudgeMoveへ接続 |
| `verify164.js` | 第99→101 | 観測点の回転ボタン(第101で依頼者仕様に確定・v1.83.0): 体の向きを回す=体に載ったカメラも回る(画面は回る)がカメラオフセット方位角は体基準なので値は不変。B1=段構成(6段)・H1=右回転で画面のカメラ方向が実測+1°回り、オフセット(値と入力欄0.0000)/基準方位角/移動は不変+読みに「回転+1°」・H2=回転90°の「前▲」が東へ1m+読みの分解も体の向き基準・H3=リセットで回転も0へ・回転のみの位置反映は何もしない・移動+回転の位置反映は位置だけ確定し回転は残る |
| `verify164.js` | 第99→100 | 観測点の回転ボタン(第100で依頼者仕様に全面修正・v1.82.0): 観測点(観測者自身)中心に「移動の向き」を回す(カメラも位置も動かない)。B1=段構成(1段目=左/後/前/右・2段目=左回転/下/上/右回転・読み・リセット/位置反映・📷4・リセット/カメラ反映の順序)・H1=右回転でカメラオフセット/基準方位角/移動が全て不変+読みに「回転+1°」・H2=北向き+回転90°の「前▲」が東へ1m+読みの分解も回した向き基準・H3=リセットで回転も0へ・回転のみの位置反映は何もしない・移動+回転の位置反映は位置だけ確定し回転は残る。意図更新: verify150 V1(カメラ位置の形を元へ復帰) |
| `verify157.js` | 第91 | 組込天体の色/線種の天体毎URLキー(v1.74.0・v16第2弾=叩き台承認済み): C1=既定値の省略(全て既定なら44キーが0個・変えた天体だけ発行[太陽の色/月の線種/非表示の金星の色])・C2=短縮URLが17版でエンコード・C3=短縮URL往復で色/線種が復元(触っていない天体は既定のまま)・C4=復元の形検査(不正な色値bodyColorMars=red/線種値yes/未知IDは無視・正しい値は効く・ページエラーなし)。発行はリンター対応の44キー配列リテラル・辞書v17は名前シード44個のみ(省略設計により既定値ペア不要)。意図更新: verify124 L2(v17網羅)・verify125 M0(17版)/M4(v17ゴールデン新設・v16降格)・verify156 V0(版数ピン移譲) |
| `verify156.js` | 第90 | プラスコードの短縮形入力(v1.73.0・第78依頼の後段): R1=分解(「MQPJ+2V 港区」「港区 MQPJ+2V」「コードのみ」「複数語の地名」を受け、フルコード/奇数プレフィクス/座標/地名のみは拒否)・R2=recoverNearest相当の性質(基準がセルの近くなら元のセルへ復元[2/4/6桁落ちとも]・半セルを超えた基準では最も近い隣セルへ±1調整)・R3=E2E(searchLocationモック→基準地名ピッカーで選択→選んだ地点の近傍セルの中心が観測点に・標高取得経路も通る)・R4=地名省略時は地図の中心を基準に展開(適用後に地図が動くため期待値は先に計算)。意図更新: verify155 V0(版数ピン移譲) |
| `verify155.js` | 第88 | v16 URL第1弾: 大気差・気象・基本オプションの16キー(v1.72.0・第81ラウンド承認): U1=発行(buildCommonUrlParamsに新16キーが全て乗り値が状態どおり)・U3=短縮URLの往復E2E(既定値以外の状態→?query=→開き直しで大気差true/気圧990/気温5/減率0.0125/offset/-25/星座線/座標順/除外33/辻オフセット込みfalseが復元し、UI(:天の川オプションチェック・±反転の表示角25・:辻オフセットチェック・大気差チェック)にも映る)・U4=壊れた値の丸め(bogus→center・999→360・zzz→aiueo=normalize)+NaNガード(meteoP=abc→既定)。復元はmode必須(実URLは常にmode付き=素のクエリ直書きはmodeを添える)。意図更新: verify124 L2(v16キー網羅+SEEDS16抽出)・verify125 M0(16版)/M4(v16ゴールデン新設qp-v16-golden.json。v15は復号のみ保証で存続)・verify154 V0(版数ピン移譲) |
| `verify154.js` | 第87 | Googleプラスコードのフルコード入力(v1.71.0・怒号の項目15): P1=公式テストベクタ5種(10桁中心/南半球/パディング2桁・6桁/11桁グリッド)+小文字+不正な形(短縮形・+無し・パディング後の桁・1桁後半・不正文字)の全拒否・P2=parseInputでの共存(プラスコード=東京付近・「緯度,経度」従来どおり・数値のみ/地名はnullのままでZipコード保護不変)・P3=E2E(観測点欄に8Q7XMQPJ+2V→復号したセル中心が観測点に設定・標高取得経路[モック]にも乗る)。復号は_plusCodeDecode(約40行の純関数・通信不要)。意図更新: verify153 V0(版数ピン移譲) |
| `verify153.js` | 第86 | Myセットのフィルタ+お気に入り(v1.70.0・怒号の項目14): V1=フィルタ状態4キーの保存の静的検査・M1=部品の存在(フィルタテキスト+「:Myセット名」「:メモ」既定オン+「フィルタ」トグル既定オフ+☆アイコンリンク)・M2=お気に入りの表示順(⭐️でID:3が0,3,1,2へ・appStateの配列は1,2,3のまま=表示だけ・再タップで復帰)・M3=フィルタの実測(「富士」→名前ヒット+メモヒットの2件・「富士 夏」のアンドで1件・:メモオフで名前だけ対象・トグルオフで全行復帰[既定のセット含む])。意図更新: verify123 K3基準線118→123(第85〜86の保存キー5個=bool/文字列でAPP_DEFAULTS既定あり)・verify152 V0(版数ピン移譲) |
| `verify152.js` | 第85 | 基本オプションの辻ライン項目の刷新(v1.69.0・怒号の項目12): V1=静的な形(worker/アニメ両経路のaltOffset・保存キーtsujiLineIncludeOffset)・D1=旧「天の川の基準点」ラジオ撤去+「:辻オフセット方位角/視高度」チェック(既定オン)+検索中心ラジオ2個の存在・D2=検索中心オプションの3面連動(基本オプションで線→辻検索/辻メッシュの状態とラジオが線に・辻検索で点に戻すと基本オプションも追従)・D3=辻ラインへの効きの実測(天の川だけ表示+日時固定で決定的比較: 方位角オフセット10°で線が動く・チェックオフで基準点の線に完全に戻る・視高度オフセット3°でも動く)・D4=線モード(仰角2°)で実線が1本→3本(装飾は両端のみ)。意図更新: verify149 W2/W3/W4(基準点ラジオ撤去に伴う表明の置き換え)・verify151 V0(版数ピン移譲) |
| `verify151.js` | 第84 | 宙の窓ctrlへ日時情報〜薄明メニューの丸ごと複製(v1.68.0・怒号の項目9): C1=複製部品19個(TZ/Now/アニメーション4/時刻スライダー/ショートカット4+時刻・視高度表示/月齢行)の存在・C2=TZ表示が本家と一致・C3=ctrlのNowで現在日時へ・C4=アニメーションボタンの3面連動(ctrl側開始→本家/全天儀ctrl/宙の窓ctrlが揃って点灯・停止で消灯・本家側開始でも複製2面が点灯=従来の片方向同期の穴の修正)・C5=時刻スライダー3本の双方向連動(ctrl130→2:10:00で本家/全天儀ctrlも130・+5分で135へ追従)・C6=ショートカットの表示一致+ctrl側ジャンプで日の出時刻へ+選択の相互連動・C7=月齢行(表示一致・▶︎で約29.5日先の同月齢・入力14.8でその月齢へ)。意図更新: verify150 V0(版数ピン移譲) |
| `verify150.js` | 第83 | 宙の窓ctrlの観測点/カメラ移動ボタン+全天儀ctrlの時刻スライダー(v1.67.0・怒号の項目10/11): V1=静的な形(カメラ位置が未確定移動_smObsNudgeを使う2描画経路+lookAtのカメラ位置補正4箇所)・B1=部品16個の存在・B2=観測点移動のプレビュー(北向きで前/右/上→ENU(1,1,1)にカメラだけ動き位置情報は不変+移動量の読み)・B3=リセットで移動0/読み空欄/カメラ原点・B4=前後左右がカメラの向き基準(東向きの前=東へ1m)・B5=位置反映(緯度経度が3m東4m北へ・地面標高は引き直し(モック123)・観測点高+2m・基準方位角は新位置→目的点で再計算)・B6=カメラ向きボタン(±1°即時反映+入力欄追従+リセットで押す前へ+カメラ反映で確定)・B7=時刻スライダーの双方向連動(130→2:10:00→メイン追従/+5分→135)。意図更新: verify149 V0(版数ピン移譲) |
| `verify149.js` | 第82 | 「:天の川オプション」チェックボックス+±の表示反転(v1.66.0・怒号の項目6): V1=静的な形(getMilkyWayBaseRaDecが実効角_mwEffOffsetAngleを使う・検索記録ctxのmwOff/mwOffAngleも実効角・File出力は内部符号のまま・既存行のmwOffsetEnabled正規化)・W1〜W4=チェック4面⇄基準点ラジオの双方向連動(既定centerで4面オフ+角度入力5個無効→チェックオンでoffset+ラジオ追従+入力有効→オフで無効化しつつ値は保持)・W5=実効角の切替(オフ中は角度30でも中心座標のRA/Dec)・W6=±表示反転の往復(内部-30→表示30・入力25→内部-25・スライダー10→内部-10)・W7=My辻行の行別チェック(初期値は基本オプション追従・オフで_myTsujiMwRaDecが中心座標+入力無効+値保持・全体へ波及しない)・W8=CSV出力は内部符号-13のまま。意図更新: verify148 V0(版数ピン移譲) |
| `verify148.js` | 第81 | 辻検索⇄辻メッシュ検索の同名項目連動(v1.65.0・方針転換=怒号の項目4): V1=起動時統一の静的検査(_tsujiLinkPropagate呼び出し+URLメッシュ復元でmesh正)・K1〜K3=双方向の実UI連動(辻→メッシュ: 検索期間123/月間チェック+活性追従・メッシュ→辻: 基準月齢7.5/許容範囲30/検索中心line)・K4=精度フィルタの非連動維持(辻検索の変更がメッシュ固定精度に波及しない)・K5=オフセット中心角(共有キーmwOffsetAngle)の相手メニュー表示追従・K6=?mode=tsujimesh&tsujiMeshDays=77で開くと辻検索側も77に揃う実URL検査。意図更新: verify147 V0(版数ピン移譲) |
| `verify147.js` | 第80 | 月間URL/CSV+地平線下の閉じ蓋+sync.js(v1.64.0): S1/S2=MederuU sync.jsの自己テスト(検査パターン8種)+宙の辻dry-run実走(吸い上げあり・秘密検査スキップ0件・終了コード0)・U1=短縮URL辞書v15(月間26シード+enc/dec往復+実URLで開いて月間フィルタが復元しチェックボックスに反映)・C1=My辻リストCSV 58列(37列目=月間フィルタ/50列目=精度フィルタ・出力→取込の往復・旧45列は列数判別で月間全オフ補完)・G1=地平線下の閉じ蓋(頂点高度角の上端=−伏角・下端=−89.5°・観測点標高の変更で伏角キーが変わり再構築)。意図更新: verify124 L2(v15網羅+一括出力forEachのリンター拡張)・verify125 M0/M4(15版+v15ゴールデン新設。v13/v14は復号のみ保証で存続)・verify140 T1/T2/U1(15版+58列)・verify146 V0(版数ピン移譲) |
| `verify146.js` | 第79 | 怒号の修正 第2陣(v1.63.0): A1=地点設定の追い越し防止(遅いクリックAより後の速いクリックBが勝つ=チケット_locSetSeq)・A2=可視判定の内側await(判定中に宙の窓へ切替→ポップアップなし/完走→1回。第78修正の同類穴)・C1=宙の窓ctrlの白字化4箇所(計算スタイル実測rgb(238,238,238)+金色見出しrgb(255,215,0)維持)・M1〜M4=月間フィルタ(メニュー26要素+活性制御・_monthFilterAllows真理値表・My辻行フォーム+行データ・結果コントロール2面+_resCtlAllOff+_myTsujiResPass)・T1=地形の近傍z15ブースト(広角90kmで粗ズームとdem5a_png/15の両方を要求=距離環の適応ズームの実測)・T2=タイムゾーン表示(Asia/Tokyo表記+Europe/Parisで「地名検索はOSMのみ」注記。2コンテキスト)。意図更新: verify123 K3基準線94→118・verify145 V0(版数ピン移譲) |
| `verify145.js` | 第78 | 怒号の修正 第1陣(v1.62.0): G1〜G5=GAS findDateRowのユニットテスト(Node内モックシート。行の直接計算=2026-08-14→行228に1発・全行走査なし・閏年366日目→行368・年違いは読み取りゼロで-1・行ズレシートは従来走査フォールバック)・E1=標高グラフのキャンセル(完走ならポップアップ1回・取得中に宙の窓へ切替たら出ない=世代照合を呼び出し元へ)・E2=センサーのモバイル16項目削除+旧key読み替え(SORA_SENSOR_ALIASES・未知keyは既定)・E3=天体軌跡±36時間の連続1本線(太陽1本289点・正時内の日時変更は再構築なし/正時またぎで再構築)・E4=花火11種各1値(新key20号有効・不正keyは10号・選択肢11個)・E5=地名検索のTZ分岐(Asia/Tokyo=GSI先行・Europe/Paris=OSM直行を2コンテキストの試行URL記録で実測)。意図更新: verify99(花火新仕様)・verify144 V0(版数ピン移譲) |
| `verify144.js` | 第68 | Myセット同期の「端末側に未保存の変更」印(v1.61.0・第67の状態遷移表で発見した穴の修正=案a): W1=判定純関数_mySetSyncVerdictの真理値表(時刻一致×印の4象限+同期記録なし)・W2=編集で印が立ち表示👎+localStorageへ永続(合成セット)・W3=時刻一致でも印がある間はstale-local(👍へ戻らない=穴の修正の本体)・V1=印の立つ場所(編集)/消える4箇所(保存・読込・切替取得・解除)/複製の引き継ぎ/一括確認も同じ判定/案内文。ネットワークが要る再確認経路から判定を純関数へ引き剥がしてブラウザ検査だけで検証する形。意図更新: verify143 V0(版数ピン移譲) |
| `verify143.js` | 第65 | セットでリセット+タイル500+「向きの凍結標本」(v1.60.0): W1=Hom/推山リセットの実UI(座標・標高・高さ・名前を全部汚して→リセットで全て既定値に。DEFAULT_START/ENDのname参照) ・W2=**向きの凍結標本**(実検索・冬2026-12-01固定・太陽8日: オフセット−10°が基準の左[azDiff≈−10・dist<1]にヒット=時計回り正の凍結/検索中心「線」が「点」と異なる距離[7.28<15.28]=線分判定が退化していない凍結。第65の不具合報告調査の実測をそのまま常設化。冬固定の理由=夏は最良点が線分終点の先で点=線が数学的に一致するため差が出ない)・W3=宙の窓の検索中心×が+オフセットで画面右(NDC x>0)・W4=タイル500の実測(スライダー→appState→予算fanKey)・V1=式4箇所(辻検索/My辻/メッシュworker/検索worker)の静的検査。意図更新: verify130 R1・verify131 S1/S2/S5・verify141 V1・verify142 V0/V1 |
| `verify142.js` | 第64 | 第63フィードバック4件の修正(v1.59.0): W2=Hom/推山リセットの実UI再現(座標が既定値のまま名前を消して実クリック→東京タワー/富士山が入り直す。修正前の「変化なし経路の素通り」で空のままになる不具合の再現形)・W3=移動→リセットの従来経路回帰・W4=辻メッシュ結果コントロールの精度フィルタ固定の実測(checked+disabledの5状態+全オフFを_resCtlSetに流し込んでも固定が保たれ読み値は◎のみ)・W5=スライダー300→タイル予算(fanKey末尾)300の実測(修正前は150頭打ち)・W1=新初期座標(展望台の理想位置)+初期名・V1=予算のAPP_DEFAULTS.max参照(一元化)+LRU320+精度固定リテラル+入れ直し経路の静的検査。意図更新: verify130 R1/R2(新座標)・verify126 N2(導出6キー標本)・verify131 S1/S2/S5(300/320系)。テスト独立性: verify128へ正午固定を追加(夜に回すと第54の窓明かりでP4/P5が落ちる時刻依存を解消) |
| `verify141.js` | 第63 | 辻メッシュ0件修正+中央寄せ+依頼5件(v1.58.0): W2=不具合の往復(本番経路の_resCtlFromAppStateスナップショット[◎=常時オン]で◎×Nの合成メッシュ行が残る=修正前は0件・○のみ許可では落ちる)・V1=◎クラス正規化の静的+「前後時間指定」2箇所と旧称0箇所+タイル数上限300の4箇所+Myセット「複製」+ズーム_glZoom(20)と-5補正+中央寄せCSS・V2=ヘッダコメントの守り(最初のコメント終端より前に「Version 1.0.0 - 」が残る=文中の終端2文字での早閉じ検知。壊した版でFAILを実測済み)・W1=既定名E2E(初期=東京タワー/富士山→座標移動で空白→既定座標へ戻すと再表示)・W3=最大ズーム実測20・W4=中央寄せの算出スタイル実測。意図更新: verify131 S1(上限300)・verify140 T0(版数ピンを存在検査へ) |
| `verify140.js` | 第62 | 結果コントロールメニュー+曜日URLキー+リストCSV45列(v1.57.0): U2=辻検索E2E(検索完了でメニュー値のスナップショット→コントロールの曜日フィルタで再導出=行減+件数追従→appStateは不変=独立コピー)・U3=My辻の追加絞り込み(_myTsujiResPassの5フィルタ)・U4=辻メッシュの再導出(合成メッシュ1イベント→曜日で0行・メニュー不変)・U5=File出力(生結果→再decorate→共通66列)・U1=45列CSVの往復+37列旧形式互換(29列目=曜日フィルタ)・T1=v14辞書のenc/decラウンドトリップ+14版。意図更新: verify124 L2(辞書網羅にv14キー)・verify125 M0/M4(14版・v14ゴールデン+v13ゴールデンは復号のみで凍結保証)・verify135 W1(呼出3→4) |
| `verify139.js` | 第61 | 配置修正+標高フィルタ改名+曜日フィルタ(v1.56.0): W2=位置情報の配置(GPS+radio-startが観測点名行・辻+radio-endが目的点名行・ボタン6個が縦1列順)・W1=旧称「標高オプション」0箇所(Version History除外)+My辻リストCSV34列目ヘッダ+URLキー不変・X2=辻検索E2E(8日検索→先頭行の曜日だけ通すと全行その曜日+件数減)・X3=My辻decorate(行のdowFilter=土のみで土曜だけ残る)・X1=_dowFilterAllowsの性質(無効/全未チェック=素通し)・X4=メニュー連動+localStorage保存。verify123のK3基準線は78→94へ意図更新(曜日16キー) |
| `verify138.js` | 第60 | 機能改善3+検索エリアのメニュー側(v1.55.0): V1〜V3=観測点名/目的点名のE2E(ジオコーディングはsearchLocationを同一形状スタブに差し替え。Enter→候補ダイアログ→確定で座標反映+名前保持・他の手段の座標変更で空白へ・入力途中(フォーカス中)は保護[パネルがminimizedだとfocusが効かないため開いてから検査]・取得ボタンが名前を使う/空白なら新規○○名)・V4=検索エリアのメニュー⇄ctrl双方向連動・U1=保存しないことの静的検査(startName/endNameキー不在)+名前欄の行位置 |
| `verify137.js` | 第59〜60 | 辻メッシュのフィードバック(v1.54.0。B1は第60の認識ズレ訂正で書き直し): B1=詳細リスト行クリック相当のジャンプ後、優辻ピンは強制配置せず「その辻時刻での最良画素」(テスト内でアプリと同じ定義から検算した画素・観測点とは別の場所)に立ち、zIndexは900のまま・観測点位置の最前面=観測点マーカー(選択できる=依頼者要件)・B2=辻マーカーホバーで詳細リスト連動(合成メッシュ4画素+1×1グリッドで_tmPixAtLatLng解決。確定固定中は更新しないの負の検査つき)・B3=メッシュ画素リストの検索中心列(視半径の右・23列・値point)・T1=ラベル改名+強制配置/_tmSyncPinZの無いことのテスト(Version History除外で数える)。合成メッシュのビン索引はbinSize360×1ビンで全画素走査にする最小構成 |
| `verify136.js` | 第58 | マーカーポップアップの一網打尽+検索中心列(v1.53.0): B1=観測点/目的点/My観測点/My目的点の4マーカーで「クリックで開く」実挙動(壊した版で4種FAILになることを実測済み。優辻ピンは独自経路の表明・辻/白マーカーはレイヤclickで対象外)・B2=辻検索画面E2E(視半径の右に検索中心・23列・point→line切替も実検索)・B3=共通CSV(66列・視半径の右・旧データ=centerMode未定義はpoint)・S2=メッシュ画素リストに列が無いこと(デッサン04通り。詳細リストとの違いに注意)。列数ピンはverify96(T2)/98(V3/V5)を65→66へ意図更新 |
| `verify135.js` | 第57 | リファクタリングB第2弾④(v1.52.0)=300ch以上の最後の2組: _searchSunMoonRiseSet(日月出没4探索try 325文字×2)+_pushMyTsujiResults(My辻結果の平坦化303文字×2+単発File出力の同型第3現場もついで統合=呼出3)。無いことのテスト+実挙動(東京の日の出/日の入が有限時刻・平坦化3行の形)。dup地図73グループ・300ch以上0(累計92→73) |
| `verify134.js` | 第56 | 地図2.5D「3D風ビル」(v1.51.0・案A)+リファクタリングB第2弾③: 静的(最適化ベクトルタイルXYZ+BldA+種別擬似高さmatch式3103→100/3102・3112→40/他10・合成標高分岐=_syntheticElevAtPix15の定義1+代入2)+ブラウザ(レイヤーリストのチェック初期オフ・ONでfill-extrusionレイヤ生成+visible+ピッチ60°+ベクトルソース登録・OFFで不可視+ピッチ0。easeTo完了はポーリング待ち。タイル実体はroute遮断=スタイル状態を検査)。あわせてverify124のL4を4行→5行へ意図更新 |
| `verify133.js` | 第55 | リファクタリングB第2弾②(v1.50.0)=ワーカープールの型+日時ピッカーの統合: 無いことのテスト(配車/解放の実体=_makeWorkerPoolの1箇所・_makeWorkerPool/_bindDateTimePair=定義1+呼出2・ピッカー本体1箇所)+プールの外形(tsujiPool.size健在・init/runTask/terminateAll・工場は独立状態)+日時ピッカー配線(全天儀ctrl/宙の窓ctrlのchangeでcurrentDate反映)。深いE2Eはverify97(全天儀ctrl)/98(辻メッシュ)/119(プール解放)/120(モバイル上限)が担当 |
| `verify132.js` | 第54 | 夜の磨き+リファクタリングB第2弾①(v1.49.0): 夜の底の純関数(月なし0.15/満月が高い夜0.32/高度・輝面比で比例)・フィクスチャで窓明かり切替の実挙動(正午=窓タイルなし→夜=単色ビルだけ窓タイルON+壁面UVあり・写真ビルは自分のテクスチャのまま)・月連動の床の配線(写真ビルの夜の暗さ=_smBldgMoonNight()と厳密一致)・リファクタ第1弾の無いことのテスト(z15標高チェーン1箇所のみ・riseSetCache 0箇所・工場関数=定義1+呼出2)。あわせて死にフィールドappState.riseSetCacheを発掘・削除 |
| `verify131.js` | 第53 | 都市モードの「表示タイル数」スライダー(v1.48.0): 既定値smBldgTiles=35(min1/max150)・「保存しない」の無いことのテスト(saveAppStateペイロード/復元リスト不在+localStorage実測=毎回初期値)・メニュー/ctrl双方向連動(80枚)・正規化(999→150/0→1/ゴミ→35)・予算がfanKeyに乗り変更で再選択(フィクスチャ)・LRU上限160(表示中タイルを追い出さない)の表明 |
| `verify130.js` | 第52 | 都市モードの遠景対応+夜馴染み(v1.47.0): 観測点初期値=東京タワーの建物の外(35.658595, 139.745335。URL短縮辞書v13は凍結のまま=座標ペア不在の表明つき)・奥行き上限200km・太陽高度減光の純関数(昼1.0/夜0.26床+青寄り/薄明中間)・タイル選択スコア=見かけの高さ順(高層@65km>低い街@5km・50mクランプ)・都市の扇足切り(中=真/真後ろ=偽/扇内遠方=真)・フィクスチャで正午→真夜中の材質色追従(幾何は同一参照のまま)。初期値変更で導出6キー(soraBaseAz等)の凍結標本を更新(verify126のN2が変更を検知→意図的更新の手順どおり) |
| `verify126.js` | 第41 | リファクタリングA(APP_DEFAULTS単一情報源化)の挙動不変の表明: 初期状態のappStateスカラー161キーが凍結標本(`tests/data/appstate-defaults.json`)と一致+normalizeAppStateの挙動が凍結標本(`normalize-probes.json`=161キー×8種の崩れた値)と一致+表の内部整合リント+旧h265→h264移行+HTML入力欄の起動時上書き実測。既定値・正規化規則を意図的に変える時だけ標本を更新する |
| `verify127.js` | 第42 | リファクタリングB第1弾(ファイル入出力の型)の無いことのテスト: ファイル選択の殻=pickTextFileの1箇所のみ・FileReader=1箇所・createObjectURL=4箇所(テキストDL/宙の窓DL/動画src/画像src)・CSV行分割正規表現=splitCsvLinesの1箇所のみ+CSV出力/入力の端到端(ファイル名/BOMバイト/コメント行スキップ) |
| `verify122.js` | 第38 | 検索ボックスの正規化規則(英字入力は無変換のままOSMでヒット・日本語入力は従来どおり全角化[+0xFEE0写像で期待値生成]・数値のみはOSMへ問い合わせず検索スキップ=郵便番号ジャンプ防止の維持。GSI/OSMはrouteモック) |
| `verify121.js` | 第37 | 海外対応+URLセッション限定化+懸案対応(URLで開いたセッションは変わったキーだけ凍結し再訪で自分の保存値へ・表示中セット編集で行アイコン即👎・Drive指紋=アップロード内容・花火ラベル金字・モックTerrariumで海外のスポット標高/標高グラフ/可視判定+域外GSIリクエスト0件+国内海は従来どおり・タイル取得タイムアウト+失敗ネガティブキャッシュ) |
| `verify120.js` | 第36 | 後日課題全実施+整合性修正(時刻ラベルsymbolレイヤ[DOM 0個+ローカルglyphs取得]・標準ピン+観測点/目的点マーカー永続化+ポップアップ維持・花火設定の復元・指紋の簿記非依存・[New]=前回同期からの変更・空データ上書きガード・封鎖中のMy宙検索温存・辻検索URLの基準値保護・期間clamp・閉じるボタン5種・モバイルワーカー上限) |
| `verify119.js` | 第35 | スマホ辻メッシュ不具合修正(全検索エリアでキャンバス最大辺2048px以下[旧3×3=6144/5×5=5120]・再検索3回とも毎回visible+旧objectURLのrevoke・実チェックボックスOFF→ONの再描画なし復帰・attributionのⓘ[compact]・setDataが天体数に依らずリセット+一括の2回)+監査第2弾v1.37.1(タイルキャッシュLRU上限の追い出し実測・優辻ピンmouseenterのタッチガード+ツールチップoffset毎回設定・検索完了後のワーカープール解放[ソース検査])+第3弾v1.37.2(画像ロード失敗の安全網=フラグ解除+ステータス表示+再描画復帰・クレジット重複解消・表示天体OFF→ONの辻マーカー画像復帰) |

## 道具のテスト(ClaudeMederuU/tools/。回帰と一緒に回す)

MederuUの道具のテストは道具と同じ場所に置きます(将来MederuU本体へ道具・テスト・標本の三点セットで引っ越すため。第45ラウンド〜)。
サーバ・Playwright不要(node単体)なので、回帰のついでに次の2本を実行します:

- `node ClaudeMederuU/tools/koushi.test.js` — Koushiレンダラのゴールデン13チェック(標本=koushi-golden.json。更新は`--update`+目視確認)
- `node ClaudeMederuU/tools/anchor.test.js` — 構造指紋の性質テスト14チェック(整形/コメント不変・改名検出・文字列/テンプレート/正規表現の波括弧・実物script.jsスモーク)

APP_VERSIONピンの規約(最新verifyのみ)はアプリのverify群だけの話で、道具のテストは対象外です。

## 実行方法(ローカルハーネス)

ネットワーク遮断環境でも動くよう、CDNのライブラリをローカルに置いた複製ディレクトリをHTTPで配信して実行します。

1. 作業ディレクトリ(例 `apptest/`)にリポジトリのアプリ一式(`index.html` `script.js` `style.css` 各ワーカー)をコピー
2. `vendor/` に以下を配置し、`index.html`とワーカーのCDN参照をvendorパスへ書き換える
   - astronomy-engine 2.1.19 (`astronomy.browser.min.js`) / three 0.160.0 (`three.min.js`)
   - geographiclib-geodesic / geographiclib-dms
   - maplibre-gl 4.7.1 (`maplibre-gl.js` `maplibre-gl.css`) (いずれもnpmから取得可)
   - ※leaflet 1.9.4は第32ラウンド(手順4)の撤去に伴い不要になった(verify102の実ネットワーク検証を旧版で行う場合のみ歴史的に必要)
3. `python3 -m http.server 8099 --bind 127.0.0.1` で配信
4. `node tests/verify99.js` のように実行(要 `playwright-core` とChromium。CIやサンドボックスでは
   `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader` でWebGLをソフトウェア実行)

- Google Drive/Sheets を使うテスト(verify96のT3等)は `window.fetch` をテスト内でモックしています(実アカウント不要)
- 全verifyはPlaywrightのroute abortでローカル(127.0.0.1)以外への実アクセスを遮断しています(第22ラウンドから。テスト方針の仕組み化)
- APP_VERSIONの版数ピンは**最新のverifyのみ**に置きます(第24ラウンド〜。旧verifyのピンを毎回更新する保守を廃止。ピンは常に番号最大のverifyが持つ=第108時点はverify165)
- 第36ラウンドで辻メッシュ画像はcanvasソース化(同期反映)・時刻ラベルはsymbolレイヤ化(ローカルglyphs: `fonts/SoraSans/0-255.pbf`。ハーネスへfonts/の同期が必要)・地点ピンは標準maplibregl.Marker化(識別クラスは要素に残る)。verify119の一部(旧objectURL表明)とverify111/113は実挙動へ保守済み
- 第33ラウンドの機能封鎖により、封鎖対象を検証するverify(100/101/103/104/105/106/107/108/112/116)は`?forecast=1`を付けて実行します(封鎖の検証はverify117/118)。第34ラウンドで封鎖UIはindex.htmlからforecast-features.htmlへ分離され、?forecast=1時にfetch注入で復元されます(ハーネスへ同ファイルの同期が必要)
- 第32ラウンド(手順4=Leaflet撤去)で、全verifyがMapLibre上で走ります。Leaflet内部を検査していた3本(99/103/105)はGLソース/DOMの等価チェックへ移植済み。`?maplibre=0/1`は無視されます
- 視覚検証・実時間アニメ・実マウス操作系のverify(97/98/99/113など)は、実行負荷でまれに1〜2チェック落ちることがあります(単独再実行でPASSすればフレーク。頻発時は待ち時間を延長する。verify113のドットクリックは描画完了のポーリング待ちで対策済み)
- 判定は `PASS/FAIL` を標準出力に出し、FAILが1つでもあれば終了コード1

## 実行方法(実ネットワークハーネス。verify102以降)

ネットワーク許可済みの環境では、リポジトリ直下をそのまま配信して素のindex.html(CDN書き換え無し)で検証できます。
Claude Codeクラウド環境ではエージェントプロキシの都合で、ChromiumにプロキシCAのNSS登録と
`--proxy-server=$HTTPS_PROXY` `--proxy-bypass-list=<local>` `--ssl-version-max=tls1.2` が必要です
(詳細は `verify102.js` 冒頭のコメントを参照。TLS証明書検証は有効のままです)。
